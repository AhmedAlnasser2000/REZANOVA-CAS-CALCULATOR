import type { LanguageCatalog } from '../../types';

export const englishSettings = {
  title: 'Settings',
  language: 'Language',
  languageHelp: 'English is the only installed language for now.',
} satisfies LanguageCatalog['settings'];
