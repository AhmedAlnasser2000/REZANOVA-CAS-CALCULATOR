# Current State

Last updated: 2026-07-01

## Purpose

This file is the live Calcwiz state map. It should describe what is true in the current repo, not retell every milestone that got us here.

Historical milestone detail belongs in the dated journals, session folders, and `.memory/research/milestones/current-state-milestone-archive-2026-06.md`. Do not rewrite historical records to remove old names; they are evidence. Do not present old compatibility names as active current behavior.

## Active Context

- Workspace: `Calcwiz`.
- Live checkout: `/home/ahmed/Downloads/Calculator`.
- Public identity posture: the primary public name is `REZANOVA CLASSWIZ CALCULATOR`; `Calcwiz` and `Classwiz` are friendly aliases only. Compatibility slugs such as `calcwiz-desktop`, `calcwiz_desktop`, and `com.ahmed.calcwizdesktop` remain unchanged unless a dedicated compatibility migration is approved.
- Public overview posture: current-facing docs should present the app as a Linux-first preview that is still advancing, with guided workspaces, Workspace Tabs, live Order of Execution traffic control, Formula Viewer, bounded Equation/Calculus progress, and English-only Language infrastructure. Graphing, Spreadsheet, full Settings/History/Variables pages, Surface Protocol, plugins, external software development kit, Complex numeric roots, Complex locus/set output, full Risch certificates, and Arabic/right-to-left localization remain future.
- Surface Protocol posture: the hostless spine now has DTO firewall, capability manifest, versioned error, read-only event adapter, pure query, conformance-test, and policy/vocabulary registry foundations. Surface Protocol has a dedicated `src/lib/surface-protocol/` boundary with compact result DTOs, pure `DisplayOutcome` summary mapping, a Calculate/Equation-only manifest, supported-version helpers, structured errors, curated lifecycle DTO mapping from the Order of Execution event outbox, snapshot-input queries for current result summary, workspace info, and safe settings with optional angle unit only, `test:surface-protocol` boundary/serialization coverage, and internal field/vocabulary policy registries that do not change response DTO shapes. Host commands, mounting, History, Variables, Graphing, tabs, plugins, remote compute, raw event payloads, diagnostics, app-global state facades, and external software development kit work remain unavailable.
- Current development posture: architecture boundaries are now enforced by repo validators; new infrastructure should be demand-driven, not speculative.
- Release posture: `v0.2.0` is the current Linux-first early preview target. The repo release path covers AppImage, Debian/Ubuntu `.deb`, and RPM artifacts; native Arch packaging and full CAS parity are not claimed.
- Commit convention: `0` milestones are audit/docs/readiness only; implementation or behavior/editing milestones start at `1` or higher.
- Algorithm prerequisite policy: nontrivial algorithms must declare required representation, symbolic primitives, facts/assumptions, validation, route evidence, readback/presentation, and tests before implementation. Missing prerequisites must be built before or alongside the algorithm, or the algorithm must stop and record the gap.
- Current algorithm-planning posture: the external Equation algorithms handoff is source context only, and the prerequisite gate remains active for nontrivial algorithms. Cardano is live for direct and safely rational-cleared degree-3 selected-target polynomials in Complex Exact and Real Exact; Ferrari is live for direct and safely rational-cleared degree-4 selected-target polynomials in Complex Exact and Real Exact through the distinct top-level `quartic-ferrari` family. The shared generated handoff can carry structured formula payload evidence for Real wrapper routes while preserving wrapper facts, candidate validation evidence, scoped case facts, and denominator exclusions.
- Real wrapper formula posture: generated Cardano/Ferrari formula payloads are live for Real Exact one-layer square-root, absolute-value, square-power, odd-power exponents `3,5,7,9,11`, higher even-power exponents `4,6,8,10,12`, nth-root indices `3..12`, existing non-target exp/log bases, and existing `sin`, `cos`, and `tan` carriers. Affine single-root radical shells `A*root(F,n)+C=R` are live for exact integer root indices `2..12`. Mixed algebraic square-root wrappers may opt into Real formula payloads only for a single selected-target square-root carrier plus target-free or non-carrier additive companion. Mixed exp/log wrapper formulas are live only for affine shells around one selected-target exp/log carrier with target-free companions, carrying symbolic coefficient nonzero facts and target-free exp/log domain/base facts. Mixed trig wrapper formulas are live only for Real Exact affine shells around one selected-target trig carrier and same-argument sine/cosine wrappers when generated degree-3/4 branches can delegate to Real Cardano/Ferrari. Two-layer algebraic nested wrapper chains now opt into Real formula payloads when the exact depth-2 selected-target carrier chain uses square-root, absolute-value, square-power, odd-power, even-power, or nth-root wrappers; eligible generated degree-3/4 final branch equations render as grouped `Nested Formula Cases` with full layer provenance and scoped Cardano/Ferrari details.
- Complex wrapper posture: Complex Exact affine shells around exactly one selected-target exp/log/trig carrier are live only when isolation produces an exact constant carrier equation inside the first wrapper preimage slice. These wrappers delegate `e^F=value`, `ln(F)=value`, `log_b(F)=value`, `sin(F)=value`, `cos(F)=value`, or `tan(F)=value` to existing Complex preimage solving, preserving principal-log semantics, integer branch families, nonzero/domain evidence, denominator exclusions, and `complexExactForm`. One-layer Complex power wrappers `F^n=R` and `A*F^n+C=R` are live for `n=2..12` when all generated branch equations stay inside compact Complex-capable linear, rational, factorable-polynomial, or algebraic-isolation routes and the final visible branch count stays under the existing Complex special-form budget. Power-wrapper output uses all-branch `PrincipalRoot_n(...)\omega_k` definitions and may include symbolic affine shell facts such as `A\ne0`. One-layer Complex principal root wrappers `sqrt(F)=R`, `root(F,n)=R`, and `A*root(F,n)+C=R` are live for `n=2..12` when the isolated root value is inside or guarded by the principal-root image, the target appears only inside `F`, and the powered carrier equation stays inside compact Complex-capable routes. Exact values outside the principal image stop with a controlled error; unknown symbolic values carry `Re/Im` half-plane or `arg` sector facts. Complex mixed algebraic root wrappers are live only for one selected-target principal root carrier plus a bounded selected-target algebraic companion when the powered branch equation stays compact; target-dependent principal-image facts such as `b-z` are preserved in principal-image/candidate-guard detail sections rather than global Valid When. Complex abs wrappers are policy-locked as magnitude/locus semantics and remain unsupported until set/locus output exists. Complex nested wrapper readiness now has an internal/test-facing depth-2 substrate for root/power algebraic chains: it tracks layer provenance, principal-image requirements, all-branch power definitions, generated final equations, and compact-route eligibility without enabling a new visible nested wrapper route. Symbolic exp/log/trig shell coefficients/constants, explicit generated Complex Cardano/Ferrari wrapper formula readback, same-argument mixed sine/cosine, trig products, mismatched/nested trig wrappers, two-root mixed radicals, nested mixed radicals, abs companions, over-cap mixed algebraic branches, and non-Exact or numeric-interval routes remain deferred.
- Wrapper formula deferrals: default generated handoff, Complex absolute-value, mixed-radical, and visible nested wrapper routes, wrapper-generated explicit Complex Cardano/Ferrari formula readback, true two-selected-target radical formula widening, target-dependent exp/log companions, multi-carrier/log-combination exp/log transforms, target-in-base exp/log formula widening, depth-3 nested wrappers, nested mixed algebraic or trig wrappers, trig products, mismatched trig arguments, nested trig wrappers, additive/mixed selected-target carrier widening beyond the locked single-carrier shells, symbolic carrier quadratics, visible `RootOf`, implicit-root display, persisted Display schemas, OOE, History, app-state, and Tauri remain deferred. Degree-3/4 Complex wrapper solving is not globally banned; it must avoid explicit Cardano/Ferrari formula expansion and use compact validated representations such as factorable branches, special-form `PrincipalRoot`/omega readback, or a future approved compact algebraic-root policy.
- Wrapper formula hardening posture: the first three live Real algebraic wrapper consumers have focused regression coverage for grouped case labels, local formula definition scoping, exact-zero label collapse, exact-positive legacy-only versus formula-backed branch behavior, copy compatibility, and DisplayPanel case rendering. Later odd/even power wrapper slices reuse the same grouped generated-formula substrate.
- General power wrapper policy posture: future Real wrapper formula expansion should keep proceeding one slice at a time. Odd powers `F^{2k+1}=rhs` are live only for odd exponents `3,5,7,9,11`, higher even powers are live only for even exponents `4,6,8,10,12`, and nth-root wrappers `\sqrt[n]{F}=rhs` are live only for exact integer indices `3..12`. Complex power wrappers are all-branch power relations, not principal-root functions; the live Complex route generates `PrincipalRoot_n(...)\omega_k` carrier branches and delegates only to compact Complex-capable branch solvers. Complex square-root and nth-root wrappers are principal functions; the live root-wrapper route validates or guards the isolated value against the principal-root image before solving `F=V^n` through compact routes. Role-lock tests protect the distinction between outer wrapper index/exponent, inner carrier polynomial degree, and readback method: Real wrappers may delegate inner cubics/quartics to Real Cardano/Ferrari, while Complex compact high-index powers and root wrappers use compact branch/algebraic readback and explicit generated Complex Cardano/Ferrari wrapper formulas stay blocked. Deeper nested, broader mixed, and Complex wrapper formula handoff remain separate validation/readback slices.
- Cardano/Ferrari coefficient readback posture: direct, rational-cleared, and generated-formula Cardano/Ferrari producers distinguish generic full-slot symbolic templates from specialized or concrete coefficient equations. Generic templates such as `a*x^3+b*x^2+c*x+d=0` or `a*x^4+b*x^3+c*x^2+d*x+f=0` may keep compact helper-symbol primary readback. Specialized symbolic, mixed, missing-slot, zero-slot, compound, generated, and concrete exact-rational cases substitute collected coefficients into primary branch/case rows and keep derivation details collapsed/local. Real Ferrari now closes non-generic primary `caseMath` rows by inlining depressed-quartic helpers such as `p/q/r/P/Q/Delta/t/Y/F_sigma` into both root expressions and row-local conditions; Complex Ferrari PrincipalRoot readback remains unchanged. Producer-side formula readback polish removes narrow exact-arithmetic, neutral-term, and known-nonzero self-ratio noise such as `2/2`, simple rational powers, `0+` shifts, and `(a-bd)/(a-bd)` from primary Cardano/Ferrari rows and related details before p/q/discriminant/radicand/case strings are built; it is not broad algebraic simplification or Display-side cleanup. Real formula `caseMath` answer rows carry row-local conditions as explicit `when` guards, while global `Valid When` stays reserved for whole-result facts such as leading-coefficient facts, denominator exclusions, and wrapper/domain facts. Candidate-validation rejects preserve real evidence in an `Extraneous Solutions` detail card across guarded symbolic validation, composition/generated wrappers, substitution, guarded polynomial carriers, numeric interval validation, and polynomial systems. The card is a visible-by-default detail section, not a Display/History/OOE/app-state/Tauri schema change.
- Exp/log formula posture: Real Exact exp/log generated formula handoff remains top-level opt-in only, and the route plan keeps `exp-log` available when the same selected target also appears inside a denominator such as `\ln((z^3+z+1)/(z-m))=b`; the generated equation can then safely delegate to Cardano/Ferrari while preserving denominator exclusions. Affine shells such as `A*ln(F)+C=R` and `A*e^F+C=R` remain on the existing `Parameterized Exp/Log Solve` readback, not a new grouped mixed section, and preserve local facts for `A\ne0`, target-free log arguments, and symbolic exp/log bases.
- Formula presentation performance posture: heavy Real formula `caseMath` answers use compact-first Display rendering. Large, multi-group, or visually dense guarded case rows, including four-row direct specialized Cardano/Ferrari answers with very large substituted formulas, show a metadata-only responsiveness summary and explicit expansion button before any row, condition, or raw-preview formula content mounts. Expanded compact/heavy case answers now reveal formula rows progressively in small frame-scheduled batches, defer row math rendering while scheduled, and cancel stale row reveals when the result signature changes, unmounts, or a new solve/edit replaces the result. Giant individual rows are now cost-budgeted after expansion: over-budget rows render lightweight opt-in placeholders with no formula `MathStatic`, no condition `MathStatic`, no raw LaTeX preview, and a per-row `Show formula row` action. Formula-case detail sections such as `Trig Formula Cases`, `Real Cardano Cases`, and `Real Ferrari Cases` share the same budgeted `caseMath` renderer and lazy-mount while collapsed, so they cannot bypass the answer-card budget path. Legacy/replayed raw case LaTeX with `\substack` is parsed into non-persisted structured `caseMath` display blocks where possible instead of being shown as raw `\substack` text. Deferred `MathStatic` no longer performs symbolic-display normalization before the deferred placeholder. Expanded cheap rows still render `formula` with row-local `when` guards; small direct formula cases remain fully visible. Global `Valid When` facts stay reserved for whole-result facts such as denominator exclusions, wrapper facts, leading-coefficient facts, and exp/log domain facts. Collapsed math-heavy `mixed` detail sections lazy-mount, while cheap prose-only details still mount eagerly. Extremely heavy caseMath source cards can now open a session-only Formula Viewer tab built from structured Display blocks; the source result stays compact, viewer tabs are not `ModeId`s and do not launch jobs or change Equation runtime state, and one viewer tab is reused per result signature. The Formula Viewer tab owns virtualization for extremely tall formula artifacts: it builds a virtual block/row item list from structured Display blocks, renders only viewport-plus-overscan items inside `.formula-viewer-scroll`, keeps collapsed detail bodies unmounted, preserves row-budget placeholders for giant rows, and resets measurement/row state when the result signature changes. Formula Viewer readability now adds viewer-local math sizing with `125%` as the default and `100%`, `150%`, `175%`, and `200%` alternatives, with viewer-local button contrast fixes for the size controls and `Show formula row` action. The proposed focused row inspector was removed as redundant; row inspection remains through the virtualized list and per-row `Show formula row` reveal. This remains Display/UI scheduling and tab-surface infrastructure only, not a solver, copy, History, OOE, app-state, Tauri, or persisted schema change.
- Formula count-cue posture: Answer cards and Formula Viewer artifacts may show type-aware count summaries derived from structured Display blocks. Finite branch-list answers use root counts, while finite branch-list answers with candidate-local guards may show candidate-root counts; grouped formula `caseMath` answers use branch-family and guarded-row counts; ungrouped formula `caseMath` answers use guarded-row counts. Answer, Valid When, detail, and Formula Viewer cards are collapsible display surfaces by default-open policy. These cues and collapse affordances are display metadata only and do not change `copyLatex`, History, OOE, app-state, Tauri, or persisted result schemas.
- Trig wrapper formula posture: Real Exact one-layer trig formula handoff is live for existing `sin(F)=rhs`, `cos(F)=rhs`, and `tan(F)=rhs` composition carriers, affine shells such as `a*sin(F)+c=d`, and same-argument mixed sine/cosine forms such as `A*sin(F)+B*cos(F)=C` when no numeric interval route is active. Generated direct or safely rational-cleared cubic/quartic equations may delegate to Real Cardano/Ferrari formula payloads while preserving periodic branch provenance, integer-parameter facts, sine/cosine range facts, mixed amplitude facts, angle-unit semantics, row-local case guards, closed non-generic Ferrari rows, and denominator exclusions. Mixed trig generated formula handoffs keep scaled `atan2` products explicit and use the recognized `\operatorname{atan2}` spelling so browser ComputeEngine reparsing cannot turn coefficients into unsafe tuple/error fragments. Exact sine/cosine endpoint overlaps are deduped before formula handoff. Complex trig wrappers, nested trig wrappers, trig products, mismatched trig arguments, target-dependent companions, and broad generated route-order changes remain deferred.
- Wrapper roadmap posture: Display performance/readability work is sufficient for current wrapper work; future Display polish is ordinary UX backlog, not a wrapper blocker. `EQUATION-REAL-WRAPPER-COVERAGE-BUNDLE1`, `EQUATION-REAL-MIXED-RADICAL-WRAPPER-BUNDLE1`, `EQUATION-REAL-NESTED-ALGEBRAIC-WRAPPER-FORMULA1`, `EQUATION-REAL-MIXED-EXPLOG-WRAPPER-FORMULA1`, `EQUATION-WRAPPER-INPUT-MIXED-TRIG-AUDIT-BUNDLE1`, and `EQUATION-WRAPPER-SAFETY-COUNT-POLISH1` close the Real wrapper formula/input/readback track enough for Complex catchup. `EQUATION-COMPLEX-WRAPPER-BASELINE-LOCK1`, `EQUATION-COMPLEX-PREIMAGE-WRAPPER-CATCHUP1`, `EQUATION-COMPLEX-POWER-WRAPPER-CATCHUP1 + EQUATION-COMPLEX-ROOT-WRAPPER-POLICY1`, `EQUATION-COMPLEX-PRINCIPAL-IMAGE-INEQUALITY-SUBSTRATE1`, `EQUATION-COMPLEX-ROOT-WRAPPER-PRINCIPAL-IMAGE1`, `EQUATION-COMPLEX-ABS-WRAPPER-POLICY0`, `EQUATION-COMPLEX-NESTED-WRAPPER-SUBSTRATE1`, and `EQUATION-COMPLEX-MIXED-ALGEBRAIC-WRAPPER-CATCHUP1` lock the first Complex boundary suite, exact-constant single-carrier exp/log/trig wrapper handoff, compact all-branch Complex power wrappers, reusable principal-image facts, one-layer principal root wrappers, Complex abs locus semantics, internal depth-2 nested readiness, and first compact mixed algebraic principal-root catchup. The Complex catchup roadmap remains at `.memory/research/roadmaps/equation-complex-wrapper-catchup-roadmap.md`; future Complex wrapper work should be readability-first: explicit generated Complex Cardano/Ferrari wrapper readback is retired, but compact non-Cardano/Ferrari degree-3/4 root representations remain valid candidates.
- Memory convention: meaningful code/tooling/UX/workflow commits need same-commit memory records or an explicit no-memory-needed note.
- Daily catch-up is enforced: `.memory/current-state.md` must be at least as new as the newest journal/session day before meaningful commits pass `npm run test:memory-protocol`.
- Current Equation numeric UX posture: deterministic polynomial/rational numeric fallback now uses the shared polynomial root engine with Aberth-Ehrlich for degree-3+ numeric roots, direct linear/quadratic paths, Newton polishing, coefficient-scale, nearest-root-separation, residual, repeated/clustered-root, and higher-precision guidance diagnostics, plus rational quotient-zero fallback eligibility before nonlinear search. Nonlinear numeric fallback searches progressively and stops after a wider pass adds no new accepted roots or unique extraneous values; numeric details split hard domain/exclusion facts from capped probe/search diagnostics, search/segment complexity, and textbook `Extraneous Solutions`; periodic exact solving prefers compact symbolic preimage families before numeric enumeration, including quotient-zero sine families and shifted log/exponential carriers; the existing Numeric Interval Solve panel is permanently reachable as compact `Enable Numeric Interval` on symbolic Equation surfaces and only diverts Run/F1/EXE after explicit enablement. Interval runs auto-substitute stored non-target values for parameterized inputs, protect the selected target, stop with missing-value guidance when needed, stay local to the chosen window, cap large root lists with narrowing guidance, and use the internal numeric kernel contract with ITP as the maintained bracketed sign-change refinement method. Interval/manual/auto-search now receive bounded segmentation probes for solved denominator exclusions, log/root/fractional-power boundaries, affine trig poles, and sampled discontinuity hazards; hard facts remain separate from probe/segmentation diagnostics and excluded boundary candidates are validation-visible extraneous evidence. Smooth non-periodic log/radical/exp equations may use bounded auto-search after exact and deterministic routes miss, while selected-target occurrences that are only inside periodic carriers remain interval-first. Precision v1 is diagnostics/readback only: it does not add arbitrary-precision execution. `EQUATION-NUMERIC-ROUTE-ORCHESTRATION-CLOSEOUT1` locks the Real numeric route order as exact symbolic, deterministic algebraic numeric fallback, periodic/dense interval guidance or explicit interval solve, non-periodic bounded nonlinear auto-search, then controlled unsupported guidance.
- Current numeric domain policy posture: `NUMERIC-METHODS-DOMAIN-POLICY-WIDENING1` locks shared numeric work as neutral primitives and contracts only. Equation owns root solving and target protection; Statistics owns simulation/resampling/Monte Carlo; Limits own directional/local approach semantics; Differentiation owns derivative-at-point approximation; Calculus owns quadrature, improper-integral policy, and integration-specific safety/readback. The integration pipeline Layer 5 fallback is adaptive quadrature for definite integrals, not Equation root solving.

## Agent Ownership

- Default agent: Codex.
- Other agents are opt-in and should stay inside explicitly assigned lanes.
- When another agent has dirty work in a lane, do not touch it unless the user asks for coordination or repair.
- For repo-structure work, prefer one clear milestone at a time with verified gates and same-commit memory.

## Product Posture

Calcwiz is a desktop math workbench, not a generic quick calculator. Calculate remains the compact quickform evaluator. Rich guided workflows belong in their visible workspaces such as Equation, Calculus, Trigonometry, Statistics, Geometry, Matrix/Vector, Table, and Guide.

Calculate's visible `Simplify`, `Factor`, and `Expand` soft keys are public product actions, not automatic aliases for private Symbolic Primitives. Symbolic Primitives may supply reusable bounded mechanics only after explicit bridge/parity work proves visible Calculate behavior remains correct.

Graphing remains intentionally deferred. It should be planned as a scene/runtime surface over trustworthy solver outputs, domains, restrictions, branches, discontinuities, parameter ranges, and failure reasons. There is no current graphing compartment, route, workspace, pack, or Surface candidate.

Surface Protocol, broad bus/nervous-system work, plugins, packs, external SDKs, runtime registries, generated contracts, and distro layers are deferred until a concrete product or contributor need appears.

Equation search discipline is closed as a foundation track after `EQUATION-SEARCH-DISCIPLINE-CLOSEOUT0`. The corrected handoff and roadmap remain at `.memory/research/roadmaps/equation-solver-search-discipline-handoff.md` and `.memory/research/roadmaps/equation-search-discipline-roadmap.md`. The live repo now has pure target-shape profiling, conservative route planning, internal/test-facing search trace evidence, exp/log generated-handoff routing, a shared symbolic polynomial coefficient seam, route-gated carrier/composition/mixed generated branch handoff, and shared MathJson arithmetic adoption where parity was proven. This gives Calcwiz bounded, inspectable selected-target search discipline; it is not broad CAS recursion, algorithm expansion, or a new planner authority. Future solver work should use these seams instead of adding parallel route evidence, branch delegation, or coefficient arithmetic. `EQUATION-CAP-RECALIBRATION-AUDIT0` classifies current Equation caps before any expansion, and `EQUATION-CAP-HIT-EVIDENCE1` backs that classification with focused tests across all known cap families. `EQUATION-SOURCE-MIRROR-CONTEXT-AUDIT0` compares the local FriCAS/SymPy/Maxima/SageMath/Giac-XCAS/SymEngine/GeoGebra mirrors against Calcwiz and records the durable lesson: mature systems get recursive solving breadth from algebraic substrates, domain/conversion gates, assumptions, branch facts, elimination/factorization, and honest fallback objects, not from unbounded selected-target peeling. `EQUATION-CAP-HIT-REAL-CASES0` then checks current real/default evidence and does not justify raising caps yet: a visually deep selected-target affine/quotient shell normalizes and solves under defaults, no public default generated branch-count hit is known, and current default stops still mostly classify as algorithm/readback/semantic boundaries. `EQUATION-SUBSTRATE-ROADMAP0` starts the substrate track with this order: factoring/product decomposition, higher-degree root representation policy, branch/domain/exclusion facts, compact readback/implicit-root policy, then Exact/Isolate semantics. The original Equation product-decomposition seam has now been promoted into the private Symbolic Factorization primitive; `factorable-polynomial.ts` consumes the primitive for explicit zero-product mechanics while retaining Equation ownership of solving, facts, readback, and stops. `EQUATION-ROOT-REPRESENTATION-SEAM1` adds the first solver-owned internal root model under `src/lib/equation/roots/representation.ts` and adopts it only in `factorable-polynomial.ts` explicit-product and exact-rational expanded factorable paths. The seam can model exact finite roots, factor-derived roots, exact-rational factor roots, numeric validated roots, implicit algebraic roots, and structured stops, but v1 adapts back to existing `exactLatex`, `branchReadback`, supplements, and detail lines with no visible `RootOf`, implicit-root notation, cap raise, schema change, or Exact/Isolate cleanup. `EQUATION-BRANCH-DOMAIN-FACTS1` adds the first internal Equation branch/domain facts seam under `src/lib/equation/facts/`, wrapping existing `ExactSupplementEntry` and `SolveDomainConstraint` meanings with attachment scopes while rendering the same raw `exactSupplementLatex` compatibility strings. First adoption is limited to factorable root-group facts and rational denominator exclusions; `detailSections` remain prose, and guarded algebra/composition/periodic/candidate-validation fact migration remains later. `EQUATION-IMPLICIT-ROOT-READBACK-AUDIT0` confirms visible `RootOf`/implicit-root notation should stay deferred. `EQUATION-COMPACT-ROOT-READBACK1` adds an internal producer-side compact root readback helper at `src/lib/equation/roots/readback.ts` and refactors factorable root-set consumers to use it while preserving visible `exactLatex`, `branchReadback`, supplements, details, formula-size stops, and schemas. `EQUATION-EXACT-ISOLATE-SEMANTICS-AUDIT0` confirms the intended boundary for `Exact` and `Isolate`: `Isolate` is bounded selected-target rearrangement/formula output with Valid When facts allowed as rearrangement conditions, while `Exact` may use isolation internally but should claim success only when terminal exact evidence exists, including symbolic roots, irrational radicals, symbolic-parameter formulas, branch/principal families, inequality sets, domain/exclusion facts, or candidate validation. `EQUATION-ANSWER-MODE-SIMPLIFICATION1` now makes that boundary concrete in the app shell: active Equation answer modes are `Exact` and `Isolate` only, web/Tauri settings sanitize legacy `approximate` to `exact`, new numeric interval launches no longer write `equationAnswerMode: 'approximate'`, and Numeric Interval Solve is a contextual numeric route/tool with `solutionKind: 'approximate-numeric'` on successful numeric results. Header `DECIMAL`, approximate digits, and numeric notation remain display controls. The old `Approx` answer-mode framing is gone from active settings/workspace controls but remains readable in legacy request/history/display schemas. DAG/search-graph work is deferred until repeated transformation-state pressure appears. Symbolic degree-2 and algebraic/factorable degree-4 limits are primarily correctness/readback/algorithm-boundary caps; composition depth and periodic-parameter caps are semantic boundaries; selected-target peel depth and generated branch counts stay watchlist candidates only after user-real default-cap hit examples justify them.
`EQUATION-NUMERIC-INTERVAL-REVIVAL0` starts the next docs-only audit after semantics simplification. Numeric Interval Solve remains a route/tool, not a restored `Approx` answer mode. Current evidence shows ordinary numeric roots are stable, while dense/nested periodic and discontinuity cases are sensitive to endpoint shifts and subdivision count under the bracket-first sampler. `EQUATION-NUMERIC-INTERVAL-ROUTE-REPAIR1` fixes the launch route: advisory-eligible exact stops expose the `Numeric Solve` opener without auto-arming numeric execution, and once the panel is explicitly open, header Run, F1, and EXE launch numeric interval solving. Hiding the panel returns primary Run/F1/EXE to symbolic Exact/Isolate solving. The panel is now configuration-only with Start/End/Subdivisions; the panel-local `Run Numeric Solve` button is removed. `EQUATION-NUMERIC-INTERVAL-GUIDANCE1` surfaces exact periodic `suggestedIntervals` inside the open panel as click-to-fill Start/End buttons only, keeps Subdivisions user-controlled, and does not auto-run. Numeric wording now presents interval solving as a local real-root search with final validation, not an all-roots proof; no-root, invalid-interval, rejected-candidate, dense/nested periodic, discontinuity/domain-hole, and suggested-interval guidance are clearer. `EQUATION-NUMERIC-INTERVAL-STABILITY1` adds bounded adaptive refinement to the numeric interval sampler: base-grid cells with sign changes, near-zero residuals, local minima, steep jumps, discontinuity-like endpoints, and trig/log dense-periodic expressions get capped extra samples before final original-equation validation. Diagnostics now expose adaptive sample, refined-cell, and discontinuity-cell counts. Numeric interval successes now use approximate roots as the primary visible answer/readback even under Exact output style, and Copy Result copies those numeric roots instead of an empty result. This improves dense/nested periodic windows and avoids accepting discontinuity sign changes, but still does not promise all roots.

`CALCWIZ-NUMERIC-METHODS-ROADMAP0` is the pre-implementation roadmap for the numeric pivot. The roadmap is not counted as one of the future implementation moves. It locks that broad new Equation symbolic capability expansion should pause after the narrow symbolic polish closeout, while existing symbolic routes remain live and maintained. The future implementation session is Equation-owned: numeric solving widens through a guarded Equation pipeline for genuinely numeric problems with protected targets, one remaining unknown after approved substitution, shape classification, deterministic algebraic roots where possible, domain segmentation, robust real nonlinear search, and interval/periodic solving where intervals are meaningful. `EQUATION-NUMERIC-SHAPE-CLASSIFIER1` adds the first internal substrate for that pivot: stored-value preparation with target protection, target-aware zero-form evaluation for non-`x` targets, numeric-ready classification, route recommendation, and internal denominator/log/root/periodic/discontinuity evidence. `EQUATION-STORED-VALUE-SOLVE-CONSENT1` makes the Algebra/F4 consent live as `Use Stored Values`: it appears only for parameterized Equation inputs with a selected target plus non-target variable parameters, reports missing stored values when any parameter lacks a value, never substitutes the solve target, and otherwise resumes the normal Equation Solve/Run path with a one-shot stored-value snapshot and target-protection evidence. `EQUATION-DETERMINISTIC-NUMERIC-ALGEBRAIC1 + EQUATION-NUMERIC-DOMAIN-SEGMENTATION1` makes exact-first deterministic numeric algebraic fallback live for numeric-ready single-target polynomial/rational equations after supported exact symbolic solving misses: real approximate roots are validated through degree `64`, degree-3/4 numeric fallback does not use Cardano/Ferrari readback, and denominator exclusions, solved pole exclusions, log/root/fractional-power/trig-pole facts, sampled hazards, residuals, and rejected candidates stay in detail sections rather than global `Valid When`. `EQUATION-REAL-NONLINEAR-NUMERIC-SEARCH1` adds exact-first real nonlinear numeric fallback after deterministic algebraic fallback misses: numeric-ready non-periodic nonlinear or discontinuity-heavy equations auto-search bounded windows `[-10,10]`, `[-100,100]`, `[-1000,1000]`, and `[-10000,10000]`, use target-aware interval sampling/refinement plus local-minimum recovery, validate candidates against the original equation, and report searched-window/completeness wording, residuals, domain/exclusion facts, and rejected candidates in detail sections. `EQUATION-REAL-PERIODIC-INTERVAL-NUMERIC1` now keeps periodic/trig numeric fallback interval-first: unsupported periodic numeric fallback without a numeric interval returns a guidance card asking for finite real bounds, while numeric interval runs enumerate validated roots only inside the chosen interval, carry local-completeness wording, preserve target-aware non-`x` output, and show domain/exclusion facts such as trig poles and discontinuities in details. Numeric solving still stays inside the existing Equation Solve/Run path; Algebra/F4 is substitution consent, not a second solver. Reserved functions, constants, and units are not parameters. Cross-domain work for Statistics, Limits, Differentiation, and related Calculus numeric surfaces is policy/contract enforcement and handoff only, so other agents can own those domain implementations without this Equation track interfering. The roadmap is at `.memory/research/roadmaps/calcwiz-numeric-methods-roadmap.md`.

`EQUATION-FRONTIER-SOLVER-ROADMAP0` and `EQUATION-FRONTIER-HANDOFF-RESYNC0` start the next Equation frontier track. The external solver-phase handoff is now resynced against the live repo: search discipline is implemented enough, polynomial representation work must reuse existing Algebra cores and Equation symbolic-coefficient seams, cap evidence still blocks blind cap raises, and source mirrors remain context only. The recommended first frontier implementation is `EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1`: make factor/product decomposition, root representation, branch/domain facts, and compact readback produce a visible exact capability leap for factored/product equations before broad automatic factoring, Cardano/Ferrari formulas, implicit-root notation, DAG/search graphs, graphing, step-by-step, or Rust migration. `EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1` widens only the explicit zero-product factorable path to 12 target-degree slots while keeping each target-containing factor delegated only through current linear/quadratic families. `EQUATION-EXPANDED-FACTOR-FRONTIER1` now opts the expanded exact-rational factorable path into the same 12-degree frontier only when Algebra's bounded factorization can reduce the polynomial to supported real linear/quadratic pieces; default Algebra callers remain capped at degree 4, unfactored degree 5-12 expanded exact-rational polynomials stop honestly, and degree >12 remains a `degree-limit`.

`EQUATION-SPECIAL-FORM-ROOTS-FRONTIER1` extends real Exact affine selected-target powers through degree 12 while keeping Complex power isolation capped at degree 4. It also adds an internal/test-traced `special-form-roots` selected-target family for exact-rational pure-power carrier quadratics such as `x^6-5x^3+4=0` and `x^{12}-5x^6+4=0`, with total degree `2n <= 12`.
Post-frontier bugfix: live Equation input now normalizes unbraced multi-digit pasted/typed powers such as `^12` into `^{12}` before app-state solving, and single-target real Exact special-form carrier equations route before the older shared symbolic fallback so they do not return partial roots. `EQUATION-AFFINE-CARRIER-SPECIAL-FORM-FRONTIER1` extends the same real Exact special-form family from pure carriers `x^n` to affine carriers `(q*x+r)^n` where `q` is nonzero exact-rational and `r` is target-free symbolic, so `(x+a)^6-5(x+a)^3+4=0` and `(2x-1)^{12}-5(2x-1)^6+4=0` solve exactly through the carrier seam. `EQUATION-SYMBOLIC-CARRIER-COEFF-FRONTIER1` then lets pure and affine carrier quadratics use symbolic target-free outer coefficients through the existing `SymbolicTargetPolynomial` seam, so `x^6-a*x^3+b=0`, `x^{12}-a*x^6+b=0`, and `(x+c)^6-a*(x+c)^3+b=0` produce exact symbolic carrier-root branches plus discriminant/even-root facts. `EQUATION-SYMBOLIC-FACTOR-DISCOVERY-FRONTIER1` adds a narrow symbolic common target-power factor discovery path for expanded zero-form shapes `x^k * Q(x)=0` through degree 12 when residual `Q` is linear or quadratic with target-free symbolic coefficients, so `x^3-a*x^2=0`, `x^5-a*x^3=0`, and `x^{12}-a*x^{10}=0` solve through the existing factorable/root readback path. `EQUATION-SYMBOLIC-FACTOR-PATTERNS-FRONTIER1` extracts that discovery into `symbolic-factor-patterns.ts` and widens it to affine common-carrier factors plus safe real difference-of-powers `U^n-V^n=0` patterns through degree 12, while still delegating only linear/quadratic factors through existing selected-target solvers. `EQUATION-SYMBOLIC-FACTOR-DISCOVERY-FRONTIER2` extends the same seam with bounded symbolic factor-by-grouping and monic grouped affine-carrier quadratics such as `x*(x+a)+b*(x+a)=0`, `(x+c)^2+(a+b)*(x+c)+a*b=0`, and `(x+c)^2+2*a*(x+c)+a^2=0`; it keeps broad factoring, residual degree >2, and primitive promotion deferred. `EQUATION-COMPLEX-SPECIAL-FORM-FRONTIER1` widens Complex Exact special-form roots for exact-rational direct and carrier-quadratic pure/affine cases through 12 visible branches, preserving older low-degree rectangular Complex readback and honoring the selected `complexExactForm` for high-degree branches (`cis` uses `cis`, while `rectangular`/`polar` use exact trigonometric branch notation until rectangular radical coordinates are implemented). Direct affine powers such as `(x+a)^{12}=b` stay on algebraic isolation. Target-bearing carrier coefficients, non-affine carrier substitution, residual degree >2 symbolic factor discovery, sum-of-powers, broader special forms, symbolic-coefficient Complex carrier roots, Complex branch counts above 12, visible implicit roots, numeric fallback as Exact closure, Display/History schemas, OOE/app-state/Tauri changes, graphing, step-by-step, and DAG/search graphs remain out of scope.
`EQUATION-CARRIER-ELIMINATION-FRONTIER1` adds a bounded real Exact carrier-elimination route for explicit algebraic carriers. It detects exact-rational linear/quadratic equations in `u=g(x)`, normalizes common carrier powers such as `(x+a)^4-5(x+a)^2+4=0`, then back-substitutes each real carrier root through existing generated branch solvers. Current supported carriers are algebraic shapes already solvable by the selected-target branch stack, such as `x^2+a`, `(x+a)^2`, and `sqrt(x+a)`. Symbolic reduced-carrier coefficients, periodic/transcendental carriers, arbitrary auxiliary-variable inference, general Groebner/resultant solving, schema changes, and runtime/UI changes remain deferred; future symbolic primitives should absorb repeated substitution/elimination mechanics.
`EQUATION-COMPLEX-SYMBOLIC-SPECIAL-FORM-FRONTIER1` is audit-gated and deliberately keeps symbolic Complex special-form roots deferred. Existing exact-rational Complex special forms remain supported and still honor `complexExactForm`, but symbolic cases such as `x^5=a` or `x^6-a*x^3+b=0` require a formal principal-branch root policy before Calcwiz can show exact branches honestly. The visible boundary now says that symbolic carrier coefficients/constants are deferred until that policy exists and tests prevent visible `RootOf` or informal principal-branch notation from appearing.
`EQUATION-ELIMINATION-FRONTIER1` makes the product-facing Polynomial 2x2 system route opt into 12-degree retained resultant projections while Algebra's default elimination caps remain conservative. Candidate-pair validation remains mandatory, stored exact-rational constants are supported, unsupported free symbolic parameters stay guidance-only, and this is not Groebner solving, single-equation auxiliary inference, or a broad multivariable CAS.

`SYMBOLIC-PRIMITIVES-SURFACE-AUDIT0` starts the post-frontier reusable substrate governance track. The five reusable algebraic building blocks are named Symbolic Primitives: expansion, substitution, factorization, simplification, and elimination. Future implementation should use dedicated folders under `src/lib/symbolic-engine/primitives/`, beginning with expansion, then substitution, factorization, simplification, and elimination. This is an audit-only posture: no code moved, no solver behavior changed, and no broad CAS engine was introduced. Future solver work should consume a primitive when one exists, add the primitive first when repeated mechanics are emerging, or document why route-local logic remains semantic and owned.

`SYMBOLIC-PRIMITIVES-COMPARTMENT-ROADMAP0` locks Symbolic Primitives as a governed district inside the existing `symbolic-engine` compartment rather than a new top-level compartment for now. Future implementation should create one folder per primitive under `src/lib/symbolic-engine/primitives/` and keep APIs private-first until real consumers prove a public seam. No app-wide primitive-surveillance validator exists yet; the current rule is planning/test governance: consume a primitive when available, add one when repeated mechanics emerge, or document why a route-local helper remains semantic and owned. Recommended implementation order is `SYMBOLIC-EXPANSION-PRIMITIVE1`, then substitution, factorization, simplification, and elimination.

`SYMBOLIC-EXPANSION-PRIMITIVE1` creates the first private Symbolic Primitive under `src/lib/symbolic-engine/primitives/expansion/`. The primitive performs bounded MathJSON expansion for `Add`, `Subtract`, `Negate`, `Multiply`/`InvisibleOperator`, and positive integer powers with default caps `maxPower: 12`, `maxExpandedTerms: 256`, and `maxNodeCount: 2000`. First adoption is deliberately narrow: Equation polynomial carrier follow-on uses `expandMathJsonNodeOrOriginal` instead of its old local ComputeEngine `expand` loop, and top-level quadratic-carrier equations such as `(x^2+x)^2-(x^2+x)-1=0` now reach that proven bridge in real Exact and in Complex Exact via a narrow quadratic-carrier follow-on that returns the full complex branch set without double-appending the imaginary unit. Broader nonlinear carriers, non-quadratic Complex carrier closure, broad solver migration, public primitive facades, and final-answer readback polishing/normalization remain deferred.

`SYMBOLIC-SUBSTITUTION-PRIMITIVE1` adds the second private Symbolic Primitive under `src/lib/symbolic-engine/primitives/substitution/`. V1 is structural and carrier-focused: protected symbol substitution, exact subtree replacement, and carrier power-basis substitution with bounded node/power-shape stops. First adoption is only Equation carrier elimination, which now uses the primitive to construct reduced `u` equations while Equation still owns carrier detection, route order, branch delegation, facts, stop wording, and readback. Stored-variable substitution remains in `algebra/variable-memory`, and final-answer readback polishing stays deferred until after all five primitives are established.

`SYMBOLIC-FACTORIZATION-PRIMITIVE1` adds the third private Symbolic Primitive under `src/lib/symbolic-engine/primitives/factorization/`. V1 promotes reusable mechanics from Equation factorable solving: explicit product decomposition, target-power multiplicity, common pure/affine carrier factors, safe real difference-of-powers, shared-carrier grouping, and grouped affine-carrier quadratics. First adoption is only Equation `factorable-polynomial.ts` and its thin `symbolic-factor-patterns.ts` adapter; Equation still owns route order, messages, LaTeX/readback, branch/domain facts, root construction, validation, and degree-12 boundaries. The older `src/lib/symbolic-engine/factoring.ts` facade remains unchanged and is a later parity consumer, not part of this milestone.

`SYMBOLIC-SIMPLIFICATION-SURFACE-AUDIT0` confirms simplification should not start as final-answer prettification or a universal reducer. `SYMBOLIC-SIMPLIFICATION-PRIMITIVE1` adds the fourth private Symbolic Primitive under `src/lib/symbolic-engine/primitives/simplification/`, with bounded structural MathJSON simplification helpers for add/multiply/negate/subtract/divide/square, additive-term splitting, exact scalar folding, stable structural keys, and node-limit stops. First adoption is only `src/lib/symbolic-engine/primitives/factorization/node-helpers.ts`, replacing its direct ComputeEngine simplification calls while preserving Equation route order, facts, readback, stops, and degree boundaries. Radical, power/log, Algebra exact-rational, Complex branch/readback, and final-answer polish surfaces remain separately owned. The grouped-factor readability case `x*(x+a)+b*(x+a)=0` remains later route/readback polish because the factorization primitive can recognize the structure but current app route order may still let generic quadratic output claim the answer first.

`SYMBOLIC-ELIMINATION-PRIMITIVE1` adds the fifth private Symbolic Primitive under `src/lib/symbolic-engine/primitives/elimination/`, completing the initial five-primitive establishment pass. V1 is a MathJSON-first bounded bivariate resultant bridge: callers pass zero-form MathJSON nodes, the primitive renders normalized zero forms, delegates exact polynomial/resultant arithmetic to Algebra's existing elimination substrate, and returns projection metadata plus the projected MathJSON polynomial node. First adoption is only the Polynomial 2x2 Equation route, which keeps zero-form parsing, root solving, candidate-pair validation, stored-value readback, error wording, detail sections, and `DisplayOutcome` shape unchanged. Algebra remains owner of exact resultant arithmetic; Symbolic Engine owns the reusable bridge mechanics; Equation remains solver/readback authority. Broad consumer expansion, primitive-surveillance tooling, carrier-elimination migration, general Groebner/multivariable CAS behavior, and final-answer readback polish are follow-up work after this five-primitive foundation.

`SYMBOLIC-PRIMITIVES-CONSUMER-AUDIT0` confirms all five primitives have a proven first production consumer but should not be broadened by sweep or hard linter yet. Expansion, substitution, factorization, and simplification have real parity candidates; simplification has the strongest immediate pressure around Equation parameterized MathJSON arithmetic. Elimination remains intentionally narrow with no safe second consumer yet. Near-term governance is still milestone/test-based: consume a primitive when the mechanic truly matches, add or extend a primitive when repeated mechanics emerge, or document why route-local logic remains semantic and owned.

`CALCULATE-ALGEBRA-ACTIONS-SURFACE-AUDIT0` keeps Calculate's algebra action buttons outside the private primitive parity sweep. A later Calculate bridge may consume expansion/factorization/simplification primitives, but only as an explicit product milestone with visible-output parity tests.

`SYMBOLIC-SIMPLIFICATION-CONSUMER-PARITY1` makes Equation `parameterized/math-json.ts` the next simplification primitive consumer. Its default arithmetic helpers now use the private structural simplification primitive, while custom route simplifiers remain preserved for consumers whose raw shapes/readback depend on local semantics. This prepares coefficient-expression normalization pressure for future Cardano/Ferrari work without doing final-answer readback polish.

`SYMBOLIC-EXPANSION-CONSUMER-PARITY1` makes Equation `mixed-algebraic.ts` the next expansion primitive consumer. The route now uses the private expansion primitive for its bounded algebraic expansion while preserving the old square-only consumer cap and leaving `symbolic-engine/mixed-factor` for a later parity milestone. This keeps carrier/mixed route behavior unchanged while moving toward expansion-ready coefficient-map inputs for future higher-degree algorithms.

`SYMBOLIC-SUBSTITUTION-CONSUMER-PARITY1` makes Equation polynomial carrier follow-on the next substitution primitive consumer by using `substituteMathJsonSubtree` for exact carrier replacement. Stored-variable substitution, candidate-validation substitution, and user variable memory stay out of this parity slice; future depressed-polynomial substitutions and carrier reductions should use the primitive when semantics match.

`SYMBOLIC-FACTORIZATION-FACADE-PARITY1` gives the older `src/lib/symbolic-engine/factoring.ts` facade a primitive-backed shared-carrier grouping parity path while retaining its legacy fallback. The facade still preserves public return shapes, ordering, and unsupported behavior; Algebra exact-rational factorization and Equation root construction remain separate owners. Broader symbolic factoring before Cardano/Ferrari remains future frontier work.

`SYMBOLIC-ELIMINATION-CONSUMER-READINESS0` confirms elimination should stay with one production consumer for now: Equation Polynomial 2x2 systems. Carrier elimination, broader polynomial systems, higher-dimensional resultants, and auxiliary-variable inference all need dedicated product/algorithm milestones before they can consume or extend the elimination primitive. No app-wide primitive-surveillance validator is justified yet.

`SYMBOLIC-PRIMITIVES-PARITY-CLOSEOUT0` pauses the Symbolic Primitives lane as complete enough for now. Remaining local primitive-like helpers are not automatic debt: Algebra exact-domain helpers, Equation route/fact/readback semantics, Calculate public actions, inequality/complex/variable-memory paths, and guarded transforms retain local ownership unless a focused parity milestone proves a primitive match. Future dedup should be targeted; no sweep or app-wide primitive-surveillance validator is active.

`ANSWER-READBACK-POLICY-AUDIT0` records that recurring exact-answer readability issues such as double imaginary units, reducible zero terms, noisy scalar factors, and awkward branch roots are final-answer readback normalization gaps before Display, not reasons to make Display a CAS. `ANSWER-READBACK-NORMALIZATION1` adds the first producer-side Equation readback normalizer under `src/lib/equation/readback/` and adopts it only for validated root-set exact finite roots before `exactLatex` and `branchReadback` assembly. V1 normalizes safe canonical presentation noise such as additive/multiplicative identities, exact numeric zero factors in validated roots, reserved imaginary-unit products, and external coefficient-before-radical ordering while preserving exact overrides, facts, supplements, detail sections, stops, History, Display schemas, OOE, app-state, Tauri, and Calculate actions. `ANSWER-READBACK-SIGN-NORMALIZATION1` extends that seam with local sign cleanup for double signs, leading plus signs, exact numeric-fraction signs, and simple negative grouped terms, while still refusing symbolic cancellation/factoring/radical extraction and leaving `exactLatexOverride` intact. `COMPLEX-READBACK-POLICY1` locks Complex exact-form readback: `cis` may render `\operatorname{cis}`, `rectangular` and `polar` must not silently force `cis`, high-degree cases without compact rectangular radical coordinates use exact trigonometric branch notation as the readable fallback, and symbolic principal-branch Complex roots remain deferred. `BRANCH-READBACK-POLISH1` adds a shared finite-branch helper so standalone finite Equation branch arrays are normalized before dedupe, set joining, and `DisplayBranchReadback` creation across Complex, algebraic isolation, parameterized branches, generated handoffs, and root-set readback. `ANSWER-READBACK-OVERRIDE-POLICY1` closes the finite-root override bypass: safely decomposable single-target finite root overrides are normalized and rebuilt as canonical `exactLatex`, so Display, Copy Result, To Editor, History, and replay see the cleaned form; unsafe overrides fail closed and remain raw. The same slice now also cleans the manual-QA complex carrier branch rows by reordering single radical terms around trailing exact numeric fractions, unwrapping parenthesized square-root factors, and recursively cleaning sign noise inside square-root radicands while failing closed on multi-term symbolic groups. Periodic families, inequalities, facts, supplements, detail sections, decimal/approx branches, Display schema changes, Complex rectangular exact coordinates, deeper algebraic simplification of radicands such as expanding `1-4(...)`, and symbolic principal-branch Complex roots remain future explicit milestones.

`ANSWER-READBACK-MATHJSON-BRANCHES1` upgrades finite Equation root/branch readback to prefer existing MathJSON nodes before LaTeX strings. Node-backed branches run through the private simplification primitive recursively, render back to LaTeX, then receive the safe final LaTeX polish. First producers are parameterized quadratic/polynomial roots plus polynomial/complex carrier follow-on roots, including the `(x^2+x)^2-(x^2+x)-1=0` Complex Exact QA path. The expected cleaned output no longer contains `1+-4`, `1+\left(-4`, double imaginary units, decimal `0.5i`, or plus-negative fraction groups in that finite-branch path. The string normalizer is now final polish/fallback, not the primary tree traversal. No Display/History schema, OOE, app-state, Tauri, Calculate action, periodic-family, inequality, fact, supplement, or detail-section behavior changed.

`ANSWER-PRESENTATION-SOURCE-MIRROR-AUDIT0` refines the active readback posture after user QA still found recurring presentation noise. Local source mirrors such as SymPy, Maxima, FriCAS, Giac/Xcas, SymEngine, SageMath, and GeoGebra consistently route final answers through expression-aware printers, output forms, rich representations, or template-aware expression renderers. Calcwiz already has MathJSON and Symbolic Primitives, but raw `exactLatex`, `exactLatexOverride`, and route-built branch strings remain strong bypass paths. `ANSWER-PRESENTATION-PIPELINE-ROADMAP0` now sets the durable strategy: add a producer-side Equation presentation pipeline/IR that carries MathJSON-backed finite roots/sets/families/facts to one shared renderer, leaving LaTeX normalization as final polish and compatibility fallback only. `ANSWER-PRESENTATION-IR1` adds the first internal Equation presentation district under `src/lib/equation/presentation/` and promotes finite root/set rendering there. The first slice is carrier follow-on plus parameterized quadratic/polynomial finite branches: node-backed roots prefer MathJSON, pass through the private simplification primitive, render back to LaTeX, and then receive final safe polish before adapting to existing `exactLatex` and `branchReadback` compatibility surfaces. Raw route LaTeX remains fallback only; Display, History, OOE, app-state, Tauri, Calculate actions, periodic families, inequalities, facts, supplements, and detail sections remain unchanged.

Language is now a first-class static compartment foundation, not a translation wave. `src/lib/language/` owns the English-only typed catalog, deterministic English fallback, metadata with direction, validation, typed function interpolation, and a React provider/hook. English remains the safe default/fallback for reset, invalid persisted settings, missing language resources, unknown language codes, or failed validation. Language owns prose, labels, commands, metadata, fallback, and interpolation; Display/notation continues owning math rendering. Mixed prose plus math should move toward structured text/math parts so math fragments honor MathNotation, and RTL metadata must not blindly change math semantics. After `LANGUAGE-SHELL-PILOT1`, low-risk app-shell consumers read shell/common labels through `useLanguage()`. After `LANGUAGE-SETTINGS-SEAM1`, `languageCode` is the durable settings field, `LanguageProvider` is mounted from settings in `AppMain`, the app shell exposes `lang="en"`, Settings shows a visible English-only language control, and web-preview plus Tauri desktop persistence both sanitize unsupported language codes back to English. After `LANGUAGE-PANELS-PILOT1`, `SettingsPanel`, `HistoryPanel`, and `VariablesPanel` read panel-owned labels, helper text, empty states, actions, aria text, History pending-row interpolation, and Variables feedback messages through the English Language catalog without changing settings persistence, History schemas, stored localized data, variable policy, solver wording, Display math rendering, or OOE/runtime behavior. After `LANGUAGE-ROADMAP-CLOSEOUT0`, the Language roadmap is intentionally paused: the foundation and first English seams are complete and tested, while navigation metadata, RTL, non-English packs, Guide/content work, and solver/readback localization should wait for real product pressure. The DisplayTextPart / mixed prose-math seam should resurface later as Display/math-fidelity work, not as translation work.

Workspace tabs now have the first visible browser-style shell after `WORKSPACE-TABS-SHELL1`, built on the earlier session identity, active-only surface-state hosting, OOE workspace-instance scoping, and per-instance Display state from `WORKSPACE-INSTANCE-MODEL1`, `WORKSPACE-STATE-HOST1`, `OOE-WORKSPACE-INSTANCE-SCOPE1`, `WORKSPACE-STATE-HOST-EXPANSION1`, and `WORKSPACE-DISPLAY-STATE-HOST1`. The app still uses one `AppMain`, one active rendered workspace surface, one OOE authority, and global committed History. The tab strip sits above the mode strip, shows one tab per `WorkspaceInstance`, supports focusing, plus-created blank Calculate tabs, rename, duplicate, close, close others, clear state, and stop jobs in a tab. Inactive tabs are not hidden mounted React trees; switching routes through the state host and singleton `currentMode`. Normal mode selection retargets the active tab, like browser same-tab navigation, instead of focusing or creating another tab. After `WORKSPACE-TABS-LAUNCHER-ACTIONS1`, launcher leaf rows keep primary click, Enter, hotkeys, and the existing Open action as current-tab retarget, while hosted runtime workspace leaves expose explicit `Open in new tab` through a visible row action and a right-click context menu. Labs remains open-here only, Guide is not a launcher leaf, and the `+` tab button still creates a blank Calculate tab; `WORKSPACE-TABS-DEFAULTS1` is deliberately deferred. Retargeting clears that tab's surface/display/runtime slots, updates default titles, preserves custom titles, increments a workspace-instance navigation revision, and makes older in-flight OOE work for that tab stale if it returns late. Retargeting does not request real job cancellation; old-revision jobs may finish internally but cannot visibly commit into the retargeted tab, and old-revision pending/running work is ignored by active-tab status and tab job summaries. Focusing another tab does not cancel the origin tab's running jobs. After `WORKSPACE-INACTIVE-TAB-COMMIT-FIX1`, OOE active-input revision checks can resolve the origin workspace instance's current revision from saved surface state when that instance is inactive but still open; this lets open inactive tabs accept their own completed results while still stale-dropping closed, retargeted, missing, or edited-origin jobs. Inactive-tab completions update only the origin tab's saved Display/Ans state. Manual testing exposed the key leak invariant: even when a completed job belongs to the correct workspace instance, async commit code must not read the active tab at Promise resolution time as the visible target; launch workspace context is frozen for ticket/runtime commit routing, and ref-backed runtime context getters keep completion writes scoped to the launch/origin instance. Surface state is hosted for all current runtime workspaces: Calculate, Equation, Calculus, Table, Trigonometry, Statistics, Geometry, Matrix, and Vector. Display outcome, `Ans`, and replay display/substitution fragments are workspace-instance session state, so a result committed in one tab does not overwrite another tab's visible answer/facts/assumptions. Display state is updated by explicit tab capture or origin commit only; it is not passively mirrored from whichever display happens to be visible. Runtime/editor status is also workspace-instance session state: editor stopped state, restart generation, runtime status overrides, final runtime elapsed duration, and Display header clipboard notices are captured/restored per active tab, and the Display header no longer reads the app-wide React transition pending flag. Runtime jobs now show calm elapsed timing in the Display header: whole-second `Computing`/`Stopping` labels while running, then a two-decimal `Ready` elapsed label after completion until that workspace launches another runtime job. Finalized committed History entries may store `runtimeElapsedMs`, and History rows show compact duration metadata; this is UI/persistence metadata only and does not change OOE stale gates, cancellation, host routing, or commit authority. Guide and Labs remain outside normal workspace surface-state hosting. OOE job identity, active/recent jobs, diagnostics records, lifecycle events, runtime envelopes, and launch-ticket evidence can carry optional `workspaceInstanceId` plus launch-time labels and revision metadata; pending/running History rows show launch-time tab labels for disambiguation, but finalized History rows return to normal workspace identity and do not store tab names. Closing a tab with active work asks for cancel/keep-open, and confirmed close cancels matching active work, clears matching pending tickets, then relies on existing instance-aware stale-drop to prevent late commits. Stop jobs remains an explicit active-tab cancellation path. After the 2026-06-19 stop-control follow-up, Display header Stop and Restart first target the active workspace scoped pending runtime ticket before falling back to legacy editor-analysis cancellation, so a hard Calculus worker run can be stopped from the header just like from the History pending row. Committed History, persistence, and global settings such as angle unit remain global. The agreed direction remains one app shell, no projects/files, no multiple `AppMain` copies, no second OOE authority, and no broad bus.

Tabs do not replace side panels. Side panels remain for quick, essential, while-working access: recent History, common Settings toggles, Vars, and developer diagnostics. Dedicated tab pages are the future home for deeper management surfaces that need room, such as full History/Records, full Settings, richer Variables management, Guide pages if needed, and future Graphing/Spreadsheet surfaces. The reason is architectural as much as UX: tabs are the app's general full-surface layer, so future rich surfaces do not have to be squeezed into the MathEditor/result shell, tiny side panels, or one-off modal/route systems. If tabs are treated only as multiple calculators, Calcwiz stays artificially limited: Settings remains cramped, History cannot grow into a real records manager, Variables cannot become a richer manager, and graphing/spreadsheet-style surfaces get forced into the wrong history/editor model. The distinction is important: side panels stay small and efficient, while tab pages can handle search, categories, filtering, large records, custom layouts, and future artifact types that do not fit the current computation `HistoryEntry` model. Graphing and Spreadsheet saved-work or artifact history remains deferred.

`WORKSPACE-TABS-PAGE-FOUNDATION-AUDIT0` finds the tab foundation ready for future full-page surfaces, but not by adding pseudo calculator modes. A later page foundation should add a thin tab-surface/page-kind descriptor and per-surface tab action policy before implementing full Settings, full History/Records, Variables manager, Guide pages, Graphing, or Spreadsheet.

## Calculus Identity

Calculus is canonical. Live `src` and `src-tauri` should not contain retired legacy Calculus identity vocabulary.

Current shape:

- Guided Calculus workspace code lives under `src/lib/calculus/workspace/`.
- Shared Calculus compute code lives under `src/lib/calculus/engine/`.
- Stable Calculus root surfaces are intentionally small: `calculus-identity.ts`, `calculus-workbench.ts`, and `calculus-strategy.ts`.
- `src/lib/modes/calculus.ts` is the public mode facade for current Calculus execution.
- Calculus CSS identity is canonical `calculus`, not retired legacy naming.
- Symbolic integration dispatch now starts with an internal `classifyIntegrandForm()` route plan under `src/lib/symbolic-engine/integration/`. The classifier preserves existing route precedence and visible outputs while keeping classifier evidence internal/test-facing only; Rubi rule translation, public metadata fields, lazy tier imports, and result-schema changes remain deferred.
- `RUBI-TIER1-SECTION1-AUDIT0` maps IntegrationRules Section 1 against current Calcwiz coverage. The recommended first implementation slice is bounded positive-integer polynomial/binomial/trinomial expansion into the existing direct polynomial integrator with original-integrand backcheck; recurrence-heavy, fractional-power, normalization, and branch-sensitive Rubi families remain deferred.
- `RUBI-TIER1-SECTION1-POLY-EXPAND1` adds the first Rubi Section 1 native rule slice: bounded products and nonnegative integer polynomial powers can expand through `expandMathJsonNode()`, integrate through the existing direct-rule path, and verify exactly against the original integrand. Target-free symbolic coefficients are allowed; public strategy/result metadata remains unchanged.
- `RUBI-TIER1-SECTION1-POLY-DEGREE-LIFT1` widens only the integration-local expanded-direct and exact-backcheck caps so higher-degree monomials produced by still-bounded positive-integer polynomial expansion can be adopted through `direct-rule`; term/node caps, branch-sensitive stops, public strategy metadata, and Rubi recurrence machinery remain unchanged.
- `RUBI-TIER1-POLY-BYPARTS-FEEDER1` adds an internal expanded-polynomial feeder before existing integration-by-parts fallback: normal by-parts still runs first, bounded polynomial-like factors may expand only as fallback, successful results keep visible `integration-by-parts`, and original-integrand exact backcheck is mandatory.
- `RUBI-TIER1-SECTION1-QUADRATIC-RECIPROCAL-POWER1` adds the narrow repeated quadratic reciprocal case `1/(c+u^2)^2` where `c` is a positive exact square scalar and `u` is an exact affine form. It routes through visible `partial-fractions`, preserves inverse-trig/substitution precedence, and relies on exact rational-function equivalence in antiderivative backcheck; broader recurrence, symbolic coefficients, higher powers, and branch-sensitive forms remain deferred.
- `RUBI-TIER1-AFFINE-POWER1` adds exact-rational affine power integration for `(m*x+n)^p` with bounded integer powers, logarithmic `p=-1`, selected negative powers, and exact derivative backcheck support for `ln|u|`; public Calculus strategy/result metadata remains unchanged.
- `RUBI-TIER1-BINOMIAL-DERIVATIVE-SUB1` adds an exact-rational derivative-present binomial substitution fallback for `C*x^(n-1)*(a+b*x^n)^p`, including reciprocal/log cases where exact backcheck succeeds. It stays under visible `u-substitution` and leaves existing broader numeric substitution behavior untouched.
- `RUBI-TIER1-REPEATED-LINEAR-RATIONAL1` strengthens exact repeated-linear rational integration: high single affine reciprocals like `1/(m*x+n)^k` route through visible `partial-fractions`, while bounded products of repeated exact linear factors continue through the rational-function core with exact backcheck.
- `RUBI-TIER1-QUADRATIC-RECIPROCAL-NUMERATOR1` adds exact-rational affine numerators over repeated quadratic reciprocals `(A*u+B)/(c+u^2)^2` when `u` is exact affine and `c` is a positive exact square. Mixed and constant numerator cases stay visible `partial-fractions`; pure derivative-numerator overlaps still fall through to `u-substitution`.
- `RUBI-TIER1-QUADRATIC-RECIPROCAL-POWER-LIFT1` lifts constant repeated quadratic reciprocal support to powers `3` and `4` only, using bounded recurrence terms over exact affine `u` and positive exact-square `c`; power `5+`, non-square constants, symbolic coefficients, and broad recurrence machinery remain deferred.
- `RUBI-TIER1-RATIONAL-QUADRATIC-MIXED1` locks bounded exact-rational partial fractions with repeated/ordinary linear factors plus one irreducible quadratic factor as verified coverage through the rational-function core. Multiple quadratic factors, symbolic coefficients, and broad factorization remain controlled unsupported.
- `RUBI-TIER1-QUADRATIC-RECIPROCAL-NONSQUARE1` extends literal repeated quadratic reciprocal powers `1/(c+u^2)^p` to positive exact-rational nonsquare `c` for powers `2..4`. It keeps visible `partial-fractions`, preserves inverse-trig power-1 precedence, reuses exact affine recurrence formulas, and adds verifier normalization for constant radical factors so accepted nonsquare arctan forms backcheck as `verified-exact`; symbolic coefficients, `c<=0`, power `5+`, completed-square parsing, public schemas, and UI/runtime surfaces remain deferred.
- `RUBI-TIER1-QUADRATIC-COMPLETE-SQUARE1` adds a bounded completed-square parser for exact-rational irreducible quadratics, including scalar factors such as `2x^2+4x+5`. Supported repeated powers normalize to the existing quadratic reciprocal recurrence under visible `partial-fractions` with exact backcheck; reducible quadratics remain on existing linear partial-fraction routes, while symbolic coefficients, branch-sensitive forms, and broad factorization stay deferred.
- `RUBI-TIER1-QUADRATIC-NUMERATOR-POWER-LIFT1` lifts exact-rational affine numerators over repeated quadratic reciprocal powers from power `2` to powers `3` and `4`, including nonsquare and completed-square denominators. Mixed and constant-numerator cases stay visible `partial-fractions`, pure derivative-numerator overlaps remain with `u-substitution`, and symbolic coefficients, powers `5+`, branch-sensitive forms, and public schema changes remain deferred.
- `RUBI-TIER1-MULTI-QUADRATIC-PARTIAL-FRACTIONS1` extends the rational-function primitive to factor and solve partial fractions with up to two irreducible quadratic factor groups, each multiplicity capped at `2`, under the existing denominator-degree cap. Quadratic-power terms integrate through the repeated quadratic recurrence, pure derivative overlaps keep substitution precedence, and broader quartic/factorization families remain controlled unsupported.
- `RUBI-TIER1-AFFINE-LOG-BYPARTS1` adds polynomial times affine-log integration by parts for exact-rational affine arguments such as `x ln(2x+3)` and bounded expanded polynomial products such as `(x+1)^2 ln(2x+3)`. Successful natural-log cases keep visible `integration-by-parts` with exact original-integrand backcheck, while base-10 `log` is accepted only with derivative-backcheck confidence because symbolic `ln(10)` cancellation remains deferred.
- `RUBI-TIER1-RECIPROCAL-BINOMIAL-SUB1` adds exact-rational derivative-present reciprocal binomial substitution for `C*x^(-n-1)*(a+b*x^(-n))^p`, including bounded integer powers and logarithmic `p=-1`. Successful matches keep visible `u-substitution` even when rational routes would otherwise claim the expression first; missing derivative factors are not claimed by this route, and may still resolve by existing partial fractions when already supported.
- `CALCULUS-INTEGRALS-EDITOR-SOURCE1` makes the main editor the body/source editor for Indefinite, Definite, and Improper Integral screens. The lower Calculus workspace now keeps bounds/kind controls and a read-only generated request preview, F2 is `Focus Editor` on integral screens, and the structured `Answer` block is the only rendered result answer; Display, History, OOE, Tauri, persistence, and public Calculus result schemas remain unchanged.
- Guided Calculus derivative screens use the shared main editor for the raw body, keep the structured Display `Answer` block as the single derivative result owner, gate symbolic differentiation with internal route/cost preflight, and allow a selected single-symbol variable such as `x`, `t`, or canonical Greek ids like `theta`. Partial Derivative lives under `Calculus > Derivatives` while legacy `partialsHome` route compatibility remains. The lower editor strip is now an editable derivative operator rail: it keeps the function body separate, accepts compact operator strings such as `d/dx`, `d^3/dx^3`, and compact partial forms, stores optional `operatorLatex` for preview/copy/history/replay, and shows written plus applied readback using rightmost-first mixed-partial convention. Ordinary higher-order derivatives up to order `10` evaluate through repeated symbolic differentiation, and higher-order derivative-at-point differentiates symbolically before substituting the numeric point. Mixed partial operators up to order `10` now evaluate through the same applied-path model without Clairaut/symmetry reordering. Successful guided derivative, derivative-at-point, and partial-derivative runs expose a collapsed `Derivative Steps` detail card with the operator, applied order, each `D_n` stage, and a separate substitution line for derivative-at-point while the Answer card remains the only final-result owner.
- Equation now exposes `src/lib/equation/implicit-derivative-solve.ts` as the public seam for later Calculus implicit differentiation. The seam accepts a differentiated relation plus one internal derivative placeholder, delegates to Equation selected-target isolation, accepts only one clean placeholder equality, maps it to a display derivative such as `dy/dx`, and returns controlled stops for unsupported or multi-branch derivative outputs. The guided Calculus implicit screen remains the next separate milestone.
- `CALCULUS-ANSWER-PREVIEW-AUTOSIZE1` makes structured Display answer cards and generated integral preview math expand or scroll cleanly for large/nested expressions. This is a CSS/layout-only fix over existing result and preview surfaces; Formula Viewer, Display schemas, OOE, History, Tauri, persistence, copy contracts, and public Calculus result shapes remain unchanged.
- `INTEGRATION-RATIONAL-PF-PERF-SPLIT-AUDIT0` splits the former monolithic rational partial-fractions integration test into named timing groups and per-case hot tests without changing integration behavior. Evidence points to exact verifier/equivalence cost for nonsquare and completed-square repeated-quadratic arctan forms, not classifier routing or general partial-fraction decomposition.
- `INTEGRATION-RATIONAL-PF-BACKCHECK-CACHE1` fixes the rational partial-fractions verifier bottleneck: AST-only differentiation no longer renders LaTeX during normalization, exact backcheck renders derivative LaTeX lazily only for non-exact paths, radical-scalar cleanup runs before expensive equivalence fallbacks, and verifier-only raw exact-rational equivalence avoids public rational normalization/rendering for proof checks. The full symbolic integration test file now runs in about `5s` instead of about `250s`.
- `RUBI-TIER1-LINEAR-QUADRATIC-PF-LIFT2` lifts bounded exact-rational rational partial fractions to one irreducible quadratic factor group with multiplicity up to `3` alongside bounded exact-rational linear factors. Two-quadratic support remains capped at existing multiplicity `2`; the rational-function PF solve may use an 8x8 exact matrix only in this bounded path, and verifier-local raw rational reduction cancels exact polynomial gcds after arithmetic so exact backcheck does not fail from fake denominator-degree growth.
- `RUBI-TIER1-QUADRATIC-NUMERATOR-GENERAL1` locks existing completed-square numerator handling as verified Tier-1 coverage for exact-rational `(A*x+B)/(a*x^2+b*x+c)^p` with irreducible quadratics and powers `2..4`. Scaled completed-square examples such as `(x+5)/(2x^2+4x+5)^3` remain visible `partial-fractions`, while pure derivative-numerator overlaps such as `(2x+2)/(x^2+2x+3)^4` stay visible `u-substitution`.
- `RUBI-TIER1-TRIG-AFFINE-BASIC1` adds exact-rational affine trig primitives for `sin(m*x+n)`, `cos(m*x+n)`, `tan(m*x+n)`, `cot(m*x+n)`, `sec(m*x+n)^2`, and `csc(m*x+n)^2` under visible `direct-rule` after the classifier route plan. Differentiation now supports the needed `cot`, `sec`, `csc`, `ln(sin(u))`, and `ln(cos(u))` derivatives for exact backcheck; broader trig powers, products, symbolic parameters, branch analysis, and public schema changes remain deferred.
- `RUBI-TIER1-EXP-AFFINE-BASIC1` adds exact-rational positive non-unit numeric-base affine exponentials such as `2^(2x+3)` and `(1/2)^(3x-1)` under visible `direct-rule` with exact `ln(a)` derivative backcheck. Existing `e^(m*x+n)` coverage now formats exact-rational affine slopes without decimal leakage in the direct helper; zero, negative, unit, decimal, and symbolic bases plus exponential products remain deferred.
- `MATH-EDITOR-PASTE-FUNCTION-POWER-CANONICALIZATION1` repairs the shared MathLive paste canonicalization path so reciprocal trig names (`sec`, `csc`, `cot`), grouped exponent text, and clear `e^(...)` exponential bases reach Calculus and other editors as canonical LaTeX. This is an input-layer repair only; solver behavior, Display, History, OOE, Tauri, persistence, and public Calculus schemas remain unchanged.
- `RUBI-TIER1-TRIG-DERIVATIVE-PRODUCTS1` adds exact-rational affine derivative-present trig products `sec(u)tan(u)`, `csc(u)cot(u)`, and `sin(u)cos(u)` under visible `u-substitution` with exact backcheck. The matcher strips exact numeric scalars but rejects extra symbolic factors, non-affine arguments, symbolic coefficients, and broader trig products.
- `RUBI-TIER1-TRIG-POWER-REDUCTION1` adds the core exact-rational affine square reductions for `sin(u)^2`, `cos(u)^2`, `tan(u)^2`, and `cot(u)^2` under visible `direct-rule`. Exact backcheck now has a scoped verifier-local trig-square identity normalization for these four identities only; higher trig powers, sec/csc reductions beyond existing square primitives, symbolic coefficients, and broad trig recurrence remain deferred.
- `RUBI-TIER1-TRIG-PRODUCT-TO-SUM1` adds bounded exact-rational affine product-to-sum support for two-factor `sin*cos`, `sin*sin`, and `cos*cos` products under visible `direct-rule`. Generated affine trig terms delegate back to existing direct primitives and exact backcheck uses scoped two-factor trig product identity normalization; symbolic coefficients, non-affine arguments, branch-sensitive carriers, and products with more than two trig factors remain deferred.
- `RUBI-TIER1-EXP-POLY-NUMERIC-BASE-BYPARTS1` extends polynomial-times-exponential integration by parts from `e^(mx+n)` to positive exact-rational numeric bases `a^(mx+n)`. Simple products still verify through the normal derivative backcheck; expanded polynomial-factor cases can use the exact bounded expansion plus finite recurrence proof, while symbolic, zero, negative, and decimal bases remain deferred.
- `CALCULUS-LAPLACE-TABLE1` adds a bounded Calculus Laplace transform screen with the main editor editing `f(t)` and fixed transform variable `s`. The v1 table covers constants, `t^n` up to the cap, `e^(a t)`, `sin/cos/sinh/cosh(a t)`, and `e^(a t)sin/cos(b t)` for exact-rational numeric parameters; inverse Laplace, convolution, step functions, Dirac delta, symbolic parameters, Display schemas, OOE, History schema shape, and public Calculus result schemas remain unchanged.
- `RUBI-TIER1-BYPARTS-CASCADE-POLY-EXP-TRIG1` hardens finite by-parts cascade coverage for exact-rational polynomial factors times affine exponential, numeric-base exponential, sine, and cosine factors up to the existing degree cap. The current runtime helpers already cover the agreed cases, so this slice adds focused regression evidence while keeping visible `integration-by-parts`, exact backcheck, public schemas, and symbolic coefficient policy unchanged.
- `RUBI-TIER1-TRIG-SIN-COS-PARITY-POWER1` adds bounded parity reductions for exact-rational affine `sin(u)^n` and `cos(u)^n` through exponent `12`. Results stay visible `direct-rule` and verify exactly through a scoped sin/cos power identity normalizer; over-cap powers, symbolic coefficients, branch-sensitive forms, broad trig recurrence, and public schema changes remain deferred.
- `RUBI-TIER1-TRIG-TAN-SEC-COT-CSC-POWER1` adds bounded exact-rational affine `tan/sec` and `cot/csc` power reductions with each exponent capped at `6`. Derivative-present overlaps keep `u-substitution`; direct-rule cases verify exactly through scoped even `sec/csc` identity normalization. Symbolic coefficients, non-affine arguments, over-cap products, odd standalone `sec/csc` recurrences, branch analysis, and broad trig recurrence remain deferred.
- `RUBI-TIER1-TRIG-PRODUCT-TO-SUM-LIFT1` lifts the two-factor exact-rational affine `sin/cos` product-to-sum route to accept exact scalar multiples and normalized factor order under visible `direct-rule`. Exact proof uses the same scoped trig-product identity normalizer with scalar-aware decomposition; symbolic scalar coefficients, non-affine arguments, branch-sensitive carriers, and products with extra trig factors remain deferred.
- `RUBI-TIER1-INVERSE-TRIG-AFFINE-VARIANTS1` widens visible `inverse-trig` coverage for exact-rational scalar numerators, affine shifts, rational square constants, and positive completed-square arctan denominators. Scaled arcsin variants now prove exactly through verifier-local reciprocal-square-root normalization. Arcsec-style reciprocal-root forms remain unsupported until branch/domain facts and readback are available.
- `RUBI-TIER1-TRIG-SUBSTITUTION-AUDIT0` closes the Tier-I trig-substitution readiness audit without runtime changes. The next safe implementation slice is exact-rational affine `sqrt(a^2-(m*x+n)^2)` only; `sqrt(a^2+u^2)` needs logarithmic radical readback proof, and `sqrt(u^2-a^2)`/arcsec-style forms need stronger branch/domain facts.
- `RUBI-TIER1-SYMBOLIC-COEFFICIENT-CATCHUP1` adds variable-aware Calculus integrals and target-free symbolic coefficient catchup for the completed Tier-I families. Accepted symbolic coefficient cases use route-local rule proofs plus visible exact-supplement facts, positive symbolic-base facts are explicit, and numeric-confidence symbolic adoption remains disallowed.
- `MATH-EDITOR-ENTER-SUBMIT1` makes plain Enter in shared main MathEditor surfaces submit through the existing Run/F1 path instead of letting MathLive insert a line break. This prevents newline-contaminated Calculus integral inputs while leaving solver, Display, History, OOE, Tauri, persistence, and public result schemas unchanged.
- `RUBI-TIER1-CLOSEOUT-GATED1` closes the agreed Rubi Tier-I surface for current exact-rational plus target-free symbolic scope. Backend reliability now includes guarded lazy Compute Engine fallback, selected-variable target-free polynomial direct integration, and exact-rational affine trig-substitution radicals; UI/copy/display reliability now copies exact results as MathLive-safe LaTeX in rendered/LaTeX modes and stacks combined `Valid When` facts vertically.
- `RISCH-NORMAN-SUBSTRATE1` starts Layer 2 as behavior-invisible internal profiling only. It recognizes selected-variable affine `e^u`, positive-base `a^u`, affine `sin/cos` pairs, and affine `ln/log` readiness over exact-rational plus target-free symbolic coefficients, records derivative-closure basis evidence and required facts, and leaves dispatch/public Calculus outputs unchanged; ansatz solving, symbolic linear systems, non-elementary certificates, and public strategy labels remain deferred.
- `RISCH-NORMAN-COEFFICIENT-FIELD1` adds the internal MathJSON coefficient field for later ansatz solving: exact-rational plus target-free symbolic coefficients can be combined with scoped arithmetic and denominator facts, while decimals, branch-sensitive carriers, selected-variable-dependent coefficients, coefficient-side transcendentals, and zero denominators stop before solving.
- `RISCH-NORMAN-LINEAR-SOLVER1` adds the internal bounded square linear solver over that coefficient field. It records target-free symbolic pivot facts for future ansatz proof evidence, stops cleanly on unsupported systems, and remains behavior-invisible to integration dispatch and public Calculus output.
- `RISCH-NORMAN-EXP-ANSATZ1` adds direct/test-facing exponential ansatz solving for polynomial coefficients times affine `e^u` or positive-base `q^u`. It accepts exact-rational plus target-free symbolic polynomial coefficients and generated positive-base log constants under facts, but still does not run from integration dispatch.
- `RISCH-NORMAN-SINCOS-ANSATZ1` adds direct/test-facing paired sine/cosine ansatz solving for `P(v)sin(u)` and `P(v)cos(u)` over the same target-free symbolic polynomial substrate. It uses a finite derivative-closed recurrence for fast proof evidence and remains behavior-invisible to integration dispatch.
- `RISCH-NORMAN-LOG-READINESS1` keeps affine-log work readiness-only. Profiles for `P(v)ln(u)` and `P(v)log(u)` now expose the missing symbolic rational-correction solver prerequisite without adding integration adoption.
- `RISCH-NORMAN-DISPATCH-PROBE1` makes the first guarded Risch-Norman adoption live only as an internal fallback after existing Tier-I routes miss. It accepts exponential and sine/cosine ansatz proofs for target-free symbolic polynomial coefficients, keeps the public strategy label as `integration-by-parts`, carries visible supplement facts, and leaves logs readiness-only.
- `CALCULUS-INTEGRAND-EQUATION-GUARD1` blocks relation roots such as `Equal` before symbolic integration routing. Calculus indefinite integrals now report that they expect an expression `f(x)`, not an equation or relation, while normal expression inputs still route through the existing symbolic/CE fallback pipeline.
- `RISCH-NORMAN-LOG-CORRECTION1` makes affine-log correction live through the internal RN dispatch probe for `P(v)ln(a*v+b)` and `P(v)log(a*v+b)` with target-free symbolic polynomial coefficients through the existing by-parts degree cap. It emits substitution-form finite sums in powers of `a*v+b`, carries visible slope/domain facts, keeps public strategy as `integration-by-parts`, and still rejects non-affine logs, branch-sensitive carriers, and selected-variable-dependent coefficients.
- `RISCH-NORMAN-LINEAR-COMBINATION1` adds a final guarded top-level sum combiner after normal planned routes and compatibility fallback miss. It resolves each additive term through existing Tier-I/RN routes without recursively invoking itself, adopts only all-term successes, merges supplement facts, keeps homogeneous strategies when possible, and uses public `integration-by-parts` for mixed-strategy sums.
- `RISCH-NORMAN-EXP-SINCOS-MIXED1` adds guarded internal RN adoption for bounded polynomial times affine exponential-sine/cosine products such as `P(x)e^(a*x+b)sin(c*x+d)` and `P(x)e^(a*x+b)cos(c*x+d)`. The route solves in the derivative-closed `e^u(Q sin(v)+R cos(v))` span, carries the pivot fact `a^2+c^2\ne0`, keeps public strategy as `integration-by-parts`, and rejects nested towers, extra trig factors, branch-sensitive carriers, and selected-variable-dependent coefficients.
- `RISCH-NORMAN-SYMBOLIC-TRIG-PRODUCTS1` adds internal symbolic affine product-to-sum support for two-factor `sin/cos` products with exact-rational or target-free symbolic slopes/shifts. The helper runs from the existing `direct-rule` route after exact direct rules miss, emits nonzero denominator facts such as `a+c\ne0` and `a-c\ne0`, handles structural same-argument products through direct identities, and keeps unresolved degenerate symbolic same-slope cases unsupported.
- `RISCH-NORMAN-AFFINE-RATIONAL-CORRECTION-LIFT1` adds a bounded affine rational-correction helper for `P(v)/(a*v+b)^k` with polynomial degree `<=6` and affine denominator powers `1..3`. It uses affine substitution, keeps simple reciprocal ownership with existing direct affine rules, surfaces supported target-dependent affine denominators as `partial-fractions`, and does not widen to symbolic quadratic partial fractions or broad symbolic rational decomposition.
- `RISCH-NORMAN-FOUNDATION-CHECKPOINT0` records the current RN foundation as a bounded internal proof-based layer rather than full Risch. The foundation now includes coefficient representation, bounded linear solving, exp/sin/cos/log/mixed/rational correction adoption, visible facts, and guarded dispatch; remaining deferrals include full Risch, non-elementary certificates, broad symbolic partial fractions, symbolic degeneracy case splitting, nested towers, Euler substitutions, and public RN metadata.
- `RISCH-NORMAN-OUTPUT-HYGIENE1` keeps RN exact-output readback notation-safe for the current generated families. Mixed exp-sincos, affine-log, exponential, and affine-rational correction outputs now normalize malformed sign sequences, negative fraction placement, and inline slash divisions such as `(2c)/a` before Display, with MathLive-renderability regression checks and no public schema/strategy changes.
- RN generic-case fact posture is baseline-locked: symbolic trig product-to-sum denominators such as `a+c` and `a-c`, exp-sincos pivots such as `a^2+c^2`, positive-base facts, affine-log facts, and affine-slope facts remain visible `Valid When` conditions unless a later branch milestone explicitly implements the corresponding degenerate case.
- `RN-ANSATZ-ORCHESTRATOR1` unifies the internal RN fallback entrypoint across exponential, sine/cosine, exp-sincos, affine-log, and affine-rational correction families. Dispatch still filters by existing route family so public labels remain `integration-by-parts` or `partial-fractions`, not a public `risch-norman` strategy.
- `RN-NODE-FIRST-READBACK1` makes successful RN orchestrator results carry an internal MathJSON antiderivative node. Affine rational correction now builds the node alongside its existing exact readback, while public exact LaTeX and strategy labels remain unchanged.
- `RN-LOG-RATIONAL-CORRECTION-LIFT1` adds bounded affine Laurent-log correction for `P(x)ln(a*x+b)/(a*x+b)^k` and `P(x)log(a*x+b)/(a*x+b)^k` with degree cap `6` and denominator power cap `3`. It stays visible as `integration-by-parts`, carries `a\ne0` and `ax+b>0` facts, and rejects non-affine logs, denominator mismatches, decimals, branch-sensitive carriers, and over-cap powers.
- `SYMBOLIC-RATIONAL-QUADRATIC-AUDIT0` keeps symbolic quadratic rational widening audit-only. The safe first later slice is degree-one numerator over one power-one symbolic quadratic denominator under generic facts `a\ne0` and `4ac-b^2>0`; repeated powers, reducible/negative-discriminant branches, and broad symbolic partial fractions remain deferred.
- `RN-READBACK-MULTIPLICATION-STYLE1` scopes explicit multiplication-dot readback and harmless sign/group cleanup to RN-generated and symbolic-quadratic rational exact outputs. It avoids global Display rendering changes while making function factors such as `sin`, `cos`, and `arctan` visibly multiplied by preceding nontrivial factors.
- `RN-SYMBOLIC-QUADRATIC-RATIONAL-LINEAR-NUMERATOR1` makes the first symbolic quadratic rational numerator slice live for `(A*v+B)/(a*v^2+b*v+c)`. It stays visible as `partial-fractions`, proves by derivative-log plus reciprocal-quadratic arctan decomposition, and carries only the generic positive-branch facts `a\ne0` and `4ac-b^2>0`.
- `RN-SYMBOLIC-QUADRATIC-BRANCH-FACTS-BASELINE1` locks symbolic quadratic branch facts without adding branches. The generic symbolic branch is conditional on `4ac-b^2>0`; exact-rational reducible/positive cases keep existing precedence, while repeated-linear, negative-discriminant, unknown-sign, repeated-power, and reducible symbolic branches remain deferred.
- `RN-SYMBOLIC-QUADRATIC-POWER-READINESS1` adds readiness-only profiling for `(A*v+B)/(a*v^2+b*v+c)^2` and power `3` under the same generic positive symbolic branch facts. Dispatch still returns controlled unsupported output for repeated symbolic quadratic powers; the readiness record captures the needed derivative/residual split, recurrence shape, readback constraints, and stop reasons before live adoption.
- `RN-READBACK-FACTOR-ORDER-AND-FUNCTION-CLARITY2` keeps RN exp-sincos results node-backed internally while polishing family `exactLatex`: safe fraction/monomial products are folded into fraction numerators, additive coefficient products prefer `x(...)` ordering, and symbolic display normalization preserves explicit function-product dots such as `\cdot\arctan`.
- `RN-SYMBOLIC-QUADRATIC-CASEWISE-BRANCHES1` supersedes the earlier positive-only symbolic quadratic rational branch for power-one denominators. `1/(a*v^2+b*v+c)` and `(A*v+B)/(a*v^2+b*v+c)` now emit a casewise answer for `4ac-b^2>0`, `=0`, and `<0`, while repeated symbolic powers remain deferred.
- `RN-SYMBOLIC-QUADRATIC-REPEATED-POWER-ADOPTION1` makes repeated symbolic quadratic rational powers `2` and `3` live only under the positive generic branch `4ac-b^2>0`. The route uses a derivative/residual split plus bounded reciprocal-quadratic recurrence, stays visible as `partial-fractions`, and keeps powers `4+`, zero/negative repeated branches, multiple symbolic quadratics, reducible symbolic case splitting, and broad symbolic partial fractions deferred.
- `RN-LOG-DERIVATIVE-DETECTOR1` adds bounded symbolic log-derivative detection for `k*D'(v)/D(v)` with denominator degree `2..6`. It reuses the RN coefficient/polynomial substrate, proves matches by coefficient cross-products, stays visible as `partial-fractions`, and preserves existing exact-rational derivative-ratio plus affine direct-rule overlap ownership.
- `RN-SYMBOLIC-RATIONAL-HERMITE-REDUCTION1` adds the first bounded symbolic rational Hermite-correction adoption. It detects exact derivatives of `P(v)/Q(v)^m` and residuals reducible to symbolic log-derivatives for target-free symbolic rational inputs with denominator powers `2..3`, stays visible as `partial-fractions`, and avoids stealing exact-rational substitution/rational routes by requiring actual target-free symbolic coefficients.
- `RN-DIFFERENTIAL-FIELD-TOWER-ORCHESTRATOR1` consolidates RN fallback probing behind a single bounded tower profile and attempt plan over exponential, sine/cosine, exp-sincos, affine-log, affine-log-rational, symbolic log-derivative, Hermite correction, and affine-rational correction families. Public strategy labels, result schemas, and Tier-I precedence remain unchanged.
- `RN-READBACK-FACTOR-ORDER-AND-CASEWISE-USABILITY3` keeps the existing RN log-derivative route usable from typed Calculus integral input by canonicalizing only the Calculus body parse boundary and normalizing scalar-times-quotient ASTs. Generated RN/readback output now cleans nested negative fraction signs, and casewise answer rows have scoped extra spacing; Equation, shared MathEditor live input, public schemas, and OOE/History/Tauri stay unchanged.
- `SYMBOLIC-COEFFICIENT-DOMAIN-AUDIT0` locks the next infrastructure boundary before LRT work: LRT remains Integration/RN-owned, while only domain-neutral algebra primitives such as coefficient domains, squarefree/resultant helpers, algebraic-root descriptors, and exact fact objects are candidates for later sharing. Equation may consume those primitives only through Equation-owned routes and readback, never by exposing RN/LRT steps.
- `SHARED-ALGEBRA-COEFFICIENT-DOMAIN1` extracts the RN MathJSON coefficient field into a domain-neutral symbolic primitive. RN keeps compatibility wrappers and behavior is unchanged; Equation has no consumer yet, and LRT/RN orchestration remains Integration-owned.
- `SHARED-POLYNOMIAL-SQUAREFREE-RESULTANT1` adds bounded shared symbolic-polynomial primitives over the shared coefficient domain: parse/build, derivative, exact coefficient-field division, squarefree readiness, Sylvester matrices, and determinant resultants. It remains primitive infrastructure only; Equation elimination and RN/LRT dispatch are unchanged.
- `ALGEBRAIC-ROOT-DESCRIPTOR1` adds internal named-root descriptors for future LRT algebraic-log readback. Definitions use `alpha_i`-style roots and detail-section lines without raw `RootOf`; Equation has no descriptor consumer and no public Display schema changed.
- `RN-HERMITE-REDUCTION-GENERALIZE1` lifts the internal symbolic Hermite rational-correction proof cap to denominator degree `8` while preserving denominator power cap `3`, proof-only adoption, public `partial-fractions` labels, and LRT logarithmic completion deferral.
- `RN-LRT-LOG-PART-SUBSTRATE1` adds a behavior-invisible Rothstein/Lazard-Rioboo-Trager logarithmic-part constructor for squarefree rational residuals. It builds bounded resultants and named-root/formal-gcd log evidence without dispatch adoption; exact denominators and target-free symbolic numerators are covered, while symbolic denominator coefficients stop before algebraic coefficient reduction.
- `RN-LRT-RATIONAL-INTEGRATION1` wires the bounded LRT logarithmic-part constructor into RN partial-fractions fallback for squarefree exact cubic denominators with proper residuals, returning named `alpha_i` log terms and detail definitions. Symbolic denominator coefficients, degree `4+` LRT adoption, repeated denominators, public RN/LRT labels, and public schemas remain deferred.
- `RN-TOWER-BASIS-GENERATOR1` extracts RN fallback planning into a bounded tower-basis generator that profiles once and returns ordered method attempts across exp, sin/cos, exp-sincos, affine-log, affine-log-rational, log-derivative, Hermite, LRT-rational, and affine-rational families. Dispatch behavior and public strategy labels remain unchanged.
- `RN-PRACTICAL-RISCH-NORMAN-CHECKPOINT0` records the current RN/LRT layer as roughly `70-80%` of a practical bounded RN heuristic for Calcwiz's scoped target-free symbolic work. Remaining general-RN gaps are nested towers, algebraic extensions beyond descriptors, broad degeneracy branching, robust algebraic-log trace/readback, and formal non-existence certificates.
- `TRANSCENDENTAL-RISCH-CERTIFICATE-AUDIT0` records the next integration direction as certificate-first rather than more RN breadth. The audit scopes prerequisites for theorem-backed non-elementary messages, including exact differentiation closure, tower classification, constant-field discipline, Liouville/log-derivative proof objects, node-first readback, visible facts, and controlled stop reasons.
- `TRANSCENDENTAL-RISCH-DIFFERENTIATION-CLOSURE-AUDIT0` records that no broad differentiation rewrite is needed before first certificates, but certificate proofs must use a direct exact differentiation path with Compute Engine fallback denied, exact scalar preservation, tower-head normalization, and focused closure tests before any live non-elementary claim.
- `TRANSCENDENTAL-RISCH-CERTIFICATE-PROOF-DIFF1` adds that proof-local exact differentiation wrapper. Certificate proof differentiation normalizes supported `Exp(u)` spelling to canonical `e^u`, denies Compute Engine fallback, records closure heads/strategies, and rejects decimals, branch-sensitive carriers, special-function heads, and unsupported fallback forms without changing live integration dispatch.
- `TRANSCENDENTAL-RISCH-TOWER-PROFILE1` adds behavior-invisible certificate tower profiling. It marks constant/affine exponentials as elementary-owned, quadratic exponentials as certificate-ready with target-free symbolic coefficient facts, and stops cubic, nested, decimal, branch-sensitive, unsupported-head, and non-polynomial cases before any live certificate dispatch.
- `TRANSCENDENTAL-RISCH-CERTIFICATE-RESULT-SHAPE1` adds an internal non-elementary certificate result object and Calculus evaluation adapter. Certificates render through existing exact/result detail/supplement fields, omit antiderivative backcheck/candidate metadata, and keep public Calculus strategies/schema unchanged until live dispatch wiring.
- `TRANSCENDENTAL-RISCH-EXP-QUADRATIC-PROOF1` adds behavior-invisible proof evidence for certificate-ready `e^(quadratic)` towers. The proof records proof-local exponent/integrand differentiation, the Liouville rational-certificate obstruction equation, required facts, and detail sections while keeping affine exponentials elementary-owned and dispatch unchanged.
- `TRANSCENDENTAL-RISCH-EXP-QUADRATIC-CERTIFICATE1` makes the first non-elementary certificate family live for indefinite integrals after existing symbolic antiderivative routes miss and before Compute Engine fallback. Pure `e^(quadratic)` integrands now render successful certificate cards with visible facts and proof details, while affine exponentials and derivative-present substitution overlaps remain elementary-owned.
- `TRANSCENDENTAL-CERTIFICATE-EVIDENCE-CLARITY1` keeps the live `e^(quadratic)` certificate route behavior unchanged but clarifies proof evidence readback: the Liouville equation is labeled as an auxiliary rational-certificate obligation for `r(variable)`, not a user-facing condition on the input.
- `SPECIAL-FUNCTION-ERF-ERFI-SUBSTRATE1` adds behavior-invisible `erf`/`erfi` substrate support in symbolic differentiation and certificate proof-local differentiation. Direct `erf(u)` and `erfi(u)` derivatives are exact and Compute Engine-free, while `Si`, `Ci`, `Ei`, and integration adoption remain deferred.
- `SPECIAL-FUNCTION-ERF-ERFI-EXP-QUADRATIC-SYMBOLIC-CASEWISE1` extends the special-function answer layer to target-free symbolic quadratic leading coefficients. Pure `e^(a*v^2+b*v+c)` certificate cases now show casewise `erf`/`erfi` rows for `a<0` and `a>0`, keep `a\ne0` as the global fact, and preserve affine exponential and substitution overlap precedence.
- `SPECIAL-FUNCTION-ERF-ERFI-EXP-QUADRATIC-EXACT1` makes exact-rational quadratic exponential integrals show named `erf`/`erfi` formulas as the main answer while preserving the non-elementary certificate details as proof support. Symbolic leading-coefficient casewise `erf/erfi` readback remains deferred to the next special-function slice.
- `SPECIAL-FUNCTION-COPY-READBACK-NOTATION1` locks the copy/readback contract for special-function answers: generated integral Copy Expr, Copy Result, rendered/LaTeX copy, plain-text copy, casewise symbolic `erf`/`erfi` output, and History replay now have focused UI regression coverage, while the runtime copy path remains unchanged because it already honors the active math-notation setting.
- `TRANSCENDENTAL-SPECIAL-FUNCTION-READBACK-AUDIT0` records future requirements for named special-function output such as `erf`, `erfi`, `Si`, and `Ci`. The audit keeps current certificate output as the non-elementary baseline and defers special-function readback until direct differentiation, branch/domain facts, and MathLive-safe copy/readback are locked.
- `SPECIAL-FUNCTION-SI-CI-AUDIT0` narrows the next special-function slice to affine sine/cosine quotient families such as `sin(u)/u` and `cos(u)/u`, with direct `Si`/`Ci` differentiation, MathLive-safe readback, conservative input-domain facts, and a positive-real first branch for `Ci` required before live adoption.
- `TRANSCENDENTAL-CERTIFICATE-NEXT-FAMILY-AUDIT0` recommends `Si/Ci` as the next rigorous certificate family after `erf/erfi`, while deferring `Ei/li`, Fresnel-style quadratic trig, and broad depth-2 tower widening until their proof-field, branch/readback, and exact differentiation prerequisites are explicit.

The hard-removal policy is intentional: old records are not mapped forward in live source. Historical journals may still mention old names because they document what happened at the time.

## Supercarrier Foundation

Supercarrier foundation is closed enough for current development after `SUPERCARRIER-FOUNDATION-CLOSEOUT0`.

Standing model:

- OOE is runtime traffic control.
- OOE Event Outbox reports OOE lifecycle facts.
- Supercarrier compartments define ownership, import boundaries, damage containment, diagnostics labels, and extension discipline.
- Diagnostics and compartment projections observe existing facts; they do not decide, route, cancel, retry, or commit.
- Surface Protocol remains a future external integration contract, not a current implementation.

Current foundation state:

- `src/lib/compartments/manifest.ts` is the declarative contract source for compartment ids, labels, owned paths, public seams, private paths, dependency policies, state surface posture, OOE fact mappings, and Surface candidacy metadata.
- `tools/compartment-boundaries-core.mjs` consumes manifest-owned truth for ids, path labels, and private district checks while keeping validator rule semantics in code.
- `npm run test:compartments-boundaries` validates the read-only boundary contract.
- `npm run report:compartments` gives a static contract report plus validator pass/fail summary.
- The latest closeout report passed and validated 651 source files, 26 OOE TypeScript files, and 6 OOE Rust files.

Known broad-but-acceptable compartments:

- `app-shell`: broad visual shell/workspace/component/style ownership.
- `app-runtime`: request construction and mode-hook coordination.
- `app-state-history-variables`: persistence, history, calculator memory, variable memory, hints, and named-variable policy.
- `table`: still small and flat.
- `navigation-input-kernel`: broad shared primitives around navigation, input, editor, numeric, kernel, and virtual keyboard.
- `playground` and `reference-mirrors`: isolated research/incubation/reference areas.

Do not add more Supercarrier scaffolding unless a real messy compartment, product feature, or contributor workflow needs it.

## OOE Current State

OOE is grouped into direct districts without root compatibility stubs:

- `pilots/`
- `job-launch/`
- `runtime-control/`
- `diagnostics/`
- `bridge-schema/`
- `events/`

OOE owns runtime traffic decisions: host selection, launch tickets, stale gates, cancellation checkpoints, commit legality, runtime envelopes, and diagnostics records.

OOE Event Outbox is load-bearing. It reports lifecycle facts and carries optional compartment labels for diagnostics. It is not a command authority, app-wide bus, Surface Protocol, plugin system, or generic event framework.

OOE Diagnostics Panel current state:

- Developer-only panel with `Records`, `Events`, `Jobs`, and `Compartments` tabs.
- Panel reads OOE and compartment diagnostics through `src/lib/ooe/diagnostics/panel-surface.ts`.
- `Records` and `Jobs` keep selected-detail/copy behavior.
- `Events` shows compact lifecycle facts and compartment filtering.
- `Compartments` shows health, issue evidence, manifest metadata, and inspect targets.
- Clear removes diagnostics records, recent jobs, OOE events, and UI-boundary records while preserving active jobs.
- The latest layout fix moved selected detail above scrollable lists and increased desktop panel height.

## App Shell And Runtime

`AppMain.tsx` remains the visual and cross-mode orchestration root, but it should not own persistence or OOE internals directly.

Current extracted app-runtime shells:

- `useHistoryDisplayRuntime`: history state, pending history tickets, display outcome, `Ans`, commit/finalization, replay display restoration, and History persistence helpers.
- `useEquationRuntime`: Equation workspace state, route/menu helpers, request construction, and Equation runtime wiring.
- `useAppPersistenceRuntime`: bootstrap load, runtime label hydration, settings persistence, calculator-memory snapshot restore/reset, variable-memory callbacks, and autosave coordination.

Persistence boundary:

- App runtime imports persistence through `src/lib/app-state/persistence.ts`.
- `src/lib/app-state/tauri.ts` remains the implementation/behavior-test authority.
- App runtime/logic must not import `src/lib/app-state/tauri.ts` directly.
- `AppMain.tsx` must not import `src/lib/app-state/**` or `variable-memory-store.ts` directly.

OOE diagnostics boundary:

- `OoeDiagnosticsPanel` may import only the OOE diagnostics panel seam.
- `CompartmentErrorBoundary` may import only the public `src/lib/compartments/ui-boundary.ts` facade.
- Normal components and workspaces must not deep-read OOE internals.

## Workspace Runtime Boundaries

App runtime request-building uses narrow public request facades for guided workspaces:

- `src/lib/trigonometry/runtime-request.ts`
- `src/lib/statistics/runtime-request.ts`
- `src/lib/geometry/runtime-request.ts`

App runtime may use public workspace request, navigation, examples, mode facade, and core-mode seams. It must not import parser/runtime-input/serializer internals or workspace math-core internals directly.

## Algebra, Equation, Display, Modes, Symbolic, Engine

The earlier district/facade cleanup lane is current and should be preserved:

- Algebra, Equation, Display, Modes, Symbolic Engine, and Engine keep stable public root facades where they protect shared import APIs.
- Private implementation districts own the moved internals.
- OOE traffic-control internals are the exception: they moved without root stubs and are protected by direct import updates plus boundary validators.
- Display library policy is split into notation, result, and scheduling districts; DisplayPanel is app-shell component structure.
- App CSS is decomposed out of the old shell monolith.

Do not remove root facades casually. Retire a facade only after an audit proves it is not public or load-bearing.

## Playground And Source Mirrors

Playground remains an incubation/reference system, not product runtime authority.

- Stable `src/**` code must not import Playground or source mirrors.
- Source mirrors live under ignored local mirror paths and are context only.
- Production source and OOE events must not embed source-mirror paths.
- Research snapshots that must be preserved verbatim belong under `.memory/sources/` with index metadata.

## Current Verification Commands

Use these for boundary-sensitive work:

- `npx tsc -b --pretty false`
- `npm run test:compartments-boundaries`
- `npm run test:ooe-boundaries`
- `npm run report:compartments`
- `npm run test:file-sizes`
- `npm run test:memory-protocol`
- `git diff --check`

Use focused unit/UI suites for the compartment being changed, then run `npm run lint` and `npm run build` before final code commits that touch runtime, UI, validators, or shared libraries.

The recurring Node warning about `NO_COLOR` being ignored when `FORCE_COLOR` is set is non-fatal unless a command exits nonzero for another reason.

## Near-Term Guidance

Prefer product-facing or correctness work over more infrastructure. Good next lanes should come from real pressure:

- App shell component pressure.
- Navigation/Input/keypad/editor breadth.
- Table growth.
- Solver correctness, domain facts, and display fidelity.
- Future graphing only after a concrete product plan consumes validated solver/domain/branch state.

Stop if a proposed follow-up creates a broad bus, runtime registry, plugin layer, Surface Protocol, command authority, source generator, new execution authority, or graphing compartment without a concrete need.
