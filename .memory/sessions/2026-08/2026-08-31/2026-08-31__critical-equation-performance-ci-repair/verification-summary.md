# Verification Summary

## Attribution

- primary_agent: codex
- primary_agent_model: gpt-5.6
- primary_agent_family: sol
- contributors: none
- recorded_by_agent: codex
- recorded_by_agent_model: gpt-5.6
- recorded_by_agent_family: sol
- verified_by_agent: codex
- verified_by_agent_model: gpt-5.6
- verified_by_agent_family: sol
- attribution_basis: live

## Gate 1 Green Evidence

- Targeted Equation matrix: 32/32 passed across active typed-supplement routes, explicit transforms, domain evidence, stage carriers, and runtime V2 fail-closed coverage.
- Post-relocation selector delta: PRL4, COMP2, COMP4, and RAD2 all passed; RAD2 retained all four guarded conditions and COMP2 excluded the constant-base diagnostic from supplements.
- Focused runtime/AppMain delta: 35/35 backend tests passed after centralizing the non-frozen worker adapter, and the three selected AppMain regressions passed.
- `npm run test:result-contract`: 137/137 passed after final worker/History/golden request normalization; no compiled-equality fallback warning appeared.
- `npm run test:canonical-result-v2-enforcement`: passed for all 20 frozen producer files. MathJSON coverage remains `506/506/0/0`, and display inversion remains baseline-clean with zero compatibility projections and zero legacy reads.
- `npm run build`: passed under Node v24.19.0 and npm 11.17.0.
- `npm run test:file-sizes`: passed with 2,170 files and the five existing caps.
- `git diff --check`: passed.

## Real-App Playwright Evidence

- LCD transform: successful V2 answer card, `Use LCD` provenance, transform explanation, two visible exclusions (`x\ne0`, `x+1\ne0`), Copy Result, History replay, answer overflow, and page overflow passed.
- PRL4: `x=4`, `Same-Base Equality`, two right-facing conditions (`x+1>0`, `2x-3>0`), Copy Result, History replay, answer overflow, and page overflow passed.
- Screenshots are retained only under ignored `.task_tmp/ci-equation-transform-v2-authority1/`; the temporary Playwright spec was removed.
- The in-app browser runtime could not be initialized because its required JavaScript control tool was unavailable in this session. Repository Playwright CLI completed the required real-app inspection, so no visual acceptance risk remains.

## Performance Evidence Preserved for Gate 2

- In the targeted route matrix, PRL4 completed in about 0.34 seconds, COMP2 in about 1.21 seconds, COMP4 in about 0.49 seconds, and RAD2 in about 2.04 seconds.
- The outer logarithmic carrier still took about 44.56 seconds. This confirms the performance defect remains independent from Gate 1 and supplies the pre-repair reference for Gate 2.

## Diagnostic Notes

- The first browser LCD attempt used a stale pre-existing `dist` and correctly could not see the source repair. After the required build, the only retry issue was an ambiguous test locator; the final source-built LCD case passed.
- Local `NO_COLOR`/`FORCE_COLOR` warnings are harmless environment noise. No repository color configuration was added.
