# EQUATION-ALGEBRAIC-WRAPPER-FORMULA1 Completion Report

## Attribution

- primary_agent: codex
- primary_agent_model: gpt-5-codex
- contributors: []
- recorded_by_agent: codex
- recorded_by_agent_model: gpt-5-codex
- verified_by_agent: codex
- verified_by_agent_model: gpt-5-codex
- attribution_basis: live

## Gate

- Gate label: backend
- Scope: first live algebraic wrapper formula consumer for Real Exact one-layer square-root composition.

## Summary

Enabled Real Exact `sqrt(F(target)) = value` formula handoff for one-layer square-root wrappers when the generated equation safely delegates to existing Cardano or Ferrari selected-target formula routes. The feature is opt-in through the composition consumer only; default generated handoff still excludes formula families.

## Completed

- Added Real generated formula adapters for Cardano and Ferrari under Equation parameterized ownership.
- Let square-root composition opt into generated formula families only for Real Exact Equation mode and only for one-layer square-root carriers.
- Preserved structured Real `caseMath` output, global wrapper facts such as `b\ge0`, formula detail sections, and case-local conditions instead of flattening formula answers through `exactLatex`.
- Refined generated formula validation wording and evidence from lifting scoped facts globally to preserving scoped facts structurally.
- Added mode-level and parameterized tests for cubic/quartic square-root formula handoff, non-`x` targets, rational-cleared generated branches, degree caps, Complex unsupported behavior, and non-square-root wrapper boundaries.
- Amended the route planner so radical-denominator shapes such as `sqrt((z^4+z+1)/(z-m))=b` keep `composition` in the top-level plan; denominator-only shapes remain rational/formula-only.
- Added mode-level regressions for both `\frac{...}{...}` and slash-style `(...)/(...)` square-root rational Cardano/Ferrari handoffs.
- Fixed the CI assertion drift in `parameterized-families.test.ts`: the raw adjacent-product mixed-carrier example now expects the current selected-target-island stop wording.

## Out Of Scope Preserved

- No Complex Exact square-root wrapper formula route.
- No absolute value, square-power, nested composition, mixed algebraic wrappers, carrier-elimination formula handoff, exp/log wrappers, or trig wrappers.
- No generated/wrapper formula route widening outside the Real square-root composition consumer.
- No Cardano/Ferrari formula rewrite.
- No Display, History, OOE, app-state, Tauri, or persisted schema change.
- No `RootOf`, implicit-root output, or numeric-only Exact fallback.

## Durable Memory Updated

- `.memory/current-state.md`
- `.memory/decisions.md`
- `.memory/journal/2026-06/2026-06-25.md`
- `.memory/sessions/2026-06/2026-06-25/2026-06-25__equation-algebraic-wrapper-formula1/`

## Commit Status

Committed and then amended with the user-reported rational square-root wrapper fix.

## Next Discussion Focus

Choose the next wrapper family after Real square-root formula handoff. Recommended candidates are absolute-value or square-power algebraic wrappers only after a separate validation/fact policy decision; Complex square-root wrappers need principal complex square-root back-substitution policy first.
