## Manual Checklist

- Gate: ui.

## What Is Achieved Now

- Exact symbolic branch-family routes remain ahead of bounded Complex Region solving for trig, logarithmic, and exponential infinite-family carriers.
- Bounded Complex Region results for infinite-family expressions explicitly say they enumerate verified roots only inside the selected rectangle.
- Benchmark evidence distinguishes `symbolic-family` from `bounded-region` instead of treating both as the same support claim.

## Manual App Steps

- Open Equation > Symbolic, turn Complex On, enable Complex Region.
- Run `e^x+x=0` over `[-1,1] x [-1,1]` with grid `7`.
- Inspect the answer card and the `Complex Infinite-Family Policy` and `Complex Contour Verification` cards.

## Expected Results

- The answer is labeled as a region-local complex root.
- The policy card says exact Complex branch-family routes are tried first and that bounded Complex Region output is not a global solution set for infinite families.
- The contour verification card reports one verified root in the selected region.
