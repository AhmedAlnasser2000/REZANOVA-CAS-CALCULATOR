# EQUATION-PRODUCTION-PATH-PERFORMANCE-CLOSEOUT1 Completion Report

## Attribution

- primary_agent: codex
- primary_agent_model: gpt-5.6
- primary_agent_family: sol
- contributors: none
- recorded_by_agent: codex
- recorded_by_agent_model: gpt-5.6
- recorded_by_agent_family: sol
- verified_by_agent: codex
- verified_by_agent_model: gpt-5.6
- verified_by_agent_family: sol
- attribution_basis: live

## Gates

- Checkpoint A: backend; verified.
- Checkpoint B: backend; verified.
- Real-app output: ui; verified.
- Combined selective commit: pending explicit user approval.

## Checkpoint A - Shared Formal-Proof Correctness

- Deterministic formal normalization recognizes `ImaginaryUnit`, `i`, and `\imaginaryI`, including normalized signed unit products inside equality and set trees.
- Exact producer serialization or deterministic formal equality may pass. When deterministic comparison applies, its mismatch cannot be overridden by Compute Engine `isSame()`.
- Correct aliases, nested formal functions, subscripts, matrices, radicals, and existing equivalent forms remain accepted; opposite signed-imaginary trees fail closed.
- Failure results remain uncached, and provenance, standard-operator, clone-safety, bounds, and final V2 validation remain enforced.

## Checkpoint B - Production-Path Performance

- Public runtime-adapter tests now own all V2 route finalization coverage and statically forbid imports of the frozen low-level runner or manual finalizer calls.
- Canonical printer serialization reuses the already validated boxed expression through a private prepared-printer seam; public printer and Canonical Result interfaces are unchanged.
- Shared candidate validation and composition validation parse each residual once per batch and prepare each distinct constraint expression once for candidate reuse.
- Existing tolerances, candidate order, angle-unit handling, domain/rejection wording, route order, solver recursion, worker topology, OOE behavior, and request isolation are unchanged.
- No request-wide solver AST cache, GPU/WASM path, or worker-topology change was introduced.

## Boundary

- The immutable 57-route V1 inventory, MathJSON exemptions, proof/print/display baselines, public schemas, and frozen producer files remain unchanged.
- Calculate MathLive-root and Node 24/Actions work remain outside this commit.
- No push is authorized.
