# Structured History And Display Contract Manual Verification

## Attribution

- primary_agent: codex
- primary_agent_model: gpt-5.6
- primary_agent_family: sol
- recorded_by_agent: codex
- recorded_by_agent_model: gpt-5.6
- recorded_by_agent_family: sol
- verified_by_agent: codex
- verified_by_agent_model: gpt-5.6
- verified_by_agent_family: sol
- attribution_basis: live

## What Is Achieved Now

- Every live success and math-bearing controlled error uses producer-owned canonical result truth.
- Display, Clipboard, editor transfer, `Ans`, workspace state, structured History, print hygiene, replay comparison, and Surface mapping read canonical truth before compatibility fields.
- New successful History rows store bounded structured results plus legacy projections; old rows remain loadable without reparsing LaTeX.
- Browser and native persistence retain versioned extensions, exact Table rows, launch-time replay settings, and session rows after a durability failure.

## Manual App Steps

1. Run one successful result in each computational workspace, open its details, copy it, and create a History row.
2. Reload the browser app, then replay each row and confirm its original workspace, card title, answer, branches/details, warnings, and Table rows.
3. Change Roots/Powers presentation after creating a History row and confirm replay uses the current visible preference without changing stored canonical copy text.
4. Trigger a math-bearing Matrix or Vector dimension stop and confirm the input math and limit message remain readable.
5. In a native build, create a History row, restart the app, and confirm the row and Calculator Memory survive. Copy one result and paste its exact canonical text back into an editor.

## Expected Results

- No mathematical wording, exact value, card identity, detail cardinality, or Table data changes across live Display and History replay.
- Clipboard and editor transfer preserve canonical semantics even when visible root/power presentation changes.
- Legacy rows remain readable, executable actions are not restored from History, and persistence failure warns without deleting the in-session row.
- Compatibility fields still exist as derived projections; this checklist does not authorize their removal or a push.
