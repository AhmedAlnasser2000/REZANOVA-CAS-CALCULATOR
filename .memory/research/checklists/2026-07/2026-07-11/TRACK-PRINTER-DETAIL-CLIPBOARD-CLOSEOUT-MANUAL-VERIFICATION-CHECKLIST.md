# Printer, Detail, And Clipboard Manual Acceptance

## Attribution

- primary_agent: codex
- primary_agent_model: gpt-5.6
- primary_agent_family: sol
- recorded_by_agent: codex
- recorded_by_agent_model: gpt-5.6
- recorded_by_agent_family: sol
- verified_by_agent: codex
- verified_by_agent_model: gpt-5.6
- verified_by_agent_family: sol
- attribution_basis: live

## What Is Achieved Now

- All 519 governed result paths use an explicit printer profile or forward an owned result; compatibility fallback is zero.
- All 445 live detail producers declare mathematical or prose intent; renderer inference remains only for legacy History.
- Browser and Tauri Clipboard paths preserve canonical LaTeX fallback, and all production copy/paste surfaces use shared adapters.
- All 100 replay fixtures hard-compare normalized LaTeX, with no accepted visible printer drift.

## Manual App Steps

1. Run `arcsin(1)` in DEG and RAD and confirm `90` then `\pi/2`.
2. Simplify `(\sqrt{x})^{1/3}`, copy the result, clear the editor, and paste; confirm the exact expression remains usable while Roots/Powers changes only visible result presentation.
3. Inspect one result and expanded detail card in each of the nine computational workspaces.
4. Open Formula Viewer for a dense Equation result and inspect initial, expanded, and scrolled content.
5. Create and replay one History row per workspace; confirm legacy saved strings remain readable and no action or detail is malformed.

## Expected Results

- No mathematical wording or canonical output changes from the pre-program baseline.
- Typed detail math renders as math, declared prose remains prose, and no card overlaps or clips.
- Clipboard copy/paste retains canonical semantics in browser and Tauri fallback paths.
- Accepting this checklist permits the separate structured-History and Display-inversion roadmap; it does not authorize a push.
