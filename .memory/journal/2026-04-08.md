# 2026-04-08

- Implemented `POLY2` as the bounded exact cubic/quartic factor-and-solve milestone.
- Added `src/lib/polynomial-factor-solve.ts` with denominator clearing, rational-root search, exact linear division, quartic biquadratic handling, and bounded quartic factor-into-quadratics.
- Reused the same bounded engine across guided `Equation > Polynomial`, free-form `Equation > Symbolic`, and `Calculate > Factor` while preserving existing numeric fallback and unsupported behavior for out-of-scope cubic/quartic families.
- Added focused regression coverage for exact cubic/quartic solve, bounded factorization reuse, and direct `math-engine` factor output.
- Verified with:
  - `npm run test:unit -- src/lib/math-engine.test.ts src/lib/polynomial-factor-solve.test.ts src/lib/symbolic-engine/factoring.test.ts src/lib/symbolic-engine/orchestrator.test.ts src/lib/modes/equation.test.ts src/lib/equation/guarded-solve.test.ts`
  - `npm run test:gate`
- Locked an architecture direction during POLY2 close-out: shared reusable math cores should be preferred over duplicated feature-local logic when the same bounded algebra surface is needed across multiple engines or solver paths.
- Implemented `RAD1` as the shared radical-core and bounded visible-normalization milestone.
- Added `src/lib/radical-core.ts` to centralize supported-radicand recognition, exact radical condition generation, perfect-square quadratic detection through the shared polynomial core, and bounded one-step conjugate/rationalization eligibility.
- Refactored `src/lib/symbolic-engine/radical.ts` and `src/lib/equation/guarded/algebra-stage.ts` to consume the shared radical core, while keeping Equation solve preprocess conservative and widening visible normalization only in read-only simplify/factor surfaces.
- Added visible exact normalization wins for perfect-square quadratic radicands and bounded two-radical denominator rationalization, plus a guarded-supplement dedupe fix so repeated denominator restrictions no longer show twice as both exclusions and conditions.
- Verified with:
  - `npm run test:unit -- src/lib/modes/equation.test.ts src/lib/symbolic-engine/radical.test.ts src/lib/math-engine.test.ts src/lib/equation/shared-solve.test.ts`
  - `npm run lint`
  - `npm run test:gate`
- Implemented `RAD2` as the bounded sequential radical-isolation milestone for `Equation > Symbolic`.
- Extended guarded algebra with a two-step radical transform budget, bounded abs follow-on only from exact radical steps like `\sqrt{(u)^2}`, and cleaner merged condition/exclusion supplements across sequential radical recursion.
- Added exact coverage for broader radical families including `\sqrt{x+1}+\sqrt{x}=3`, `\sqrt{x+1}=\sqrt{2x-1}+1`, `\sqrt{x+\sqrt{x}}=3`, and bounded radical-to-polynomial / radical-to-abs follow-ons with original-equation candidate rejection preserved.
- Tightened UI and Playwright assertions to follow the new supplement/exclusion rendering model and raw-latex exact rendering where MathLive aria labels were lossy.
- Verified with:
  - `npm run test:gate`
- Implemented `POLY-RAD1` as the bounded exact polynomialized-radical bridge across Equation, Factor, and direct radical Simplify wins.
- Extended `src/lib/polynomial-factor-solve.ts` so bounded biquadratic follow-ons can synthesize exact real roots from algebraic `x^2-α` factors and preserve repeated biquadratic factor multiplicity.
- Extended `src/lib/radical-core.ts` so direct radical normalization can recognize repeated quadratic-in-even-power perfect squares such as `\sqrt{x^4-10x^2+25}` and reuse the shared polynomial factorization instead of radical-local polynomial logic.
- Added visible coverage for:
  - `\sqrt{x^4-5x^2+4}=1`
  - a sequential radical family that polynomializes into a bounded biquadratic exact follow-on
  - algebraic biquadratic factorization like `x^4-5x^2+3`
  - direct radical simplify wins like `\sqrt{x^4-10x^2+25}\to|x^2-5|`
- Verified with:
  - `npm run test:unit -- src/lib/polynomial-factor-solve.test.ts src/lib/math-engine.test.ts src/lib/equation/shared-solve.test.ts src/AppMain.ui.test.tsx`
  - `npm run test:ui -- --run src/AppMain.ui.test.tsx`
  - `npm run build`
  - `npx playwright test e2e/qa1-smoke.spec.ts -g "POLY-RAD1"`
  - `npm run test:gate`
- Captured the kernel/microkernel/pillars architecture discussion in detail in `.memory/research/kernel-architecture-notes.md` using both the repo's current code shape and the external `calcwiz_kernel_architecture.zip` pack.
- Preserved the important distinctions explicitly:
  - kernel-centered modular monolith first
  - microkernel/plugin-style architecture later and only when justified
  - internal extensibility now, external extensibility later
  - Rust-authoritative runtime as a destination, not an immediate forced rewrite
- Recorded the architecture direction as a durable decision and added the remaining timing questions about whether `ARCH1` should come before the next major algebra/composition/abs milestone.
