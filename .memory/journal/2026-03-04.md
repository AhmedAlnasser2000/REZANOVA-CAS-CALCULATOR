- Migrated Geometry 3D solids and triangle tools into the shared top-editor Geometry core, removing the last preview-only Geometry screens and deleting the old screen-based Geometry execution bridge.
- Extended Geometry parsing, serialization, and evaluation to support cube, cuboid, cylinder, cone, sphere, triangle area, and Heron requests in both structured and screen-aware shorthand flows.
- Added optional `geometryScreen` history metadata so Geometry shorthand entries replay back into the correct tool instead of relying on brittle string matching.
- Validation passed with `npm test`, `npm run build`, `npm run lint`, and `cargo check`.
- Migrated Trigonometry onto the shared core-mode contract with one top trig draft/editor, explicit `Send to Calc` / `Send to Equation` actions, and no remaining implicit `Calculate` fallback from trig editing.
- Added trig draft parsing, serialization, shared-core execution, and `trigScreen` history metadata so trig replay restores the correct screen and guided state without string heuristics.
- Rebuilt the trig workspace around the shared top editor, kept guided numeric builders below it, removed the old trig screen-dispatch engine, and added parser/core/navigation tests for the new flow.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Migrated Statistics onto the shared core-mode contract with one top Statistics draft/editor, parser/core/serializer modules, real probability/regression/correlation engines, and `statisticsScreen` history metadata for replay.
- Reworked Statistics UX around explicit dataset vs frequency-table sources, manual frequency-table editing, dataset/table import-expansion actions, and guided point-set builders for regression and correlation.
- Added a narrow Calculate-mode trig-angle fix that rewrites direct numeric trig arguments by angle unit without using the global Compute Engine angular-unit switch, so `sin(90)` follows DEG/GRAD while explicit radian input like `sin(pi/2)` stays intact.
- Validation passed with `npm test -- --run`, `npm run build`, `npm run lint`, and `cargo check`.
- Polished shared dark-panel button contrast across the app by strengthening guide chips, quick-template buttons, top-strip controls, workspace action buttons, and statistics row actions while keeping light display-panel actions readable through scoped overrides.
- Polished Statistics, Trigonometry, and Geometry workbenches with tighter card spacing, stronger section hierarchy, table/point column labels, and subtler per-core card tinting; also added a shared accessibility-style control sweep for hover, active, focus-visible, and disabled states across app buttons and form fields.
- Validation passed with `npm run build` and `npm run lint`.
- Implemented Memory V2 and the repo workflow overhaul: durable `.memory` docs are now meant to be tracked in git, `.task_tmp/` is the ignored task/gate workspace, `docs/checkpoints/` holds verified app-state summaries, and PowerShell helpers now support task-session tracking plus pre-commit/pre-push planning.
- Validation passed with PowerShell smoke tests for `tools/dev/task_session.ps1` and `tools/dev/git_plan.ps1`, plus `npm run build` and `npm run lint`.
- Excluded `.tmp/` from the tracked repo baseline so temporary media captures stay out of the initial commit and future git status output.
