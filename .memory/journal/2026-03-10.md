- Verified and completed Exact Algebra Core `R4`:
  - added shared bounded algebra transform actions (`Combine Fractions`, `Cancel Factors`, `Use LCD`, `Rationalize`, `Conjugate`)
  - wired `F4 Algebra` in `Calculate` and `Equation > Symbolic` with inline transform trays
  - separated transform badges/summaries from solve badges/summaries in the result card
  - expanded unit, UI, and browser smoke coverage for the new transform flows
- Full automation gate passed:
  - `npm run test:gate`
- Patched the `R4` algebra tray contrast on the light display panel so transform chips use dark readable text and stronger borders/backgrounds inside the result area.
- Rechecked the shared result UI coverage:
  - `npm run test:ui -- --run src/AppMain.ui.test.tsx`
