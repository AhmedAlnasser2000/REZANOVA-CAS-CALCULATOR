# 2026-06-26

- [agent: codex | model: gpt-5.5] Follow-up for `EXP-LOG-WRAPPER-FORMULA1`: Real Exact exp/log formula handoff now remains eligible when the selected target is also in a denominator, so rational log carriers such as `\ln((z^3+z+1)/(z-m))=b` can reach generated Cardano with the `z-m\ne0` exclusion preserved. The same checkpoint stabilizes the AppMain variable-hint UI test by waiting for the fresh editor-analysis hint instead of reading a stale previous hint.
- [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-PIPELINE2` fixes the sluggish large-formula display path without solver or schema changes. Heavy Real formula `caseMath` answers now render a compact responsiveness summary first and mount full formula rows plus row-local `when` guards only after the user chooses `Show full formula cases`; global `Valid When` facts still render normally. Collapsed math-heavy `mixed` detail sections now lazy-mount while cheap prose-only details remain eager.
- [agent: codex | model: gpt-5.5] Follow-up for `FORMULA-PRESENTATION-PIPELINE2`: user QA found that direct specialized Cardano output such as `x^3+p*x^2*q+x=1` could still freeze because it had only four rows but very large substituted formulas. The caseMath size policy now also compacts visually dense four-row answers by total/per-row LaTeX weight, while compact direct cases such as `x^3+p*x+2=0` remain fully visible.
- [agent: codex | model: gpt-5.5] `EQUATION-TRIG-WRAPPER-FORMULA-POLICY0` keeps trig Cardano/Ferrari generated formula handoff docs-only for now. The audit confirms existing trig composition can generate periodic branch equations with range and integer facts, but a live formula route must first preserve periodic branch provenance, row-local Cardano/Ferrari guards, endpoint dedupe for sine/cosine, angle-unit semantics, and original-equation candidate validation. Recommended first live scope is Real Exact one-layer `sin(F)=rhs`, `cos(F)=rhs`, and `tan(F)=rhs`, with `tan` as the fallback first slice if sine/cosine endpoint evidence is unstable.
- [agent: codex | model: gpt-5.5] `EQUATION-TRIG-WRAPPER-FORMULA1` makes Real Exact one-layer trig generated formula handoff live for existing `sin`, `cos`, and `tan` composition carriers. Generated direct or safely rational-cleared degree-3/4 equations may delegate to Real Cardano/Ferrari while preserving periodic branch provenance, integer-parameter and range facts, row-local case guards, denominator exclusions, and endpoint dedupe for exact sine/cosine overlaps. Complex trig wrappers, nested/mixed trig wrappers, and broad generated route-order widening remain deferred.
- [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-RENDER-SCHEDULER-ROADMAP0` creates a dedicated roadmap for heavy formula rendering. Current wrapper widening is deliberately paused until Display can expand huge `caseMath` answers progressively with cancellation, row budgets, and stronger math-detail lazy mounting; existing live algebraic, exp/log, and trig wrappers remain supported.
- [agent: codex | model: gpt-5.5] `FORMULA-PRESENTATION-PIPELINE3` makes heavy `caseMath` compact summaries metadata-only. Formula-case compact previews now show counts, generated-branch counts, total character count, and the row-local-condition note without carrying a raw LaTeX snippet or mounting formula-row math before expansion. Non-case large-result previews retain their existing snippet behavior.
- [agent: codex | model: gpt-5.5] `DISPLAY-CASE-ROW-SCHEDULER1` adds progressive row rendering for expanded heavy formula `caseMath` answers. Compact/heavy cases still require explicit expansion; after expansion, rows reveal in small frame-scheduled batches with deferred row math, progress feedback, pending-row placeholders, and stale-render cancellation on result replacement or unmount. Small direct formula cases keep immediate rendering.
- [agent: codex | model: gpt-5.5] `DISPLAY-CASE-ROW-BUDGETS1` fixes the remaining expanded heavy-formula freeze by adding row-level render budgets. Expanded heavy `caseMath` answers still reveal rows progressively, but over-budget individual rows now show opt-in placeholders with no formula-row MathStatic, condition MathStatic, raw LaTeX preview, or group-label math mounted until `Show formula row`; deferred MathStatic now skips symbolic-display normalization before its placeholder. This is Display/UI only and leaves solver behavior, formula semantics, Copy Result, To Editor, History, OOE, app-state, Tauri, and persisted schemas unchanged.
- [agent: codex | model: gpt-5.5] Follow-up for `DISPLAY-CASE-ROW-BUDGETS1`: formula-case detail cards now use the same budgeted `caseMath` renderer as answer blocks, so opening `Trig Formula Cases` no longer mounts all heavy formulas at once. Replayed/history raw case expressions with `\substack` are parsed into structured case rows where possible instead of being displayed as raw `\substack` text.
- [agent: codex | model: gpt-5.5] `INTEGRATION-FORM-CLASSIFIER1` adds a behavior-preserving internal classifier before Rubi work. Symbolic integration now profiles the integrand MathJSON once, builds a prioritized route plan, and dispatches through existing integration families while preserving current visible strategies, metadata, and verification behavior.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-AUDIT0` registers the IntegrationRules mirror as static-only Rubi source context and audits Section 1 Algebraic functions against current Calcwiz integration coverage. The recommended first implementation slice is bounded positive-integer polynomial/binomial/trinomial expansion into the existing direct polynomial integrator with original-integrand backcheck; Rubi recurrences, fractional powers, normalization, branch-sensitive transforms, public metadata, and lazy tier imports remain deferred.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-POLY-EXPAND1` makes the first Rubi Section 1 implementation live as bounded algebraic expansion into the existing direct-rule integration family. Products and nonnegative integer polynomial powers, including target-free symbolic coefficients, can expand through the Symbolic Primitives expansion helper, integrate as direct polynomials, and verify exactly against the original compact integrand; overlap precedence, public strategy metadata, Display, History, OOE, Tauri, workspace contracts, Rubi recurrence rules, and branch-sensitive transforms remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-POLY-DEGREE-LIFT1` widens the already-live expanded-direct slice by lifting integration-local expansion and exact-backcheck caps enough for higher-degree monomials produced by bounded positive-integer polynomial powers. New cases remain adopted only through exact original-integrand verification and visible `direct-rule`; branch-sensitive, root, division, negative/fractional-power, public Rubi metadata, and recurrence machinery remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-POLY-BYPARTS-FEEDER1` adds an internal expanded-polynomial feeder before the existing integration-by-parts fallback. Normal by-parts still runs first; bounded polynomial-like factors may expand only as fallback, and successful feeder results keep visible `integration-by-parts` after exact backcheck against the original compact integrand. Public Calculus strategy/result metadata, Display, History, OOE, Tauri, workspace contracts, Rubi recurrence machinery, and branch-sensitive transforms remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-SECTION1-QUADRATIC-RECIPROCAL-POWER1` adds a narrow repeated quadratic reciprocal case for `1/(c+u^2)^2` where `c` is a positive exact square scalar and `u` is an exact affine form. It reports visible `partial-fractions`, preserves inverse-trig precedence for power `1` and substitution precedence for derivative-numerator overlaps, and adds exact rational-function equivalence to antiderivative backcheck so adoption does not depend on numeric confidence. Symbolic coefficients, higher powers, broad recurrence, branch-sensitive forms, public Rubi metadata, Display, History, OOE, Tauri, and workspace contracts remain unchanged.
- [agent: codex | model: gpt-5.5] `FORMULA-VIEWER-TAB-FOUNDATION1` adds a session-only in-app Formula Viewer tab surface for monstrous structured `caseMath` answers. Heavy source cards can now stay compact and open a viewer artifact built from Display blocks; viewer tabs are not calculator `ModeId`s, do not launch jobs, and reuse the existing app shell/OOE authority while preserving Copy Result, History replay, source facts, and row-local formula guards. Wrapper widening remains paused until this viewer lane is stable enough for the next virtualization slice.
- [agent: codex | model: gpt-5.5] `FORMULA-VIEWER-VIRTUALIZATION1` adds viewer-only virtualization for huge Formula Viewer artifacts. The viewer now builds a virtual block/row item list from structured Display blocks, renders only viewport-plus-overscan items inside `.formula-viewer-scroll`, keeps collapsed detail bodies unmounted, preserves row-budget placeholders for giant rows, measures visible items safely, and resets row/measurement state when the result signature changes. Source Equation cards stay compact, and solver behavior, formula semantics, Copy Result, To Editor, History, OOE, app-state, Tauri, and persisted schemas remain unchanged.
- [agent: codex | model: gpt-5.5] Follow-up note for `FORMULA-VIEWER-VIRTUALIZATION1`: the display roadmap remains open. User QA confirms virtualization helps the DOM/render freeze, but huge formulas can still be visually too small or dense in the viewer; the next Display follow-up should address inspectable row sizing/zoom/focus without changing solver semantics.
- [agent: codex | model: gpt-5.5] `CALCULUS-INTEGRALS-EDITOR-SOURCE1` moves Indefinite, Definite, and Improper Integral body editing into the main editor. The lower Calculus workspace now keeps only integral controls plus the generated request preview, F2 is labeled `Focus Editor` for integral screens, Copy Expr keeps copying the generated integral request, and structured `Answer` remains the only rendered result answer.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-AFFINE-POWER1` adds exact-rational affine power support for `(m*x+n)^p` inside symbolic integration. Bounded integer powers, logarithmic `p=-1`, and selected negative integer powers now backcheck exactly, including `ln|u|` derivative support; public Calculus strategy/result metadata, Display, History, OOE, Tauri, workspace contracts, symbolic coefficients, and branch-sensitive widening remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-BINOMIAL-DERIVATIVE-SUB1` adds an exact-rational fallback for derivative-present binomial substitutions such as `C*x^(n-1)*(a+b*x^n)^p`. Successful cases keep visible `u-substitution` and require exact original-integrand backcheck; branch-sensitive forms, symbolic coefficient widening, broad recurrence machinery, public Rubi metadata, Display, History, OOE, Tauri, workspace contracts, and public Calculus schemas remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-REPEATED-LINEAR-RATIONAL1` strengthens repeated exact linear rational integration. High single affine reciprocals such as `1/(2x+3)^9` now stay visible as `partial-fractions`, bounded mixed repeated-linear products keep using the rational-function core, and over-cap mixed products remain controlled unsupported; Display, History, OOE, Tauri, workspace contracts, public schemas, symbolic coefficient widening, and broad factorization remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-RECIPROCAL-NUMERATOR1` adds exact-rational affine numerator support for repeated quadratic reciprocal denominators such as `(x+1)/(1+x^2)^2` and `(2x+3)/(4+x^2)^2`. Mixed and constant numerator cases stay visible as `partial-fractions`, pure derivative numerator overlaps still resolve as `u-substitution`, and non-square constants, higher powers, symbolic coefficients, broad recurrence, public metadata/schema changes, and UI/runtime surfaces remain unchanged.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-QUADRATIC-RECIPROCAL-POWER-LIFT1` lifts constant repeated quadratic reciprocal integration to powers `3` and `4` for exact affine `u` and positive exact-square `c`. The formulas use bounded recurrence terms, keep visible `partial-fractions`, preserve derivative-numerator substitution overlaps, and leave power `5+`, non-square constants, symbolic coefficients, broad recurrence, public metadata/schema changes, and UI/runtime surfaces deferred.
- [agent: codex | model: gpt-5.5] `RUBI-TIER1-RATIONAL-QUADRATIC-MIXED1` verifies the existing rational-function core as the Tier-1 implementation for bounded exact-rational partial fractions with repeated/ordinary linear factors plus one irreducible quadratic factor. The milestone adds regression coverage for the headline mixed families and the multiple-quadratic stop; runtime behavior, public metadata/schema, Display, History, OOE, Tauri, workspace contracts, symbolic coefficients, and broad factorization remain unchanged.
