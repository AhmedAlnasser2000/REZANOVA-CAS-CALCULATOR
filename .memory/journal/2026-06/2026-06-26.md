# 2026-06-26

- [agent: codex | model: gpt-5-codex] Follow-up for `EXP-LOG-WRAPPER-FORMULA1`: Real Exact exp/log formula handoff now remains eligible when the selected target is also in a denominator, so rational log carriers such as `\ln((z^3+z+1)/(z-m))=b` can reach generated Cardano with the `z-m\ne0` exclusion preserved. The same checkpoint stabilizes the AppMain variable-hint UI test by waiting for the fresh editor-analysis hint instead of reading a stale previous hint.
