# 2026-06-17

## WORKSPACE-TABS-SURFACE-AUDIT0

- [agent: codex | model: gpt-5.5] Added a docs-only Supercarrier audit for future workspace tabs as session-scoped workspace instances inside one app shell.
- Recorded that tabs are not multiple `AppMain` copies, projects/files, separate OOE authorities, a Supercarrier runtime, a broad bus, or Surface Protocol.
- Mapped the current singleton `currentMode`, global Display/History, app-runtime hooks, and OOE traffic-control model to a future `workspaceInstanceId` model.
- Captured the agreed UX posture: browser-style tabs, `+` defaulting to blank Calculate for V1, mode-launcher context commands, mutable tab titles as runtime labels only, and committed History staying workspace-based.
- Captured the close policy: idle tabs close by dropping session instance state, active-job tabs must cancel or stale-drop scoped work before removal, and closing a tab never deletes global History.
- Deferred projects/files, `My Work`, Graphing, Spreadsheet persistence, multi-window behavior, broad bus work, Surface Protocol, and tab implementation to later explicit milestones.

## MEMORY-CURRENT-STATE-DAILY-GATE1

- [agent: codex | model: gpt-5.5] Enforced daily `.memory/current-state.md` catch-up through `npm run test:memory-protocol`.
- Updated `AGENTS.md` and `.memory/PROTOCOL.md` to state that current-state must be at least as new as the latest journal/session day.
- Updated the memory protocol validator and tests so a stale `Last updated` date fails before commit.
- Refreshed `.memory/current-state.md` to 2026-06-17 and recorded the current workspace-tabs planning posture without turning it into implementation.

## WORKSPACE-TABS-ROADMAP0

- [agent: codex | model: gpt-5.5] Added a planning roadmap for browser-style workspace tabs as session-scoped workspace instances.
- Recorded the intended build order: `WORKSPACE-INSTANCE-MODEL1`, `WORKSPACE-STATE-HOST1`, `OOE-WORKSPACE-INSTANCE-SCOPE1`, `WORKSPACE-TABS-SHELL1`, `WORKSPACE-MODE-LAUNCHER-TABS1`, and `WORKSPACE-TABS-HISTORY-PENDING-LABELS1`.
- Preserved the hard boundaries from the tabs audit: one app shell, one OOE authority, active-only rendering by default, committed History stays workspace-based, tab titles are mutable labels only, and projects/files/Graphing/Spreadsheet/bus/Surface Protocol are deferred.
- Updated the research index and current-state snapshot to point to the new roadmap without starting implementation.

## WORKSPACE-INSTANCE-MODEL1

- [agent: codex | model: gpt-5.5] Added the first internal workspace-instance model under app runtime without visible browser tabs.
- Added stable session workspace ids, workspace kinds, mutable titles, compartment metadata, ordering timestamps, and empty future `surfaceState` / `displayState` / `runtimeState` slots.
- Added a `useWorkspaceInstancesRuntime` hook that shadows the singleton `currentMode` and lets existing `setMode` activate/focus the matching session instance.
- Kept `currentMode` as the live behavior source; no History schema, OOE job identity, diagnostics, persistence, visible tab UI, projects/files, Graphing, Spreadsheet, bus, or Surface Protocol changes.

## WORKSPACE-STATE-HOST1

- [agent: codex | model: gpt-5.5] Added the first active-only workspace state host behind the invisible workspace-instance model.
- Added `useWorkspaceStateHostRuntime` plus workspace-instance surface-state slot updates so outgoing core workspace state is captured before focus changes and incoming saved state restores into the singleton runtime hooks.
- Added Calculate, Equation, and Calculus capture/restore adapters for surface state only.
- Kept the app visually unchanged: no visible tabs, no OOE `workspaceInstanceId`, no per-instance Display/History, no persistence/schema changes, no projects/files, no Graphing/Spreadsheet, no bus, and no Surface Protocol.

## OOE-WORKSPACE-INSTANCE-SCOPE1

- [agent: codex | model: gpt-5.5] Added optional workspace-instance identity to OOE job traffic ahead of visible workspace tabs.
- Added shared `WorkspaceInstanceId` / `WorkspaceInstanceRuntimeContext` types and re-exported them from the app-runtime workspace-instance model.
- Threaded launch-time workspace-instance context from AppMain through History ticket reservations into Calculate, Equation, Calculus, Table, Trigonometry, Statistics, Geometry, Matrix, and Vector OOE-backed launches.
- Extended OOE job identity, commit assessment, active/recent job records, diagnostics records, lifecycle events, runtime shell launch-ticket evidence, and diagnostics inspector rows with optional workspace-instance metadata.
- Made OOE commit legality stale-drop jobs whose launch instance is closed or missing while preserving the existing input-revision gate for open instances and old behavior for jobs without instance metadata.
- Kept final committed History workspace-based and global; mutable tab labels remain launch-time diagnostics/pending labels only.
- Kept the app visually unchanged: no visible tabs, no per-instance Display/History, no persistence/schema changes, no projects/files, no Graphing/Spreadsheet, no broad bus, no Surface Protocol, and no new OOE authority.

## WORKSPACE-STATE-HOST-EXPANSION1

- [agent: codex | model: gpt-5.5] Expanded invisible workspace-instance surface-state hosting from Calculate/Equation/Calculus to all current runtime workspaces.
- Renamed the private core state-host wrapper to the general `workspace-surface-state` / `useWorkspaceSurfaceStateHostRuntime` layer.
- Added capture/restore adapters for Trigonometry, Statistics, Geometry, Table, Matrix, and Vector while preserving the existing core adapters.
- Kept Matrix and Vector as separate surface snapshots even though they share the linear-algebra runtime hook.
- Kept Table as a current workspace surface only; spreadsheet/file-style persistence remains deferred.
- Kept Display, committed History, persistence, OOE behavior, global settings, Guide, Labs, visible tabs, projects/files, Graphing, Spreadsheet, broad bus work, Surface Protocol, and multiple mounted workspace trees out of scope.

## WORKSPACE-TABS-SHELL1

- [agent: codex | model: gpt-5.5] Added the first visible browser-style workspace tab strip above the mode strip.
- Added private `WorkspaceTabs` app-shell UI with focus, plus-created blank Calculate tabs, rename, duplicate, close, close others, clear tab state, and stop jobs in tab actions.
- Routed tab switching through the existing workspace state host so only one workspace surface renders at a time and inactive tabs preserve hosted surface state without hidden mounted workspace trees.
- Added app-runtime tab job helpers for active-job and pending-ticket summaries by `workspaceInstanceId`, plus cancellation on confirmed tab close without letting shell UI import OOE internals.
- Added temporary pending-ticket workspace-instance metadata for UI disambiguation only; committed History remains global and workspace-based.
- Kept mode-launcher right-click tab commands, default-new-tab settings, per-instance Display, per-instance committed History, projects/files, Graphing, Spreadsheet, broad bus work, Surface Protocol, and multiple mounted workspace trees out of scope.

## WORKSPACE-DISPLAY-STATE-HOST1

- [agent: codex | model: gpt-5.5] Added workspace-instance scoped Display state for visible results, `Ans`, and replay display/substitution fragments.
- Added a display-state host so tab switches capture the outgoing instance's display state and restore the incoming instance's saved display state.
- Extended `useHistoryDisplayRuntime` so committed results update the origin workspace instance: active-origin commits update visible Display as before, while inactive-origin commits update only that instance's saved Display/Ans state.
- Wired tab focus, duplicate, close, close-others, and clear-tab-state through the display host; duplicate tabs copy display state and clear-tab-state clears both surface and display fragments.
- Added AppMain regression coverage proving Calculate and Equation tab results do not leak into each other.
- Kept committed History global and schema-stable; no projects/files, saved tab sessions, mode-launcher context menu, Graphing, Spreadsheet, broad bus, Surface Protocol, runtime registry, plugin layer, or multi-window behavior.

## WORKSPACE-TABS-HISTORY-PENDING-LABELS1

- [agent: codex | model: gpt-5.5] Added launch-time tab labels to pending/running/stopping History rows so simultaneous tab jobs are distinguishable.
- Kept committed History rows normal and workspace-based; finalized rows do not render or persist tab titles, `workspaceInstanceId`, or mutable labels.
- Preserved launch-time semantics: tab renames during a running job do not rewrite the pending row label.
- Kept tab close/cancel behavior on the existing workspace-tab path: confirmed close cancels matching active work, clears matching pending tickets, and relies on OOE instance-aware stale-drop for late results.
- Deferred mode-launcher tab commands, default-new-tab settings, projects/files, saved tab sessions, Graphing, Spreadsheet, broad bus, Surface Protocol, runtime registry, plugin layer, and multi-window behavior.
