# Journal

## Historical Attribution
- primary_agent: codex
- primary_agent_model: gpt-5.5
- attribution_basis: live
- scope: all entries in this file

- [agent: codex | model: gpt-5.5] Added a day-level checklist calendarization pass so `.memory/research/checklists/` now uses `.memory/research/checklists/YYYY-MM/YYYY-MM-DD/` instead of putting every manual verification checklist directly in the month folder. The memory protocol validator and research docs were updated to reject root-flat and month-flat checklist clutter.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-INPUT1` as the Equation-first imaginary input contract. Standalone `i` now canonicalizes to `\imaginaryI` only in Equation input, `ImaginaryUnit` is reserved from target discovery, Complex Off gives controlled guidance for explicit imaginary input, and Equation OOE provenance records explicit imaginary input. No stored complex values, non-Equation adoption, Approximate complex search, Isolate complex solving, or broad complex parser was added.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-EQUATION3` as the major algebraic complex Equation milestone after `COMPLEX-INPUT1`. `Exact + Complex On` now handles explicit imaginary linear equations, bounded factorable polynomial complex branches, selected-target power carriers already in the bounded complex route, and supported rational equations by numerator roots with denominator exclusions in `Valid when`. Output style is honored for exact/decimal/both complex readback where approximate branch values are available. Complex trig/log/exp solving, stored complex values, Approximate complex search, Isolate complex solving, non-Equation adoption, OOE behavior changes, and Rust solver execution remain deferred.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-READBACK-STABILITY1` as complex Equation stabilization/readback polish. Equation analysis now treats `i` / `\imaginaryI` as a reserved unit, so it appears as `i reserved unit` instead of a target/parameter/stored-value hint. Bounded complex power readback now avoids construction artifacts for awkward exact roots, may use clean `cis(...)` notation, and respects `EXACT`, `DECIMAL`, and `BOTH`. `j` and `k` remain ordinary symbols, and no reserved-symbol override syntax, stored complex values, complex Approximate search, Isolate complex solving, complex trig/log/exp solving, non-Equation adoption, OOE behavior change, or Rust solver execution was added.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-DISPLAY-SETTINGS1` as complex display/settings stabilization. Settings now persist `complexExactForm` with `rectangular`, `polar`, and `cis`; `auto` is intentionally absent. Symbolic display normalization preserves `ImaginaryUnit` / `\imaginaryI` as the imaginary unit instead of numeric `1`, keeping inputs such as `x^4+i=0` distinct from `x^4+1=0`. Equation requests, history/replay, and OOE provenance carry the complex exact form, while exact complex branch readback honors the selected form for bounded power and exact scalar complex branches. No new solver family, stored complex values, complex Approximate search, Isolate complex solving, reserved-symbol override syntax, non-Equation adoption, OOE runtime behavior, or Rust solver execution changed.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-PREIMAGE-EQUATION1` as the first guarded complex function-preimage route for Equation `Exact + Complex On`. Supported routes now reduce principal log, supported log-base, exponential, direct complex trig, affine shells, rational-linear inners, bounded powers, and selected quadratic-over-linear rational equations into exact inner equations while preserving denominator/log-domain facts in `Valid when`. Periodic routes use integer-family readback, root-family main answers for periodic powers, and collapsed expanded-branch details. Absolute-value complex locus solving, complex Approximate, complex Isolate, stored complex values, non-Equation adoption, OOE behavior changes, broad unfactorable cubic/quartic formulas, and Rust solver execution remain deferred.

- [agent: codex | model: gpt-5.5] Implemented `COMPLEX-PREIMAGE-EQUATION2` as the broader rational and true two-trig-layer complex preimage milestone. Equation `Exact + Complex On` now broadens finite rational denominator clearing when the cleared equation stays in supported exact bounded routes, preserves the four-layer finite composition cap, and supports outer `sin`/`cos`/`tan` over inner `sin`/`cos`/`tan` with distinct branch parameters such as `k,n in Z`. Inner trig arguments may be affine or bounded selected-target powers through degree 4. Periodic-over-rational, degree-limit, multivariable, absolute-value complex locus, complex Approximate, complex Isolate, stored complex values, non-Equation adoption, OOE behavior changes, and Rust solver execution remain deferred.
