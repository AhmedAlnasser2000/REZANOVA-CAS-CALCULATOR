# 2026-06-27

- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-RECIPROCAL-NONSQUARE1` extends literal repeated quadratic reciprocal powers `1/(c+u^2)^p` to positive exact-rational nonsquare constants for powers `2..4`. Successful cases keep visible `partial-fractions`, preserve inverse-trig precedence for power `1`, and use exact derivative backcheck after the verifier normalizes constant radical factors such as squared `sqrt(c)` terms. Completed-square parsing, symbolic coefficients, `c<=0`, power `5+`, broad recurrence machinery, public Rubi metadata, Display, History, OOE, Tauri, workspace contracts, and public Calculus schemas remain unchanged.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-COMPLETE-SQUARE1` adds a bounded completed-square parser for exact-rational irreducible quadratic reciprocal powers, including scalar-factor cases such as `2x^2+4x+5`. Supported cases normalize to the existing repeated quadratic recurrence and keep visible `partial-fractions` with exact backcheck; reducible quadratics continue through existing linear partial fractions. The completed-square parser lives in a small integration helper so `rational.ts` stays under the file-size ratchet.
- [agent: codex | model: gpt-5-codex] `FORMULA-VIEWER-READABILITY1` improves the Formula Viewer tab after virtualization with viewer-local math sizing (`125%` default, options through `200%`) and readable viewer-control contrast for math-size and `Show formula row` buttons. The proposed focused row inspector was removed as redundant; row inspection remains through the virtualized row list and per-row reveal without changing solver output, Copy Result, History, OOE, app-state, Tauri, or persisted Display schemas.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-NUMERATOR-POWER-LIFT1` lifts exact-rational affine numerator support over repeated quadratic reciprocal powers from power `2` to powers `3` and `4`, reusing the existing recurrence pieces for nonsquare and completed-square denominators. Successful mixed/constant numerator cases remain visible `partial-fractions` with exact backcheck, while pure derivative-numerator overlaps continue to resolve through `u-substitution`.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-MULTI-QUADRATIC-PARTIAL-FRACTIONS1` extends exact-rational rational-function partial fractions to at most two irreducible quadratic factor groups with multiplicity capped at `2`. The primitive now emits quadratic-power readiness terms, integration consumes them through bounded repeated-quadratic recurrence formulas, and cases such as `1/((x^2+1)(x^2+4))` and `1/((x^2+1)^2(x^2+4))` verify exactly under visible `partial-fractions`.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-AFFINE-LOG-BYPARTS1` adds exact-rational polynomial times affine-log integration by parts, including `x ln(2x+3)`, `(x+1)^2 ln(2x+3)`, and base-10 `log` forms. Expanded polynomial-log products still pass exact original-integrand backcheck through a bounded verifier cancellation helper; base-10 `log` remains derivative-backcheck confidence because symbolic `ln(10)` cancellation is deferred.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-RECIPROCAL-BINOMIAL-SUB1` adds derivative-present reciprocal binomial substitution for exact-rational `C*x^(-n-1)*(a+b*x^(-n))^p` forms, including reciprocal powers and `p=-1` logarithms. A scoped Laurent parser recognizes exact inverse-power binomial bases and monomial derivative factors; derivative-present overlaps are surfaced as `u-substitution` before derivative-ratio or partial-fractions, while missing derivative factors are not claimed by the new route.
