# 2026-06-27

- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-EXP-POLY-NUMERIC-BASE-BYPARTS1` extends the existing integration-by-parts cascade to positive exact-rational numeric bases `a^(m*x+n)`, including `x 2^x`, `x^2 2^x`, and bounded expanded polynomial factors such as `(x+1)^2(1/2)^(3x-1)`. The numeric-base helper lives outside `rules.ts`, keeps visible `integration-by-parts`, and uses an exact finite-recurrence proof for expanded polynomial-factor adoption; symbolic, zero, negative, decimal, and exp-trig bases remain deferred.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-TRIG-PRODUCT-TO-SUM1` adds bounded exact-rational affine product-to-sum integration for two-factor `sin*cos`, `sin*sin`, and `cos*cos` products under visible `direct-rule`. Generated affine trig terms are delegated to existing direct primitives, exact proof uses scoped two-factor trig-product identity normalization, and products with more than two trig factors remain controlled unsupported.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-TRIG-POWER-REDUCTION1` adds exact-rational affine core square reductions for `sin(u)^2`, `cos(u)^2`, `tan(u)^2`, and `cot(u)^2` under visible `direct-rule`. The direct helper emits bounded identity-based antiderivatives and the verifier now normalizes those four square identities locally for exact backcheck, while `sin(x)^3` and broader trig powers remain outside this slice.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-TRIG-DERIVATIVE-PRODUCTS1` adds exact-rational affine derivative-present trig products under visible `u-substitution`: `sec(u)tan(u)`, `csc(u)cot(u)`, and `sin(u)cos(u)` with exact scalar multiples. The matcher requires exactly two same-argument affine trig factors after stripping exact numeric scalars, so extra factors such as `x sec(x)tan(x)` remain controlled unsupported.
- [agent: codex | model: gpt-5-codex] `MATH-EDITOR-PASTE-FUNCTION-POWER-CANONICALIZATION1` repairs shared MathLive paste canonicalization for reciprocal trig function names and grouped exponent bases. Plain pasted `sec`, `csc`, and `cot` now become LaTeX function commands; `e^(...)` becomes `\exponentialE^{...}` only when `e` is clearly an exponential base; and grouped powers like `(1/2)^(3x-1)` preserve the full exponent group before MathLive can split it. Solver behavior, Display, History, OOE, Tauri, persistence, and public Calculus schemas remain unchanged.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-RECIPROCAL-NONSQUARE1` extends literal repeated quadratic reciprocal powers `1/(c+u^2)^p` to positive exact-rational nonsquare constants for powers `2..4`. Successful cases keep visible `partial-fractions`, preserve inverse-trig precedence for power `1`, and use exact derivative backcheck after the verifier normalizes constant radical factors such as squared `sqrt(c)` terms. Completed-square parsing, symbolic coefficients, `c<=0`, power `5+`, broad recurrence machinery, public Rubi metadata, Display, History, OOE, Tauri, workspace contracts, and public Calculus schemas remain unchanged.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-COMPLETE-SQUARE1` adds a bounded completed-square parser for exact-rational irreducible quadratic reciprocal powers, including scalar-factor cases such as `2x^2+4x+5`. Supported cases normalize to the existing repeated quadratic recurrence and keep visible `partial-fractions` with exact backcheck; reducible quadratics continue through existing linear partial fractions. The completed-square parser lives in a small integration helper so `rational.ts` stays under the file-size ratchet.
- [agent: codex | model: gpt-5-codex] `FORMULA-VIEWER-READABILITY1` improves the Formula Viewer tab after virtualization with viewer-local math sizing (`125%` default, options through `200%`) and readable viewer-control contrast for math-size and `Show formula row` buttons. The proposed focused row inspector was removed as redundant; row inspection remains through the virtualized row list and per-row reveal without changing solver output, Copy Result, History, OOE, app-state, Tauri, or persisted Display schemas.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-NUMERATOR-POWER-LIFT1` lifts exact-rational affine numerator support over repeated quadratic reciprocal powers from power `2` to powers `3` and `4`, reusing the existing recurrence pieces for nonsquare and completed-square denominators. Successful mixed/constant numerator cases remain visible `partial-fractions` with exact backcheck, while pure derivative-numerator overlaps continue to resolve through `u-substitution`.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-MULTI-QUADRATIC-PARTIAL-FRACTIONS1` extends exact-rational rational-function partial fractions to at most two irreducible quadratic factor groups with multiplicity capped at `2`. The primitive now emits quadratic-power readiness terms, integration consumes them through bounded repeated-quadratic recurrence formulas, and cases such as `1/((x^2+1)(x^2+4))` and `1/((x^2+1)^2(x^2+4))` verify exactly under visible `partial-fractions`.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-AFFINE-LOG-BYPARTS1` adds exact-rational polynomial times affine-log integration by parts, including `x ln(2x+3)`, `(x+1)^2 ln(2x+3)`, and base-10 `log` forms. Expanded polynomial-log products still pass exact original-integrand backcheck through a bounded verifier cancellation helper; base-10 `log` remains derivative-backcheck confidence because symbolic `ln(10)` cancellation is deferred.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-RECIPROCAL-BINOMIAL-SUB1` adds derivative-present reciprocal binomial substitution for exact-rational `C*x^(-n-1)*(a+b*x^(-n))^p` forms, including reciprocal powers and `p=-1` logarithms. A scoped Laurent parser recognizes exact inverse-power binomial bases and monomial derivative factors; derivative-present overlaps are surfaced as `u-substitution` before derivative-ratio or partial-fractions, while missing derivative factors are not claimed by the new route.
- [agent: codex | model: gpt-5-codex] `CALCULUS-ANSWER-PREVIEW-AUTOSIZE1` fixes large/nested calculus answer and generated-preview cards as a layout-only polish. Structured result answer blocks now allow more vertical room and stable scrolling, generated preview math can scroll horizontally with padding, and the integral-source UI regression still verifies one generated preview and one structured answer. No Display schema, Formula Viewer, History, OOE, Tauri, persistence, public Calculus result shape, or copy-contract changes were made.
- [agent: codex | model: gpt-5-codex] `INTEGRATION-RATIONAL-PF-PERF-SPLIT-AUDIT0` splits the slow rational partial-fractions integration regression into focused named groups and per-case repeated-quadratic/numerator tests. No rules or public contracts changed. The audit shows exact-square rational PF cases are fast, while nonsquare/completed-square repeated-quadratic arctan verification dominates the suite, with completed-square p2/p3 and completed-square numerator p4 around 45-50s each.
- [agent: codex | model: gpt-5-codex] `INTEGRATION-RATIONAL-PF-BACKCHECK-CACHE1` fixes the slow rational partial-fractions verifier path. The root cause was AST-only differentiation and exact backcheck doing derivative LaTeX/rendering work before proof, plus verifier equality using public rational normalization where raw exact cross-multiplication was enough. The focused hot group now runs in about `1.13s`, and `src/lib/symbolic-engine/integration.test.ts` now runs in about `5.04s` instead of about `250s`; no integration rules or public Calculus result schemas changed.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-WRAPPER-COVERAGE-BUNDLE1` was paused at the user-requested handoff boundary before code edits. The active plan is one major gated milestone that resumes wrapper work now that Formula Viewer performance/readability is no longer a blocker: Gate 1 covers Real Exact rational/linear-fractional carrier wrappers, Gate 2 covers Real Exact affine single-root wrappers `a*root(F,n)+c=rhs` for `n=2..12`, and Gate 3 hardens shared grouped formula readback, wrapper facts, denominator exclusions, generated-branch provenance, and Formula Viewer behavior. Complex wrappers, mixed/two-radical forms, nested wrappers, mixed exp/log wrappers, mixed trig wrappers, broad generated route widening, and schema/runtime contract changes remain deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-WRAPPER-COVERAGE-BUNDLE1` is implemented locally. Cardano/Ferrari formula `ratioLatex` now performs only narrow known-nonzero self-ratio cleanup for producer-owned coefficient readback, rational/linear-fractional Real examples are locked as regression coverage, and affine single-root radical shells `A*root(F,n)+C=R` for `n=2..12` isolate `(R-C)/A`, preserve symbolic coefficient nonzero facts, reuse odd/even root policies, and delegate generated degree-3/4 branches to Real Cardano/Ferrari. Complex affine radicals, mixed/two-radical forms, nested wrappers, broad route widening, and Display/OOE/History/Tauri/app-state/schema/copy contracts remain unchanged.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-LINEAR-QUADRATIC-PF-LIFT2` lifts exact-rational rational-function partial fractions to one irreducible quadratic factor group with multiplicity `3` alongside bounded linear factors, including `(x+1)/((x-2)(x^2+1)^3)`. The partial-fraction coefficient solve stays capped at 8x8 for this bounded path, over-cap quadratic multiplicity such as `(x^2+1)^3(x^2+4)` remains a controlled stop, and raw rational verifier reduction now cancels exact polynomial gcds after arithmetic so exact backcheck proves the larger decompositions without reintroducing the slow public normalization path.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-QUADRATIC-NUMERATOR-GENERAL1` hardens the already-live exact-rational numerator-over-completed-square path with planned regression coverage. `(x+5)/(2x^2+4x+5)^3` verifies exactly under visible `partial-fractions`, while `(2x+2)/(x^2+2x+3)^4` confirms pure derivative-numerator overlaps remain visible `u-substitution`. No runtime behavior, public Calculus schemas, Rubi metadata, Display, History, OOE, Tauri, persistence, or workspace contracts changed.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-MIXED-RADICAL-WRAPPER-BUNDLE1` implements the safe first mixed-radical formula slice. The existing mixed-algebraic branch handoff now opts Real Exact single square-root carrier branches into Cardano/Ferrari formula payloads, covering `sqrt(z^3+z+1)+z=b`, `sqrt(z^4+z+1)+z=b`, and target-free radical companions such as `sqrt(z^3+z+1)+sqrt(a)=b`. Existing low-degree mixed-algebraic output remains non-formula, while Complex mixed radicals, true two-selected-target radical formula widening, nested mixed radicals, broad route widening, and Display/OOE/History/Tauri/app-state/schema/copy contracts remain unchanged.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-NESTED-WRAPPER-SUBSTRATE1` adds an internal nested algebraic wrapper formula-readiness substrate without changing visible solve output. Exact depth-2 selected-target wrapper chains using square-root, absolute-value, square-power, odd-power, even-power, or nth-root carriers can now expose generated final branch equations, facts, and layer provenance for tests and the later formula milestone. Real Cardano/Ferrari nested formula output, Complex nested wrappers, depth-3 chains, mixed/additive carriers, and Display/OOE/History/Tauri/app-state/schema/copy contracts remain unchanged.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-TRIG-AFFINE-BASIC1` adds exact-rational affine trig primitives for `sin`, `cos`, `tan`, `cot`, `sec^2`, and `csc^2`. Successful cases stay visible `direct-rule` with exact derivative backcheck; the differentiation engine now supports the reciprocal trig derivatives and `ln(sin(u))`/`ln(cos(u))` simplifications needed to prove them. Broader trig powers/products, symbolic coefficients, branch-sensitive analysis, public Rubi metadata, and public Calculus schema changes remain deferred.
- [agent: codex | model: gpt-5-codex] `RUBI-TIER1-EXP-AFFINE-BASIC1` adds exact-rational positive non-unit numeric-base affine exponentials, including `2^(2x+3)` and `(1/2)^(3x-1)`, with visible `direct-rule` and exact `ln(a)` derivative backcheck. The direct helper for existing `e^(m*x+n)` now preserves exact-rational slopes, and the differentiation engine avoids unnecessary Compute Engine fallback for exact rational constant bases. Zero, negative, unit, decimal, symbolic bases, exponential products, and broad exp-log simplification remain deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-NESTED-ALGEBRAIC-WRAPPER-FORMULA1` enables Real Exact formula output for exact depth-2 algebraic nested wrapper chains accepted by the substrate. Eligible chains now render grouped `Nested Formula Cases`, preserve generated branch equations/facts/layer provenance, and delegate generated degree-3/4 final branches to Real Cardano/Ferrari. Complex, depth-3, non-algebraic nested, additive/mixed selected-target carrier widening, and Display/OOE/History/Tauri/app-state/schema/copy contracts remain unchanged.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-FERRARI-CLOSED-READBACK1` fixes non-generic Real Ferrari readback so primary `caseMath` rows close helper-derived quartic quantities instead of showing derived symbols such as `Y`, `F_sigma`, `P`, `Q`, `Delta`, or row-local `t` definitions. Generic full-slot Ferrari templates may still use compact helper definitions, and Complex Ferrari PrincipalRoot readback remains unchanged. Generated Real wrapper payloads inherit the closed rows because they consume the producer's `Real Ferrari Cases`.
- [agent: codex | model: gpt-5-codex] `EQUATION-REAL-MIXED-EXPLOG-WRAPPER-FORMULA1` makes affine mixed exp/log formula support intentional for Real Exact: one selected-target exp/log carrier with target-free companions can still delegate generated cubic/quartic branches to Real Cardano/Ferrari. The exp/log affine collector now carries target-free log/power domain facts and symbolic coefficient nonzero facts into global supplements, while Complex, target-dependent companions, multi-carrier log combinations, target-in-base formula widening, nested towers, Display, OOE, History, Tauri, app-state, schema, and copy contracts remain unchanged.
