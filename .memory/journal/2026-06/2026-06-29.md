# 2026-06-29

- [agent: codex | model: gpt-5-codex] Refined `CALCWIZ-NUMERIC-METHODS-ROADMAP0` so the future eight-move implementation session is Equation-owned. Statistics, Limits, Differentiation, and Calculus are covered only by shared numeric policy/contract enforcement and handoff notes, leaving domain-specific algorithms, UI, runtime behavior, and schemas to their own agents/lanes.
- [agent: codex | model: gpt-5-codex] Implemented `EQUATION-SYMBOLIC-POLISH-CLOSEOUT1 + EQUATION-NUMERIC-ELIGIBILITY-VARIABLE-POLICY1`: target-dependent Complex principal-image facts now stay in candidate/detail evidence instead of global Valid When, guarded finite branches can display `candidate roots`, and Equation Algebra/F4 now offers a prepare-only `Prepare Numeric Solve` report card that protects the solve target and does not run numeric solving.
- [agent: codex | model: gpt-5-codex] `RISCH-NORMAN-OUTPUT-HYGIENE1` fixes RN-generated Calculus exact-output readback for the screenshot families: malformed signs, negative fraction placement, and inline slash divisions such as `(2c)/a` are normalized before Display, and regression tests now check MathLive rendering does not produce black-box/error markup.
- [agent: codex | model: gpt-5-codex] Implemented `EQUATION-NUMERIC-SHAPE-CLASSIFIER1`: added an Equation-owned numeric shape classifier after stored-value preparation and target protection, added target-aware zero-form evaluation for selected targets such as `z` and `t`, collected internal denominator/log/root/periodic/discontinuity evidence for tests and later solver use, and hid the prepare-only `Prepare Numeric Solve` tray action. The numeric roadmap is refined so numeric solving remains inside the existing Solve/Run path, while Algebra/F4 is only explicit stored-value substitution consent for parameterized equations and must report missing stored values when clicked without all required non-target values.
- [agent: codex | model: gpt-5-codex] `RN-DEGENERACY-FACTS-BASELINE1` locks current Risch-Norman generic-case facts with tests and readiness notes: trig product denominators, exp-sincos pivots, positive-base conditions, affine-log domains, and affine slopes stay visible as `Valid When` facts, while unresolved degenerate same-slope trig products remain unsupported until a later branch milestone.
- [agent: codex | model: gpt-5-codex] `RN-ANSATZ-ORCHESTRATOR1` adds one internal RN orchestrator for exponential, sine/cosine, exp-sincos, affine-log, and affine-rational correction fallbacks. Dispatch calls it with route-family filters so public strategy labels and result schemas stay unchanged.
- [agent: codex | model: gpt-5-codex] `RN-NODE-FIRST-READBACK1` makes RN orchestrator successes node-first internally: affine rational correction now returns an antiderivative MathJSON node, and the orchestrator requires nodes for all successful RN families while keeping public exact LaTeX/readback contracts unchanged.
- [agent: codex | model: gpt-5-codex] Implemented `EQUATION-STORED-VALUE-SOLVE-CONSENT1`: Equation Algebra/F4 now shows `Use Stored Values` for selected-target equations with non-target parameter variables, reports missing stored values as an error card, protects the solve target, and otherwise launches the existing Equation Solve/Run path with a one-shot stored-value snapshot. This slice does not add deterministic numeric root methods.
- [agent: codex | model: gpt-5-codex] `RN-LOG-RATIONAL-CORRECTION-LIFT1` adds proof-based integration for bounded polynomial times affine `ln/log` over matching affine denominator powers through `3`, using the RN orchestrator and preserving public `integration-by-parts` strategy/readback contracts.
- [agent: codex | model: gpt-5-codex] `SYMBOLIC-RATIONAL-QUADRATIC-AUDIT0` documents current exact-rational versus symbolic quadratic rational coverage and sets the first safe later implementation slice as degree-one numerator over one power-one symbolic quadratic denominator under generic `a\ne0` and `4ac-b^2>0` facts.
- [agent: codex | model: gpt-5-codex] `CALCULUS-DERIVATIVE-UX-AUDIT0` documents the derivative UX/source split: derivative and derivative-at-point still edit in lower workspace math fields, while integrals/laplace use the main editor; the next safe repair is derivative main-editor source plus single-result presentation before route/cost preflight.
- [agent: codex | model: gpt-5-codex] `CALCULUS-DERIVATIVE-EDITOR-SOURCE1` moves guided Calculus derivative and derivative-at-point body editing into the shared Calculus main editor while preserving generated derivative request LaTeX for Copy Expr/runtime/history evidence. Integration and Laplace main-editor behavior remains intact; Calculate's compact derivative workbench is unchanged.
- [agent: codex | model: gpt-5-codex] `CALCULUS-DERIVATIVE-SINGLE-RESULT1` makes guided Calculus derivative results answer-card owned: derivative main-editor screens suppress the Display expression preview, and the result shell no longer repeats derivative `resolvedInputLatex` as a separate `Resolved form` row above the structured `Answer` block.
- [agent: codex | model: gpt-5-codex] `SYMBOLIC-DIFFERENTIATION-PREFLIGHT1` adds internal route/cost classification for symbolic differentiation (`direct-symbolic`, `compute-engine-fallback`, `unsupported`, `too-complex`, `malformed`) and gates guided Calculus derivatives before differentiation. Normal unsupported/over-budget derivatives return controlled errors; derivative-at-point may use bounded numeric central difference when symbolic preflight blocks and numeric evaluation succeeds.
- [agent: codex | model: gpt-5-codex] `RN-READBACK-MULTIPLICATION-STYLE1` adds shared integration readback hygiene for RN-generated and symbolic-quadratic rational outputs so nontrivial factors visibly multiply function factors such as `sin`, `cos`, and `arctan`, while keeping the policy out of global Display rendering.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-QUADRATIC-RATIONAL-LINEAR-NUMERATOR1` implements the first symbolic quadratic rational numerator slice for `(A*v+B)/(a*v^2+b*v+c)`, decomposing into derivative-log plus reciprocal-quadratic arctan terms under visible generic facts `a\ne0` and `4ac-b^2>0`.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-QUADRATIC-BRANCH-FACTS-BASELINE1` locks the symbolic quadratic rational branch baseline: `4ac-b^2>0` is the only live symbolic generic branch, exact-rational branch precedence remains intact, and negative/repeated/reducible symbolic branches remain deferred.
- [agent: codex | model: gpt-5-codex] Implemented `EQUATION-DETERMINISTIC-NUMERIC-ALGEBRAIC1 + EQUATION-NUMERIC-DOMAIN-SEGMENTATION1`: exact symbolic solving still wins first, but unsupported numeric-ready polynomial/rational Equation inputs now fall back to validated real approximate roots through degree `64`, with domain/exclusion facts, residuals, and rejected-candidate evidence kept in detail sections.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-QUADRATIC-POWER-READINESS1` adds test-facing readiness profiling for repeated symbolic quadratic rational powers `2` and `3`, while keeping live integration dispatch unsupported until recurrence/readback proof is explicitly implemented.
- [agent: codex | model: gpt-5-codex] `RN-READBACK-FACTOR-ORDER-AND-FUNCTION-CLARITY2` tightens RN/symbolic-quadratic readback clarity: exp-sincos fraction/monomial terms no longer render as mixed-looking side products, successful RN output remains node-backed internally, and explicit function-product dots survive symbolic display normalization.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-QUADRATIC-CASEWISE-BRANCHES1` makes symbolic quadratic rational power-one answers branch-complete for unknown discriminant sign: positive arctan, zero repeated-linear, and negative log-ratio branches are emitted in one casewise answer, with `a\ne0` as the only global fact.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-QUADRATIC-REPEATED-POWER-ADOPTION1` adopts `(A*v+B)/(a*v^2+b*v+c)^p` for powers `2` and `3` under the positive generic branch `4ac-b^2>0`, using derivative/residual splitting plus a bounded reciprocal-quadratic recurrence while keeping powers `4+`, zero/negative repeated branches, multiple symbolic quadratics, and broad symbolic partial fractions deferred.
- [agent: codex | model: gpt-5-codex] `RN-LOG-DERIVATIVE-DETECTOR1` adds a bounded symbolic `k*D'(v)/D(v)` detector over the RN coefficient/polynomial substrate for denominator degrees `2..6`. It adopts as visible `partial-fractions` with `k ln|D(v)|`, preserves exact-rational and affine-direct overlap ownership, and rejects decimals, branch-sensitive carriers, selected-variable-dependent coefficients, over-cap denominators, and non-matching rational shapes.
- [agent: codex | model: gpt-5-codex] `RN-SYMBOLIC-RATIONAL-HERMITE-REDUCTION1` adds the first bounded symbolic rational Hermite-correction adoption: exact rational derivatives of `P(v)/Q(v)^m` and residuals reducible to symbolic log-derivatives now integrate under visible `partial-fractions` for target-free symbolic coefficient inputs, while exact-rational substitution overlaps, powers `4+`, broad symbolic partial fractions, and algebraic extensions remain deferred.
