# Journal

## Historical Attribution

- primary_agent: codex
- primary_agent_model: gpt-5.5
- attribution_basis: live
- scope: all entries in this file

- [agent: codex | model: gpt-5.5] Implemented `PRE-RS34-LIVE-SNAPSHOT-GATE` to fix the stale MathLive/OOE launch handoff discovered after `OOE-RS31` through `OOE-RS33`.
- [agent: codex | model: gpt-5.5] Confirmed the visible Equation failures such as `\ln\left(x+1\right)=\ln\left(2x-3\right)` were not solver failures. The bad path launched or stale-checked against an old React snapshot while the visible MathLive editor already contained the new LaTeX.
- [agent: codex | model: gpt-5.5] Fixed Equation launch so `runEquationAction`, numeric Equation solve, algebra transform, pending History ticket input, and active revision resolution prefer a live MathLive snapshot and recompute the screen-specific `equationInputLatex` from that same snapshot.
- [agent: codex | model: gpt-5.5] Fixed Equation OOE snapshot canonicalization so the active input revision and route snapshot use the same canonical request shape instead of disagreeing raw/canonical forms.
- [agent: codex | model: gpt-5.5] Applied the same live/canonical launch discipline to Statistics because `OOE-RS33` made Statistics an OOE/ticketed MathLive workspace.
- [agent: codex | model: gpt-5.5] Recorded the future OOE widening rule: do not weaken stale gates; make launch, ticket, active revision, and route snapshot agree on one live/canonical source.
- [agent: codex | model: gpt-5.5] Implemented `OOE-RS34` as the Linear Algebra runtime-shell and launch-ticket widening pass for Matrix and Vector.
- [agent: codex | model: gpt-5.5] Added shared `linear-algebra-worker-runtime` / `linear-algebra-runtime` host routing while preserving separate Matrix and Vector capabilities, `linearAlgebra.matrix` and `linearAlgebra.vector`.
- [agent: codex | model: gpt-5.5] Added Matrix/Vector pending History tickets, background commit gating, typed `matrixSeed` / `vectorSeed` replay data, and OOE diagnostics evidence for worker host lifecycle and ticket ordering.
- [agent: codex | model: gpt-5.5] Verified RS34 without changing Matrix/Vector math capability, UI taxonomy, Rust solver execution, scheduler policy, or broader workspace migration.
