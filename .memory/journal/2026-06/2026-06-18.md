# 2026-06-18

## WORKSPACE-TABS-JOB-LIFECYCLE-FIX1

- [agent: codex | model: gpt-5.5] Fixed workspace-tab job lifecycle behavior after the first visible tab shell.
- Removed real OOE cancellation from same-tab mode retarget; retarget now relies on workspace-instance `navigationRevision` stale-drop.
- Kept tab focus non-destructive: switching to another tab does not cancel the origin tab's running job, and open-origin jobs remain eligible to commit into their saved origin display state.
- Added revision-aware pending/runtime status and tab job summaries so obsolete old-revision work does not make the retargeted tab show `Computing` or busy badges.
- Preserved explicit cancellation paths: closing a tab and `Stop Jobs in This Tab` still request cancellation for matching active jobs.
- Kept committed History global and schema-stable; no mode-launcher tab commands, default-new-tab settings, projects/files, Graphing, Spreadsheet, bus, Surface Protocol, runtime registry, plugin layer, or multi-window behavior.

## WORKSPACE-INACTIVE-TAB-COMMIT-FIX1

- [agent: codex | model: gpt-5.5] Fixed the remaining workspace-tab inactive completion stale-drop bug.
- Added origin-instance input-revision resolution so OOE-backed jobs from inactive but still-open tabs validate against their saved surface state instead of the currently visible tab.
- Threaded origin revision checks through shared workspace runtime launches and custom Calculate, Equation, Calculus, Table, Trigonometry, Statistics, and Geometry paths.
- Preserved same-tab retarget stale-drop through `navigationRevision`, and preserved explicit cancellation for close/Stop.
- Kept inactive completions origin-scoped: they can update only the origin tab's saved Display/Ans state and do not disturb the active visible tab.
- Removed the passive visible-display mirror that could copy one tab's answer/facts/assumptions into another tab; display writes are now explicit capture/commit operations scoped to the origin workspace instance.
- Tightened the async completion invariant after manual testing showed a subtler leak: even when the completed job belonged to the correct workspace instance, the visible active screen could still show that result if commit code consulted active display state at Promise resolution time. Launch context is now frozen for pending tickets/runtime commits, and runtime context getters stay live through refs so completion writes to the launch/origin instance rather than whichever tab is active later.
- Fixed the source-mirror CI failure by replacing the concrete production `playground/sources` manifest path literal with a neutral reference-mirror registry path label.
- Kept committed History global and schema-stable; no visible tab UX changes, mode-launcher commands, projects/files, Graphing, Spreadsheet, bus, Surface Protocol, runtime registry, plugin layer, or multi-window behavior.

## WORKSPACE-TABS-LAUNCHER-ACTIONS1

- [agent: codex | model: gpt-5.5] Added explicit launcher actions for opening hosted workspace entries in a fresh workspace tab.
- Kept primary launcher row click, Enter/hotkey flows, and existing Open behavior as current-tab retarget.
- Added a visible row `Open in new tab` action and a right-click leaf-row context menu with `Open Here` and `Open in New Tab`.
- Routed new-tab launches through the existing workspace-tab state host; no second `AppMain`, second OOE authority, projects/files, saved tab documents, Supercarrier work, bus, Surface Protocol, runtime registry, or plugin layer.
- Kept Labs open-here only and left Guide outside launcher-tab creation.
- Deferred `WORKSPACE-TABS-DEFAULTS1`; the `+` tab button still creates a blank Calculate tab.
- Kept file-size ratchet green by extracting launcher routing out of `AppMain.tsx` and applying a no-behavior one-line Trigonometry runtime formatting trim.
