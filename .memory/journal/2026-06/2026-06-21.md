# 2026-06-21

## EQUATION-ANSWER-MODE-SIMPLIFICATION0

- [agent: codex | model: gpt-5.5] Audited the current Equation answer-mode and numeric interval solve surfaces without changing source code or committing.
- Confirmed the current repo exposes `Approx` as both a persisted answer mode and the route into numeric interval solving, which blurs product semantics.
- Locked the new direction for implementation planning: active Equation answer modes should become `Exact` and `Isolate`; numeric interval solving should remain as a contextual numeric route/tool, not as the ordinary `Approx` answer mode.
- Confirmed header `DECIMAL` / numeric-output settings are display controls and should remain separate from Equation answer-mode simplification.
- Removed the uncommitted `EQUATION-ANSWER-SEMANTICS-TAGS1` source/test slice from the worktree before commit because its Approximate answer-mode framing is superseded by this audit; only the audit/memory direction remains.

## EQUATION-ANSWER-MODE-SIMPLIFICATION1

- [agent: codex | model: gpt-5.5] Implemented the first behavior-preserving answer-mode simplification: active Equation answer modes are now `Exact` and `Isolate` only in Settings and the Equation workspace.
- Removed new app launches and replay restoration from the persisted `Approx` answer-mode path. Legacy `equationAnswerMode: 'approximate'` remains readable only through compatibility request/history schemas.
- Kept Numeric Interval Solve as an explicit route/tool. It launches with `numericInterval`, records `solutionKind: 'approximate-numeric'` on successful numeric-route outcomes, and does not write `answerMode: 'approximate'`.
- Made Numeric Interval Solve contextual: hidden by default, available when the panel is already open, when periodic results include suggested intervals, or when an error advisory suggests numeric solving.
- Added missing-value guidance that names each missing non-target parameter and points to Variables, including the `\sqrt{2}x=a` boundary where Exact preserves symbolic `a` but Numeric Solve asks for `a` unless stored.
- Updated web settings parsing and Tauri settings sanitization so legacy/invalid `approximate` settings coerce to `exact`.

## EQUATION-NUMERIC-INTERVAL-REVIVAL0

- [agent: codex | model: gpt-5.5] Started a docs/memory-only audit of Numeric Interval Solve after answer-mode simplification; no production source changes were made.
- Confirmed active answer semantics are closed enough to move on: `Exact` and `Isolate` remain answer modes, while Numeric Interval Solve is a contextual route/tool.
- Found that header Run/F1 still dispatches symbolic Equation solving even when the Numeric Interval panel is visible; the panel's inner `Run Numeric Solve` button is the separate numeric launcher.
- Recorded probe evidence that ordinary roots are stable, but dense/nested periodic and discontinuity cases are grid-phase/subdivision sensitive under the current bracket-first sampler.
- Recommended next sequence: `EQUATION-NUMERIC-INTERVAL-ROUTE-REPAIR1`, then guidance, then numeric stability.

## EQUATION-NUMERIC-INTERVAL-ROUTE-REPAIR1

- [agent: codex | model: gpt-5.5] Repaired Numeric Interval Solve routing so the open numeric panel arms the primary Equation Run path.
- Header Run/F1/EXE now launch numeric interval solving only when the Numeric Interval panel is explicitly open; hiding the panel returns those controls to symbolic Exact/Isolate solving.
- Kept numeric-solve eligibility and panel visibility distinct: advisory-eligible exact stops can expose the `Numeric Solve` opener without auto-opening or auto-arming numeric execution.
- Removed the panel-local `Run Numeric Solve` button and made the panel configuration-only for Start, End, and Subdivisions.
- Updated focused runtime/UI tests for header-run routing, hidden-panel symbolic routing, and absence of the redundant panel-local run button.

## EQUATION-NUMERIC-INTERVAL-GUIDANCE1

- [agent: codex | model: gpt-5.5] Added Numeric Interval panel guidance for exact periodic suggested intervals.
- Suggested intervals now render inside the open Numeric Interval panel as click-to-fill Start/End actions; clicking a suggestion does not run the solver and does not change Subdivisions.
- Updated panel copy to clarify that Numeric Interval Solve is a local real-root search with final validation, not a proof that every root was found.
- Improved invalid interval, invalid subdivision, no-root, and rejected-candidate wording to mention local windows, dense/nested periodic pressure, suggested intervals, discontinuities/domain holes, and residual validation.
- Added focused UI and pure numeric tests for suggestion fill behavior and guidance wording.

## EQUATION-NUMERIC-INTERVAL-STABILITY1

- [agent: codex | model: gpt-5.5] Added bounded adaptive refinement to Numeric Interval Solve.
- The sampler now refines suspect cells for sign changes, near-zero residuals, local minima, steep jumps, discontinuity-like endpoints, and trig/log dense-periodic expressions, while respecting a capped extra-sample budget.
- Final candidate validation against the original equation is unchanged; the route still makes no all-roots guarantee.
- Diagnostics and summaries now include adaptive sample, refined-cell, and discontinuity-cell counts.
- Added tests for equivalent integer/decimal endpoint forms, local shifted intervals, dense nested periodic stability, and discontinuity-domain-hole handling.
- Amended the milestone after app QA found numeric roots could be accepted but hidden under Exact output style. Numeric route roots now render as the primary visible answer/readback and Copy Result copies them without pretending they are exact symbolic roots.

## EQUATION-FRONTIER-SOLVER-ROADMAP0

- [agent: codex | model: gpt-5.5] Started the Equation frontier solver track as docs/memory only.
- Created `.memory/research/roadmaps/equation-frontier-solver-roadmap.md` to move the next phase from search-discipline cleanup into capability expansion.
- Locked the frontier posture: add algebraic capability through typed substrate and family-owned consumers, not source-mirror imitation, blind cap raises, DAG-first architecture, or broad approximate fallback.
- Added a manual verification checklist for the just-finished answer-semantics/numeric-interval track before starting the new roadmap track.
- Recommended `EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1` as the first implementation lane.

## EQUATION-FRONTIER-HANDOFF-RESYNC0

- [agent: codex | model: gpt-5.5] Resynced the external solver-phase handoff against the current repo after search discipline, substrate seams, Exact/Isolate simplification, and Numeric Interval repair.
- Preserved the handoff's useful sequencing discipline: search before algorithms, representation before caps, no graphing/step-by-step/Rust detours before solver maturity.
- Corrected stale claims: the selected-target route is not `semantic-planner.ts`, flat polynomial representation is already partly present in Algebra, and cap tables are not implementation approval.
- Recorded that DAG/search graph, Cardano/Ferrari, implicit-root notation, Durand-Kerner fallback, graphing, and step-by-step remain deferred until their Calcwiz-native prerequisites are real.

## EQUATION-FACTORABLE-DECOMPOSITION-FRONTIER1

- [agent: codex | model: gpt-5.5] Implemented the first Equation frontier capability slice by widening explicit zero-product factorable solving to 12 target-degree slots.
- Split factorable cap policy so expanded/exact-rational factorable solving keeps the existing degree-4 behavior while explicit product solving may merge more independently solvable linear/quadratic factors.
- Interprets the explicit product budget after multiplicity, so five and twelve linear factors can solve, `(z-a)^{12}=0` preserves multiplicity detail with one visible root, and thirteen target-degree slots still stop with `degree-limit`.
- Kept broad automatic factoring, individual cubic/quartic factor formulas, implicit visible roots, numeric fallback as Exact closure, DAG/search graphs, Display/History/app-state/Tauri/OOE changes, graphing, and step-by-step out of scope.

## EQUATION-EXPANDED-FACTOR-FRONTIER1

- [agent: codex | model: gpt-5.5] Implemented the second Equation frontier capability slice by widening expanded exact-rational factorable solving to 12 target-degree slots only for the factorable Equation path.
- Extended the Algebra bounded factorization API with optional max-degree settings while preserving the default degree-4 behavior for existing callers.
- The expanded factorable path now solves degree-5 and degree-12 exact-rational polynomials when rational-root extraction reduces them to supported real linear factors plus an optional real quadratic remainder.
- Degree >12 expanded exact-rational factorable cases still stop with `degree-limit`; unfactored or unsupported degree 5-12 cases stop as `unsupported-expanded-polynomial` instead of fake roots.
- Kept symbolic-coefficient expanded cubics/quartics, broad automatic factoring, Cardano/Ferrari, implicit visible roots, numeric fallback, DAG/search graphs, OOE, Display/History schemas, app-state, Tauri, graphing, and step-by-step out of scope.

## EQUATION-SPECIAL-FORM-ROOTS-FRONTIER1

- [agent: codex | model: gpt-5.5] Implemented the third Equation frontier capability slice by widening real Exact affine selected-target power isolation through degree 12 while keeping Complex power isolation capped at degree 4.
- Added an internal exact-rational `special-form-roots` selected-target family for pure-power quadratic carriers `u=x^n` with total degree `2n <= 12`.
- The new family solves cases such as `x^6-5x^3+4=0` and `x^{12}-5x^6+4=0`, routes after factorable polynomial and before generic fallbacks, and records internal search-trace evidence.
- Symbolic carrier coefficients, non-pure or affine carrier substitution, total degree >12, and complex-only power expansion stop honestly instead of producing incomplete roots.
- Kept broad automatic factoring, Cardano/Ferrari, visible implicit roots, numeric fallback as Exact closure, DAG/search graphs, OOE, Display/History schemas, app-state, Tauri, graphing, and step-by-step out of scope.

## EQUATION-FRONTIER-SPECIAL-FORM-BUGFIX1

- [agent: codex | model: gpt-5.5] Repaired the first QA issues after the expanded-factor and special-form frontier commits.
- Live Equation input now normalizes unbraced multi-digit powers such as `^12` to `^{12}` on the app-state path while keeping the MathLive field editing-friendly.
- Single-target real Exact pure-power carrier quadratics now route through `special-form-roots` before the older shared symbolic fallback, preventing partial root sets like only `x=1` for `x^6-5x^3+4=0`.
- Complex On remains intentionally capped for high-degree special forms; it now stops with an honest Complex boundary instead of returning partial real-only roots.
