# 2026-06-19

## WORKSPACE-RUNTIME-STOP-CONTROL-FIX1

- [agent: codex | model: gpt-5] Fixed the Display header Stop path after manual QA showed a hard Calculus derivative could keep running while the header Stop button became disabled.
- Header Stop now first targets the active workspace's scoped pending runtime ticket, reusing the same exact OOE cancellation path as the History pending-row Stop action.
- Header Restart Editor also attempts the active pending runtime-ticket stop before remounting/restarting editor analysis, so it does not leave a hard worker job orphaned by the legacy editor-control allowlist.
- The Stop button remains enabled while the active runtime is still `Computing` even if editor analysis was already paused, and disables once the active runtime is already `Stopping` or no stoppable runtime remains.
- Live browser smoke on the tough Calculus derivative confirmed header Stop moves from `Computing` to `Calculus evaluation stopped` and removes the pending History row.
- Scope stayed app-shell/runtime wiring only: no solver changes, no OOE authority changes, no new bus, no host merge, and no stale/commit semantics changes.
