# 2026-06-19

## WORKSPACE-RUNTIME-STOP-CONTROL-FIX1

- [agent: codex | model: gpt-5] Fixed the Display header Stop path after manual QA showed a hard Calculus derivative could keep running while the header Stop button became disabled.
- Header Stop now first targets the active workspace's scoped pending runtime ticket, reusing the same exact OOE cancellation path as the History pending-row Stop action.
- Header Restart Editor also attempts the active pending runtime-ticket stop before remounting/restarting editor analysis, so it does not leave a hard worker job orphaned by the legacy editor-control allowlist.
- The Stop button remains enabled while the active runtime is still `Computing` even if editor analysis was already paused, and disables once the active runtime is already `Stopping` or no stoppable runtime remains.
- Live browser smoke on the tough Calculus derivative confirmed header Stop moves from `Computing` to `Calculus evaluation stopped` and removes the pending History row.
- Scope stayed app-shell/runtime wiring only: no solver changes, no OOE authority changes, no new bus, no host merge, and no stale/commit semantics changes.

## LANGUAGE-SURFACE-AUDIT0

- [agent: codex | model: gpt-5] Captured the future Language compartment idea as durable architecture memory with English fallback, typed contracts, interpolation, RTL metadata, and a clear Language/Display boundary.
- Started the repo-grounded language surface audit and classified live hard-coded text across app shell, workspace tabs, launcher, settings, history, variables, navigation metadata, Display result blocks, runtime status, Guide, Labs, diagnostics, and solver/readback outputs.
- Recorded that the first implementation path should be foundation plus narrow shell pilot, not a whole-app translation pass.
- Added a dedicated Language roadmap under `.memory/research/roadmaps/language-roadmap.md` so the next work starts with foundation and shell pilot before settings, panels, navigation metadata, Display-text notation, or RTL.
- Preserved Display ownership of math rendering and noted that existing `DisplayDetailLinePart` / `mixedDetailSection` helpers are the right starting seam for notation-faithful mixed prose and math.
- Scope stayed docs/memory only: no `src/` changes, no settings schema change, no solver wording changes, no Guide rewrite, no RTL layout work, no plugin/language-pack ecosystem, and no graphing/spreadsheet coupling.

## LANGUAGE-COMPARTMENT-FOUNDATION1

- [agent: codex | model: gpt-5] Added the English-only `src/lib/language/` foundation with typed catalog contracts, English surface modules, metadata, validation, deterministic fallback, and typed dynamic string functions.
- Added the pure Language API and unmounted React provider/hook while keeping app UI strings, settings schemas, Display math rendering, solver/readback wording, and OOE/runtime authority unchanged.
- Registered `language` in the compartment manifest as static text infrastructure with no future UI surface candidate yet, private language sources under `src/lib/language/languages/`, and `library-no-app-ui` / `no-source-mirrors` policies.
- Added focused language tests for supported-code resolution, unknown/invalid fallback, validation, dynamic title/count entries, and provider/hook behavior.

## LANGUAGE-SHELL-PILOT1

- [agent: codex | model: gpt-5] Migrated low-risk app-shell text consumers to the English-only Language seam while keeping `LanguageProvider` unmounted and preserving current behavior.
- `ModeStrip`, `LauncherWorkspace`, `WorkspaceTabs`, `MenuInspectorPanel`, and `DisplayPanel` runtime controls now read shell/common labels, titles, aria text, and typed tab/launcher confirmation strings through `useLanguage()`.
- Added only the missing English shell catalog keys needed by this pilot; custom tab titles remain user data, and Display result-card labels, side panels, Guide content, navigation metadata, settings persistence, solver/readback wording, and OOE/runtime behavior stayed out of scope.
- Added focused shell UI coverage for language-backed labels and existing workflows, including launcher current-tab/new-tab behavior, workspace-tab actions/confirmations, ModeStrip utility toggles, and Display runtime controls.

## LANGUAGE-SETTINGS-SEAM1

- [agent: codex | model: gpt-5] Added `languageCode` as the durable persisted language setting with English fallback in TypeScript app-state schemas, default settings, web-preview persistence, and Tauri desktop settings.
- Mounted `LanguageProvider` from `settings.languageCode` in `AppMain` and exposed `lang="en"` on the app shell without adding `dir`, RTL layout behavior, or non-English catalogs.
- Added a visible English-only Language row to `SettingsPanel` from the public Language metadata seam, with focused UI coverage for the active English chip and settings patch.
- Scope stayed settings/provider plumbing only: no broad SettingsPanel localization, no side-panel/result-card migration, no Guide/solver/readback wording changes, no plugin/language-pack system, and no OOE/runtime behavior changes.

## LANGUAGE-PANELS-PILOT1

- [agent: codex | model: gpt-5] Migrated panel-owned visible copy in `SettingsPanel`, `HistoryPanel`, and `VariablesPanel` to the English-only Language seam.
- Expanded the typed English catalog with Settings section/helper/preview/action labels, History pending/expanded/aria/action labels and functions, and Variables form/action/empty/status messages.
- Preserved existing panel behavior and data contracts: no settings schema changes, no History schema migration, no stored localized History strings, no variable policy changes, no solver/readback wording changes, and no OOE/runtime behavior changes.
- Added focused UI and language coverage for the migrated panel labels, typed dynamic entries, and existing panel callbacks.
- Fixed the full UI CI expectation drift in hosted runtime hook tests by documenting the existing no-workspace reservation payload as `workspaceInstance: null`.

## LANGUAGE-ROADMAP-CLOSEOUT0

- [agent: codex | model: gpt-5] Paused the active Language roadmap after the foundation, settings seam, shell pilot, and panels pilot reached a tested English-only baseline.
- Recorded the pause reason explicitly: remaining translation-oriented work has little value until a real second language or RTL commitment exists, and navigation metadata migration should wait for concrete copy pressure.
- Preserved the DisplayTextPart / mixed prose-math seam as future Display/readback/math-fidelity work rather than continuing the Language roadmap just to chase result-card polish.
- Scope stayed memory-only: no `src/` changes and no runtime, schema, UI, solver, Display, or OOE behavior changes.

## EQUATION-SEARCH-DISCIPLINE-AUDIT0

- [agent: codex | model: gpt-5.5] Preserved Claude's solver-phase handoff as corrected project memory, keeping its search-before-algorithms discipline while marking stale implementation pointers.
- Recorded that the live route is Equation symbolic -> parameterized/selected-target isolation -> generated-equation handoffs, not a semantic-planner-only strategy dispatcher.
- Noted that flat exact polynomial maps already exist under `src/lib/algebra/polynomial-core/`; the live question is how, or whether, parameterized symbolic coefficient collectors should align with that core.
- Added `.memory/research/roadmaps/equation-search-discipline-roadmap.md` to sequence target-shape profiling, selected-target route pruning, generated-equation handoff cleanup, symbolic-coefficient polynomial seam work, cap recalibration, and later algorithm expansion.
- Started the audit artifact for Equation search discipline and recommended target-shape profiling plus selected-target route pruning before Cardano/Ferrari, broad factoring, integration breadth, graphing, step-by-step, or Rust migration.
- Scope stayed docs/memory only: no `src/` changes, no solver behavior changes, no OOE/runtime authority changes, and no Display/History/app-state changes.

## EQUATION-EXACT-ISOLATE-BOUNDARY-DEFERRAL0

- [agent: codex | model: gpt-5.5] Deferred the Exact/Isolate answer-mode boundary cleanup until after Equation search discipline stabilizes.
- Recorded the candidate future differentiator: `Isolate` owns rearrangement/isolation intent, while `Exact` may build on isolation only when it adds roots, branch families, principal ranges, domain/exclusion facts, and candidate validation.
- Preserved the concern that some unresolved Exact cases can currently look like Isolate-mode output, especially when roots or trig principal ranges are not closed.
- Scope stayed docs/memory only: no `src/` changes, no answer-mode behavior changes, no solver routing changes, and no Display/OOE/runtime changes.

## EQUATION-TARGET-SHAPE-PROFILE1

- [agent: codex | model: gpt-5.5] Added a pure Equation target-shape profile seam under `src/lib/equation/target-shape/` with a root facade at `src/lib/equation/equation-target-shape.ts`.
- The profile classifies selected-target side, occurrence count, top-level additive target islands, polynomial degree, shape flags, and advisory route hints without solving or expensive simplification.
- Added focused tests for linear, polynomial, exponent, denominator, radical, trig/log/exp argument, both-side, multi-island, explicit named target, ambiguous product, parse-error, non-equation, and target-not-found cases.
- Scope stayed profile-only: no live route threading, no route pruning, no solver behavior change, no OOE/runtime/schema changes, no Display/History/app-state changes, and no Exact/Isolate cleanup.

## EQUATION-SELECTED-TARGET-ROUTER-PERF1

- [agent: codex | model: gpt-5.5] Added a conservative selected-target route planner on top of the target-shape profile.
- Top-level parameterized routing and generated-equation selected-target isolation handoffs now skip impossible family attempts while falling back to existing order for mixed or unknown shapes.
- Added route-plan tests and preserved current selected-target successes/stops for rational, exp/log, trig, composition, mixed algebraic, cubic power, and shell-isolation families.
- Scope stayed routing/performance discipline only: no new solver family, no broader transcendental support, no OOE/Display/History/app-state/Tauri changes, and no Exact/Isolate boundary cleanup.

## EQUATION-SEARCH-TRACE-EVIDENCE1

- [agent: codex | model: gpt-5.5] Added optional internal selected-target search trace evidence for route planning and handoff tests.
- Trace events record profile summaries, skipped families, attempted families, winning families, and final stop reasons for top-level selected-target routing and generated-equation handoffs.
- Added focused tests proving top-level exp/log route evidence and generated-handoff trig route evidence without using brittle wall-clock assertions.
- Scope stayed internal/test-facing: no DisplayOutcome trace fields, no History/schema persistence, no OOE diagnostics expansion, no UI text, no solver capability change, and no Exact/Isolate cleanup.
