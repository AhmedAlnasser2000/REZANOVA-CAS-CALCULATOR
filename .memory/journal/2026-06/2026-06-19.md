# 2026-06-19

## WORKSPACE-RUNTIME-STOP-CONTROL-FIX1

- [agent: codex | model: gpt-5] Fixed the Display header Stop path after manual QA showed a hard Calculus derivative could keep running while the header Stop button became disabled.
- Header Stop now first targets the active workspace's scoped pending runtime ticket, reusing the same exact OOE cancellation path as the History pending-row Stop action.
- Header Restart Editor also attempts the active pending runtime-ticket stop before remounting/restarting editor analysis, so it does not leave a hard worker job orphaned by the legacy editor-control allowlist.
- The Stop button remains enabled while the active runtime is still `Computing` even if editor analysis was already paused, and disables once the active runtime is already `Stopping` or no stoppable runtime remains.
- Live browser smoke on the tough Calculus derivative confirmed header Stop moves from `Computing` to `Calculus evaluation stopped` and removes the pending History row.
- Scope stayed app-shell/runtime wiring only: no solver changes, no OOE authority changes, no new bus, no host merge, and no stale/commit semantics changes.

## LANGUAGE-SURFACE-AUDIT0

- [agent: codex | model: gpt-5] Captured the future Language compartment idea as durable architecture memory with English fallback, typed contracts, interpolation, RTL metadata, and a clear Language/Display boundary.
- Started the repo-grounded language surface audit and classified live hard-coded text across app shell, workspace tabs, launcher, settings, history, variables, navigation metadata, Display result blocks, runtime status, Guide, Labs, diagnostics, and solver/readback outputs.
- Recorded that the first implementation path should be foundation plus narrow shell pilot, not a whole-app translation pass.
- Added a dedicated Language roadmap under `.memory/research/roadmaps/language-roadmap.md` so the next work starts with foundation and shell pilot before settings, panels, navigation metadata, Display-text notation, or RTL.
- Preserved Display ownership of math rendering and noted that existing `DisplayDetailLinePart` / `mixedDetailSection` helpers are the right starting seam for notation-faithful mixed prose and math.
- Scope stayed docs/memory only: no `src/` changes, no settings schema change, no solver wording changes, no Guide rewrite, no RTL layout work, no plugin/language-pack ecosystem, and no graphing/spreadsheet coupling.

## LANGUAGE-COMPARTMENT-FOUNDATION1

- [agent: codex | model: gpt-5] Added the English-only `src/lib/language/` foundation with typed catalog contracts, English surface modules, metadata, validation, deterministic fallback, and typed dynamic string functions.
- Added the pure Language API and unmounted React provider/hook while keeping app UI strings, settings schemas, Display math rendering, solver/readback wording, and OOE/runtime authority unchanged.
- Registered `language` in the compartment manifest as static text infrastructure with no future UI surface candidate yet, private language sources under `src/lib/language/languages/`, and `library-no-app-ui` / `no-source-mirrors` policies.
- Added focused language tests for supported-code resolution, unknown/invalid fallback, validation, dynamic title/count entries, and provider/hook behavior.
