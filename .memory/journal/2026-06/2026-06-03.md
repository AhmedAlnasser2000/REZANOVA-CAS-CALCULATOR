# 2026-06-03 Journal

## INEQUALITY-EQUATION1

- [agent: codex | model: gpt-5] Implemented the first bounded Equation inequality product route.
- Supported scope is top-level one-variable numeric-coefficient linear inequalities in `Exact` mode only.
- Successful results use `INEQUALITY-CORE1`, `answerDomain: conditional-real`, and `solutionKind: inequality-solution-set`.
- `Approximate` and `Isolate` return controlled inequality guidance.
- `Complex On` keeps ordered inequality math real-domain-only and adds a visible real-order note.
- Deferred all broad inequality families, non-Equation adoption, Approximate sampling, Isolate rearrangement, stored-value policy changes, OOE runtime changes, and Rust solver execution.

## ANSWER-DOMAIN-READBACK1

- [agent: codex | model: gpt-5] Implemented focused history/readback polish after bounded complex and inequality adoption.
- Fixed the persisted-history bootstrap layout so many restored entries stay readable as compact non-shrinking cards.
- Collapsed history entries now prioritize input preview only; answer, valid-when, domain, and solution details appear on expansion inside contained scroll areas.
- Consolidated Equation result chips so actual complex answers show `Domain: Complex` without a duplicate `Domain intent: Complex`, while ordered inequalities under Complex intent still show the real-order note.
- Fixed prose readback for ASCII comparison operators such as `<=`, `>=`, and `!=`.
- Polished simple bounded complex power-branch display without adding broad simplification or new solver capability.

## Inequality/Complex Next-Step Naming

- [agent: codex | model: gpt-5] Recorded the post-readback milestone naming correction.
- Keep `POLYNOMIAL-DOMAIN-CORE1` only as a narrow shared classifier/extractor/fact substrate if the next leap needs common polynomial/rational shape metadata.
- Do not lock in `EQUATION-ADVANCED-DOMAIN1`; it is too broad and risks combining unrelated product-facing work.
- Keep the next product-facing rails explicit: `INEQUALITY-EQUATION2` for bounded quadratic/factored real inequality sets and `COMPLEX-EQUATION2` for broader bounded polynomial/power complex answers.

## POLYNOMIAL-DOMAIN-CORE1

- [agent: codex | model: gpt-5] Added the shared pure-core polynomial/rational domain classifier substrate.
- The new core classifies one-variable exact polynomial metadata up to degree 4, including coefficients, degree/shape, primitive and monic readback, and quadratic discriminants.
- Rational classification exposes normalized numerator/denominator polynomial metadata and denominator nonzero facts through the existing assumption spine with source `polynomial-domain-core`.
- No Equation route, OOE behavior, UI, history, result schema, solver capability, inequality solving, or complex solving changed.

## INEQUALITY-EQUATION2

- [agent: codex | model: gpt-5] Implemented bounded Equation polynomial inequality solving as a separate rail from complex work.
- Added a shared Equation polynomial-domain helper to extract top-level zero forms and polynomial metadata for Equation consumers.
- Extended Equation `Exact` inequality solving from linear-only to bounded one-variable numeric-coefficient polynomial inequalities through degree 4 when exact real roots are available.
- Added sign-chart interval-union readback for quadratic, repeated-root, constant true/false, and bounded factorable polynomial cases.
- Preserved Approximate and Isolate inequality guidance, real-order behavior under `Complex On`, and all non-Equation boundaries.

## COMPLEX-EQUATION2

- [agent: codex | model: gpt-5] Implemented bounded opt-in complex polynomial/power answers as a separate rail from inequality work.
- `Complex On + Exact` can now solve factorable one-variable polynomial equations through degree 4 when bounded factorization reduces to linear/quadratic factors and includes at least one non-real branch.
- Preserved real-first `Complex Off`, real-only `Approximate`, and rearrangement-only `Isolate` contracts.
- Cleaned simple complex branch readback such as `1/2 sqrt(12)i` into `sqrt(3)i`.
- Kept unfactorable cubic/quartic formulas, complex numeric root search, complex parser, stored complex values, non-Equation adoption, OOE behavior changes, and Rust solver execution deferred.

## INEQUALITY-EQUATION3

- [agent: codex | model: gpt-5] Implemented the major Equation-only guarded real inequality decision engine.
- Added a reusable sign-analysis core for exact critical points, domain exclusions, open/closed cells, sampled sign classification, and conversion back to `INEQUALITY-CORE1` interval sets.
- Extended Equation `Exact` inequality support to factorable rationals, textbook absolute-value inequalities, guarded square-root inequalities, monotone log/exp inequalities, bounded supported composition, and direct affine periodic trig inequalities.
- Kept the proof contract exact-guarded: numeric samples classify cells but numeric-only roots do not become exact answers.
- Preserved `Approximate` and `Isolate` inequality guidance, real-order behavior under `Complex On`, and all non-Equation/OOE/Rust boundaries.

## INEQUALITY-READBACK-COMPOSITION1

- [agent: codex | model: gpt-5] Implemented Equation-only inequality readback and guarded-composition polish after `INEQUALITY-EQUATION3`.
- Moved inequality restrictions into the existing `Valid when` card: denominator exclusions, radicand/log domains, monotone-base facts, tangent singularities, period/family facts, and real-order notes now use `exactSupplementLatex`.
- Kept proof detail sections as route narration only so restrictions are not duplicated below the result.
- Raised finite guarded composition to 4 layers for supported `abs`, `sqrt`, monotone `ln`/`log`, and `exp` reductions.
- Added representable two-layer trig inequality support for outer `sin`/`cos`/`tan` over inner affine `sin`/`cos` cases, plus guarded `tan(sin(x))`-style reductions where the range can be represented.
- Added safe inner-`tan` all/empty cases such as `sin(tan(x))<2`, with tangent singularities carried in `Valid when`; nontrivial inner-`tan` two-layer cases remain controlled stops because the current periodic-set readback cannot honestly represent the infinite tangent-branch subfamilies yet.
- Added guarded target-free numeric shell isolation before inequality routing, so simple forms such as `ln(x)-5<4`, `ln(x)/5<4`, and `-2ln(x)<4` reduce through the existing log route instead of requiring an equality-style inverse solver.
- Added collapsible/expandable result readback blocks for verbose `Valid when` and proof/detail sections so long periodic/domain facts stay available without burying the main answer.

## INEQUALITY-PREIMAGE-READBACK2

- [agent: codex | model: gpt-5] Implemented the combined pending inequality-preimage and readback polish milestone.
- Flattened abs-affine periodic preimages such as `tan(|5x-4|)>1/2`, `sin(|x-4|)>1/2`, and `cos(|2x+1|)<=0` into `x`-alone branch families when safe.
- Cleaned periodic readback so symbolic shifts render as `k\pi`, `2k\pi`, and `\frac{k\pi}{5}` instead of raw multiplication artifacts.
- Threaded Equation output style into inequality readback: `EXACT` keeps symbolic inverse-trig thresholds, `DECIMAL` rounds thresholds, and `BOTH` keeps exact main answers with approximate threshold detail.
- Kept tangent singularities, branch-index facts, period/step facts, and real-order notes in `Valid when`.
- Extended bounded finite preimage routing for supported rational/polynomial inners under `abs`, `sqrt`, `ln`/`log`, and `exp`.
- Preserved guarded stops for periodic-over-rational, periodic-over-nonlinear-abs, symbolic-threshold, chained, multivariable, and Tier 4 nonlinear periodic preimage cases.

## INEQUALITY-STABILITY1

- [agent: codex | model: gpt-5] Implemented an Equation-only inequality stability gate after the major preimage/readback work.
- Hardened relation operator normalization across live typing, paste/copy, runtime, and replay-shaped inputs.
- Added regression coverage for current supported inequality families and controlled stops, including operator variants that previously risked falling through to Calculate.
- Updated guarded-family stop guidance to describe the math boundary instead of leaking old milestone names.
- Preserved all capability boundaries: no new inequality family, no Approximate inequality sampling, no Isolate inequality solving, no graphing, no non-Equation adoption, no complex ordered inequalities, no OOE behavior change, and no Rust solver execution.
