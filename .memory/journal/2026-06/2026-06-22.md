# 2026-06-22

## EQUATION-COMPLEX-SPECIAL-FORM-NOTATION-FIX1

- [agent: codex | model: gpt-5.5] Fixed a readback regression where high-degree Complex special-form roots always rendered with `cis` even when the user selected another `complexExactForm`.
- High-degree special-form branch rendering now honors `cis` explicitly and uses exact trigonometric branch notation for `rectangular`/`polar` until rectangular radical coordinates are implemented.
- Added focused tests for `x^5=32` across rectangular, polar, and cis settings, plus pure/affine carrier coverage.

## EQUATION-SYMBOLIC-FACTOR-DISCOVERY-FRONTIER2

- [agent: codex | model: gpt-5.5] Extended `symbolic-factor-patterns.ts` with bounded factor-by-grouping and grouped affine-carrier quadratic recognition.
- The factorable route now detects shared-carrier grouping such as `x*(x+a)+b*(x+a)=0` and monic grouped carrier quadratics such as `(x+c)^2+(a+b)*(x+c)+a*b=0` and `(x+c)^2+2*a*(x+c)+a^2=0`.
- The new recognizers still delegate only supported linear/quadratic target factors through existing root/readback/fact seams; broad symbolic factoring and reusable primitive promotion remain deferred.

## EQUATION-CARRIER-ELIMINATION-FRONTIER1

- [agent: codex | model: gpt-5.5] Added a bounded real Exact carrier-elimination route for explicit algebraic carriers, exposed internally as the `carrier-elimination` selected-target family.
- The route detects exact-rational linear/quadratic equations in a known carrier, normalizes common carrier powers, and back-substitutes real carrier roots through existing generated branch solvers so branch facts/readback stay owned by the proven seams.
- Covered examples include `(x^2+a)^2-5(x^2+a)+4=0`, `(x+a)^4-5(x+a)^2+4=0`, and `(\sqrt{x+a})^2-5\sqrt{x+a}+4=0`; symbolic reduced-carrier coefficients, periodic/transcendental carriers, arbitrary auxiliary inference, and general elimination remain deferred to future symbolic-primitives work.

## EQUATION-COMPLEX-SYMBOLIC-SPECIAL-FORM-FRONTIER1

- [agent: codex | model: gpt-5.5] Completed the audit gate for symbolic Complex special-form roots and kept them deferred because current surfaces do not yet carry formal principal-branch semantics.
- Exact-rational Complex direct/pure/affine special-form routes remain supported and continue honoring `complexExactForm`; symbolic cases such as `x^5=a` and `x^6-a*x^3+b=0` now produce clearer boundary wording instead of fake exact branches.
- Added regression coverage that symbolic Complex special-form boundaries mention the principal-branch policy and do not surface visible `RootOf` or informal symbolic branch notation.

## SYMBOLIC-PRIMITIVES-SURFACE-AUDIT0

- [agent: codex | model: gpt-5.5] Audited the live repo for reusable symbolic primitive pressure after the Equation frontier run.
- Named the five reusable building blocks as Symbolic Primitives: expansion, substitution, factorization, simplification, and elimination.
- Recommended future dedicated folders under `src/lib/symbolic-engine/primitives/`, with expansion first, then substitution, factorization, simplification, and elimination.
- Recorded the governance rule that future solver work should consume a primitive when available, add one when repeated mechanics are emerging, or document why route-local logic remains semantic and owned.

## SYMBOLIC-PRIMITIVES-COMPARTMENT-ROADMAP0

- [agent: codex | model: gpt-5.5] Created the Symbolic Primitives compartment roadmap as a docs/memory-only gate after the surface audit.
- Locked the initial owner shape as a governed district under the existing `symbolic-engine` compartment, with future implementation folders under `src/lib/symbolic-engine/primitives/`.
- Chose private-first APIs and deferred public facades plus app-wide primitive-surveillance tooling until real consumers and repeated bypass pressure exist.
- Added an Equation frontier baseline checklist before starting the new Symbolic Primitives track.

## SYMBOLIC-EXPANSION-PRIMITIVE1

- [agent: codex | model: gpt-5.5] Added the first private Symbolic Primitive under `src/lib/symbolic-engine/primitives/expansion/`.
- The bounded MathJSON expansion API covers safe additive/multiplicative distribution, subtraction/negation normalization, and positive integer powers with capped power, term, and node counts.
- Registered `src/lib/symbolic-engine/primitives/` as a private Symbolic Engine path and replaced the Equation polynomial carrier follow-on local expansion loop with `expandMathJsonNodeOrOriginal`.
- Fixed the manual QA route gap by allowing top-level quadratic-carrier equations to reach the existing carrier-follow-on bridge in real Exact and a narrow Complex Exact quadratic-carrier follow-on; broader nonlinear carriers and non-quadratic Complex carrier closure still stop honestly.
- Fixed the manual QA `ii` readback leak in the narrow Complex quadratic-carrier follow-on by keeping the generated imaginary magnitude real before appending `i`.
- Deferred broader final-answer readback polishing/normalization, such as simplifying `0 + ...`, `(-1+1)/2`, or equivalent radical spelling, to a dedicated future readback milestone rather than folding it into the expansion primitive.
- Preserved the wider boundary: no broad solver migration, no public primitive facade, no OOE/Display/History/app-state/Tauri/UI change.
