# 2026-06-02

## INEQ-COMPLEX-FOUNDATION0 Equation-First Domain Audit

- [agent: codex | model: gpt-5] Implemented `INEQ-COMPLEX-FOUNDATION0` as a documentation/readiness milestone only.
- Froze the initial answer-domain vocabulary as `real`, `complex`, `conditional-real`, and `unknown-domain`.
- Froze solution kind as separate from answer domain: exact symbolic answer, approximate numeric answer, isolate/rearranged formula, inequality solution set, and condition/fact-only stop.
- Recorded that future complex answers are opt-in through a top-header `Complex` domain toggle, disabled by default, with real-first behavior preserved when disabled.
- Recorded that product-facing inequality and complex adoption stays Equation-first until the semantics stabilize, while the underlying cores remain reusable.
- Audited existing reusable substrates: `assumptions-core`, `branch-core`, `domain-range-core`, the current `numeric/complex` primitive, `SolveDomainConstraint`, `DisplayOutcome`, Equation answer modes, and OOE provenance.
- Audited current real-only and inequality/complex-adjacent behavior across Equation, Calculate, Table, Calculus, Advanced Calc, Trigonometry, Geometry, Statistics, Matrix, and Vector.
- Preserved boundaries: no solver code, UI toggle, result schema, history schema, stored-value policy, OOE behavior, or runtime capability change.

## VALUE-DOMAIN-CORE1 Shared Domain And Value Contract

- [agent: codex | model: gpt-5] Implemented `VALUE-DOMAIN-CORE1` as a pure internal TypeScript substrate.
- Added shared `AnswerDomain` vocabulary: `real`, `complex`, `conditional-real`, and `unknown-domain`.
- Added shared `SolutionKind` vocabulary: `exact-symbolic`, `approximate-numeric`, `isolate-formula`, `inequality-solution-set`, and `condition-fact-only-stop`.
- Added value-domain metadata builders that dedupe through the existing assumption fact spine and summarize domain/solution/fact state.
- Extended assumption facts with `inequality-constraint` and `complex-domain-note`, plus `value-domain-core`, `inequality-core`, and `complex-core` sources.
- Added simple readback grouping for inequality facts and complex-domain notes.
- Preserved boundaries: no solver behavior change, UI toggle, stored complex variables, `DisplayOutcome`, history, app-state, OOE, Rust, Tauri schema, or visible product adoption.

## CI-TIMEOUT2 Unit Timeout Follow-Up

- [agent: codex | model: gpt-5] Raised the unit Vitest timeout from `55000` to `250000` ms after the heavy Equation screenshot-regression case exceeded the previous budget in CI.
- Updated the existing explicit symbolic-integration long-test timeout to the same `250000` ms budget.
- Verified the focused failing Equation unit file with `npm run test:unit -- src/lib/modes/equation.test.ts`.

## COMPLEX-CORE1 Complex Primitive And Readback

- [agent: codex | model: gpt-5] Implemented `COMPLEX-CORE1` as a pure internal TypeScript complex-number primitive/readback milestone.
- Extended `src/lib/numeric/complex.ts` with complex conjugate, argument, polar construction, integer powers, principal nth roots, and deterministic all nth roots.
- Added principal-root and all-branches readback helpers without claiming Equation solving or changing any result surface.
- Connected complex branch readback to the shared value-domain/fact substrate only through tests and `complex-core` facts.
- Preserved boundaries: no parser, top-header `Complex` toggle, Equation adoption, stored complex values, complex Approximate mode, `DisplayOutcome`, history, app-state, OOE, Rust, Tauri schema, or visible UI behavior change.

## INEQUALITY-CORE1 Bounded Inequality Interval Core

- [agent: codex | model: gpt-5] Implemented `INEQUALITY-CORE1` as a pure internal TypeScript inequality interval/fact substrate.
- Added `src/lib/algebra/inequality-core.ts` with typed one-variable real interval unions and constructors for all-real, empty, point, open/closed intervals, and simple comparison-shaped sets.
- Added canonical normalization for finite unions, including sorting, dedupe, overlap merging, and compatible touching interval merging.
- Added intersection, containment, empty-set detection, equality, text readback, and LaTeX readback helpers.
- Connected inequality sets to `VALUE-DOMAIN-CORE1` through `inequality-core` assumption facts and `solutionKind: inequality-solution-set`.
- Preserved boundaries: no inequality parser, visible Equation route, broad nonlinear solver, piecewise engine, graphing, public assumptions UI, `DisplayOutcome`, history, app-state, OOE, Rust, Tauri schema, or visible UI behavior change.

## EQUATION-DOMAIN-INTENT1 Equation Complex Intent Toggle

- [agent: codex | model: gpt-5] Implemented `EQUATION-DOMAIN-INTENT1` as the first product-facing complex-domain intent layer.
- Added persisted `settings.equationDomainIntent` with `real` default, web-preview persistence, app-state zod defaults, and Rust desktop sanitization.
- Added a top-header `Complex Off` / `Complex On` quick toggle.
- Threaded the intent through Equation symbolic runtime requests, active Equation request refs, OOE input revisions, history entries/replay, and rich Equation OOE provenance.
- Added an Equation result-card badge/note only when Complex is enabled: `Domain intent: Complex`.
- Preserved boundaries: solvers remain real-first, and no complex solving, inequality solving, complex parser, stored complex values, non-Equation adoption, result semantics change, history schema migration, or OOE behavior change is included.

## COMPLEX-EQUATION1 Bounded Equation Complex Answers

- [agent: codex | model: gpt-5] Implemented `COMPLEX-EQUATION1` as the first bounded product-facing complex Equation slice.
- Added optional answer-domain metadata to Equation outcomes/history and carried complex-domain evidence into OOE Equation provenance.
- Added bounded exact complex branches for numeric-coefficient quadratics with negative discriminants when `Complex On` and answer mode `Exact`.
- Added bounded complex selected-target power branches for simple isolated/algebraically isolated powers.
- Marked complex Equation result cards with `Domain: Complex`.
- Preserved boundaries: `Complex Off` remains real-first, `Approximate` remains real numeric interval solving only, `Isolate` remains textbook rearrangement, and no complex parser, stored complex values, inequality solving, non-Equation complex adoption, broad transcendental complex solving, Rust solver execution, or OOE runtime behavior change is included.
