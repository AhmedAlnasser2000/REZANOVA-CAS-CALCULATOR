# 2026-06-25

- [agent: codex | model: gpt-5-codex] `EQUATION-QUARTIC-FERRARI-ROUTE1` made direct degree-4 selected-target Ferrari live in both Complex Exact and Real Exact. It adds the `quartic-ferrari` top-level family after Cardano, a parameterized Ferrari solver over the n-degree substrate, structured Ferrari branch nodes through finite-root presentation, Real `caseMath` rows with resolvent Cardano cases, a biquadratic `q=0` special form, and route trace evidence. Rational-cleared quartics, generated/wrapper Ferrari or Cardano, symbolic carrier quadratics, `RootOf`, implicit-root display, schemas, OOE, History, app-state, and Tauri remain deferred.
- [agent: codex | model: gpt-5-codex] CI/file-size follow-up for the same milestone extracted the Cardano/Ferrari top-level formula route wiring into `src/lib/modes/equation/parameterized-formula-routes.ts` so `src/lib/modes/equation/parameterized.ts` stays under the 900-line ratchet. The extraction keeps the same route order and trace evidence while avoiding a baseline cap raise.
- [agent: codex | model: gpt-5-codex] `EQUATION-CARDANO-FERRARI-RATIONAL-NORMALIZATION1` generalized the Cardano-only rational clearing helper into `formula-rational-normalization.ts`. Top-level rational clearing now delegates cleared degree 3 to Cardano and cleared degree 4 to Ferrari in Real Exact and Complex Exact, preserving denominator exclusions and formula facts. Target-denominator route planning now attempts `rational`, `cubic-cardano`, then `quartic-ferrari`; generated/wrapper Cardano and Ferrari remain non-live.
- [agent: codex | model: gpt-5-codex] CI follow-up for `EQUATION-CARDANO-FERRARI-RATIONAL-NORMALIZATION1`: stabilized the `AppMain.ui.test.tsx` Equation numeric interval angle-unit UI test that timed out in GitHub Actions. The test now uses a lighter interval grid, re-queries interval inputs after settings toggles, and relies on the UI suite timeout instead of a local 10s cap; production behavior is unchanged.
- [agent: codex | model: gpt-5-codex] `EQUATION-CARDANO-FERRARI-GENERATED-HANDOFF-AUDIT1` keeps generated/wrapper Cardano and Ferrari non-live after direct and rational-cleared top-level formula routes. The audit documents that current generated handoff flattens solver output to raw solution expressions and global supplements, so it cannot yet preserve Real case rows, Complex definition-backed branches, scoped wrapper/formula facts, candidate validation, or Display/copy semantics. Next work should build a structured generated-formula payload and validation seam before any wrapper family consumes Cardano/Ferrari.
- [agent: codex | model: gpt-5-codex] `EQUATION-GENERATED-FORMULA-HANDOFF-PAYLOAD1` adds an internal structured generated-formula payload under Equation parameterized ownership and threads it through the shared generated branch handoff. Legacy generated handoff behavior stays stable, while the new payload can carry Complex finite branch rows, Real `caseMath` rows, formula metadata, scoped facts, detail sections, answer domain, and global supplements without parsing formula answers back out of `exactLatex`. Generated/wrapper Cardano and Ferrari remain non-live until validation and wrapper consumer milestones land.
- [agent: codex | model: gpt-5-codex] `EQUATION-GENERATED-FORMULA-VALIDATION1` adds the internal validation policy for generated formula payloads. The shared handoff now records a validation-required `family-stop` for structured formula payloads unless validation evidence is present, with separate reasons for non-flattenable Real case output, case/branch-local conditions, non-liftable scoped facts, missing wrapper back-substitution validation, and missing candidate validation. Legacy generated handoff behavior remains stable and generated/wrapper Cardano/Ferrari stay non-live.
- [agent: codex | model: gpt-5-codex] `EQUATION-ALGEBRAIC-WRAPPER-FORMULA1` makes the first generated formula wrapper consumer live: Real Exact one-layer square-root composition can delegate generated degree-3 and degree-4 equations to Cardano or Ferrari when validation evidence is supplied by the square-root wrapper. The route preserves Real `caseMath`, wrapper facts such as `b\ge0`, denominator exclusions, definitions, and case-local formula conditions. A user-reported amend fixed rational square-root wrappers by keeping `composition` in the route plan when a selected target is both under a radical and in a denominator, covering both fraction-template and slash-style input. Complex square-root wrappers, absolute values, square powers, nested/mixed algebraic wrappers, exp/log/trig wrappers, global generated formula route widening, schemas, OOE, History, app-state, and Tauri remain unchanged. The same gate fixes the reported CI drift in `parameterized-families.test.ts` by expecting the current selected-target-island stop wording.
- [agent: codex | model: gpt-5-codex] `EQUATION-ABS-WRAPPER-FORMULA-POLICY0` audits the next wrapper formula candidate without code changes. Real absolute-value wrappers are mathematically suitable next through `F=b` and `F=-b` with global `b\ge0`, but live implementation needs grouped/multi-payload generated formula readback so both sign branches can keep separate Cardano/Ferrari case rows, branch provenance, denominator exclusions, and scoped facts. Complex absolute-value wrappers remain deferred because Complex `Abs` is magnitude rather than a real sign split.
- [agent: codex | model: gpt-5-codex] `EQUATION-ABS-WRAPPER-FORMULA1` makes Real Exact one-layer absolute-value composition the second live generated formula wrapper consumer. It splits `|F(target)|=b` into grouped `F=b` and `F=-b` generated formula branches with global `b\ge0`, collapses exact `b=0` to one branch, preserves denominator exclusions and case-local Cardano/Ferrari facts, and renders grouped `caseMath` with local helper definitions per sign branch. Manual-QA follow-ups added the mode-level shared fallback needed for exact numeric zero RHS such as `|z^3+z+1|=0` and fixed the isolated-worker path so non-`x` Real Cardano/formula fallbacks keep the pre-retargeted equation instead of solving `x`-retargeted shared input against target `z`; exact negative RHS remains domain-empty. Complex absolute-value wrappers, square powers, nested/mixed wrappers, carrier-elimination formula handoff, exp/log/trig wrappers, broad generated formula route widening, schemas, OOE, History, app-state, Tauri, `RootOf`, and implicit-root output remain deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-SQUARE-POWER-WRAPPER-FORMULA-POLICY0` audits the next wrapper formula candidate without source changes. Real one-layer square-power wrappers should start square-only as `F(target)^2=b`, split into `F=\sqrt{b}` and `F=-\sqrt{b}` under `b\ge0`, collapse exact `b=0` to `F=0`, and stop exact `b<0` as domain-empty. The future live milestone should reuse grouped generated formula readback, supply square-power-specific validation evidence, and bundle the small exact-zero generated-branch readback polish. Complex square powers, higher even powers, odd powers, nested/mixed wrappers, exp/log/trig wrappers, broad generated formula route widening, schemas, OOE, History, app-state, and Tauri remain deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-SQUARE-POWER-WRAPPER-FORMULA1` makes Real Exact one-layer square-power composition live as a generated formula wrapper consumer. It splits `F(target)^2=rhs` into grouped `F=\sqrt{rhs}` and `F=-\sqrt{rhs}` branches under target-free `rhs\ge0`, collapses exact zero RHS to one generated branch, stops exact negative RHS as domain-empty, preserves rational denominator exclusions and case-local Cardano/Ferrari facts, and reuses grouped `caseMath` with local helper definitions. The milestone also includes the exact-zero grouped readback polish and a file-size follow-up that extracted the shared algebraic formula fallback into `symbolic-algebraic-formula-fallback.ts` instead of raising the `symbolic.ts` ratchet cap. Complex square powers, higher even powers, odd powers, nested/mixed wrappers, exp/log/trig wrapper formulas, broad generated route widening, schemas, OOE, History, app-state, and Tauri remain deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-ALGEBRAIC-WRAPPER-FORMULA-HARDENING1` adds focused backend and UI regression coverage for the three live Real Exact one-layer algebraic wrapper formula consumers without changing solver visibility. It locks grouped wrapper case labels, local Cardano/Ferrari definition scoping, exact-zero nonredundant display, exact-positive legacy-only versus formula-backed behavior, wrapper facts, copy compatibility, and DisplayPanel `caseMath` rendering. Complex wrappers, higher powers, nested/mixed wrappers, exp/log/trig handoff, schemas, OOE, History, app-state, and Tauri remain unchanged.
- [agent: codex | model: gpt-5-codex] `EQUATION-GENERAL-POWER-WRAPPER-POLICY0` audits future Real `F^n=rhs` and `\sqrt[n]{F}=rhs` wrapper formulas without code changes. The policy recommends the first live follow-up as Real odd-power wrapper formulas because they produce one real odd-root generated branch and avoid even-power sign splitting. Higher even powers need arbitrary even-root grouped readback and scoped dedupe/multiplicity tests; nth-root wrappers need root-carrier detection/readback policy; Complex power/root wrappers remain deferred behind a principal-branch wrapper policy.
- [agent: codex | model: gpt-5-codex] `EQUATION-ODD-POWER-WRAPPER-FORMULA1` makes Real Exact one-layer odd-power composition live for odd exponents `3,5,7,9,11`. It recognizes `Power(F(target), n)`, generates one real odd-root branch `F=\sqrt[n]{rhs}`, allows exact negative RHS, collapses exact zero RHS to `F=0`, preserves denominator exclusions and generated Cardano/Ferrari `caseMath`, and keeps no `rhs\ge0` wrapper fact. Complex odd-power wrappers, nth-root wrappers, higher even powers beyond square-power, nested/mixed wrappers, exp/log/trig wrapper formulas, broad generated route widening, schemas, OOE, History, app-state, and Tauri remain deferred. The file-size follow-up moved new mode regressions into `odd-power-wrapper-formula.test.ts` and kept `composition/core.ts` at the 900-line cap instead of raising baseline caps.
- [agent: codex | model: gpt-5-codex] `EQUATION-HIGHER-EVEN-POWER-WRAPPER-FORMULA1` makes Real Exact one-layer higher even-power composition live for even exponents `4,6,8,10,12`. It extends the composition carrier/fallback/route-profile seams, splits symbolic target-free RHS into grouped `F=\sqrt[n]{rhs}` and `F=-\sqrt[n]{rhs}` branches with `rhs\ge0`, collapses exact zero RHS to `F=0`, stops exact negative RHS as domain-empty, preserves rational denominator exclusions, and promotes formula-backed branches through `Even-Power Formula Cases` grouped `caseMath`. Existing square-power `n=2`, square-root, absolute-value, odd-power, top-level Cardano, and top-level Ferrari behavior remain stable. Complex even-power wrappers, nth-root wrappers, nested/mixed algebraic wrappers, exp/log/trig formula handoff, broad generated route widening, schemas, OOE, History, app-state, and Tauri remain deferred.
