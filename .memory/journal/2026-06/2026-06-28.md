# 2026-06-28

- [agent: codex | model: gpt-5-codex] User approved the Complex wrapper NEXT5 cadence as commit-after-each-verified-milestone. `EQUATION-COMPLEX-PRINCIPAL-IMAGE-INEQUALITY-SUBSTRATE1` is the first commit in that cadence: it remains a behavior-invisible principal-root image fact substrate, with visible Complex root-wrapper solving reserved for the next milestone.
- [agent: codex | model: gpt-5-codex] `EQUATION-COMPLEX-ROOT-WRAPPER-PRINCIPAL-IMAGE1` enables one-layer Complex principal root wrappers for `sqrt(F)=R`, `root(F,n)=R`, and affine shells `A*root(F,n)+C=R` when the isolated root value is inside or guarded by the principal-root image and the powered carrier equation stays inside compact Complex routes. Exact outside-image values stop with a controlled error; generated Complex Cardano/Ferrari wrapper readback, visible `RootOf`, abs wrappers, nested wrappers, mixed algebraic wrappers, Display, Formula Viewer, Copy Result, History, OOE, app-state, Tauri, and persisted schemas remain unchanged.
- [agent: codex | model: gpt-5-codex] `EQUATION-COMPLEX-ABS-WRAPPER-POLICY0` locks Complex absolute-value wrappers as magnitude/locus semantics through focused tests only. Complex `|F|=R` remains unsupported with clear locus wording and no Real sign-split, formula-section, finite-branch, or `RootOf` leakage; Complex Off keeps the existing real-domain absolute-value path.
- [agent: codex | model: gpt-5-codex] `EQUATION-COMPLEX-NESTED-WRAPPER-SUBSTRATE1` adds an internal/test-facing depth-2 Complex nested wrapper readiness substrate for algebraic root/power chains. It records layer provenance, required principal-image facts, all-branch power definitions, generated final equations, and compact-route eligibility, while keeping visible nested Complex wrapper route/readback output deferred.
- [agent: codex | model: gpt-5-codex] `EQUATION-COMPLEX-MIXED-ALGEBRAIC-WRAPPER-CATCHUP1` enables the first compact Complex mixed algebraic root-wrapper route: one selected-target principal root carrier may be mixed with a bounded algebraic selected-target companion when powering the isolated principal-root value yields a compact branch equation. The route preserves principal-image facts for target-dependent values such as `b-z`, emits bounded finite Complex branch output, and keeps two-root carriers, nested mixed radicals, abs companions, generated Complex Cardano/Ferrari formulas, visible `RootOf`, and non-Exact routes deferred.
- [agent: codex | model: gpt-5-codex] `CALCULUS-INTEGRAND-EQUATION-GUARD1` adds a relation-root guard before Calculus symbolic integration routing. Inputs such as `a*x+b*y=e` now stop with a controlled expression-not-equation error, while ordinary expression inputs still use the existing symbolic integration and guarded Compute Engine fallback pipeline.
- [agent: codex | model: gpt-5-codex] `RISCH-NORMAN-LOG-CORRECTION1` generalizes affine-log integration by parts to target-free symbolic polynomial factors `P(v)ln(a*v+b)` and `P(v)log(a*v+b)` under the RN dispatch probe. The implementation uses a bounded affine-substitution finite sum, carries visible slope/domain facts, keeps public strategy as `integration-by-parts`, and rejects non-affine or branch-sensitive log cases cleanly.
- [agent: codex | model: gpt-5-codex] `RISCH-NORMAN-LINEAR-COMBINATION1` adds a guarded final additive combiner after whole-integrand routes miss. It resolves every term through existing Tier-I/RN routes, merges exact-supplement facts, keeps homogeneous strategies where possible, uses `integration-by-parts` for mixed sums, and refuses partial unsupported sums.
- [agent: codex | model: gpt-5-codex] `RISCH-NORMAN-EXP-SINCOS-MIXED1` adds guarded RN adoption for bounded `P(v)e^(a*v+b)sin(c*v+d)` and `P(v)e^(a*v+b)cos(c*v+d)` products. The implementation solves in the `e^u(Qsin(w)+Rcos(w))` span, emits nonzero pivot facts, keeps public strategy as `integration-by-parts`, and keeps unsupported towers/extra factors stopped.
- [agent: codex | model: gpt-5-codex] `RISCH-NORMAN-SYMBOLIC-TRIG-PRODUCTS1` adds symbolic affine two-factor `sin/cos` product-to-sum support under the existing `direct-rule` route. It emits nonzero denominator facts, supports structural same-argument identities, and leaves unresolved symbolic degenerate same-slope products unsupported.
