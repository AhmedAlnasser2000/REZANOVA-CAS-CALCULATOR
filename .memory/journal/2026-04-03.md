- 2026-04-03: Implemented `SX1.1` outboard side-surface correction. `Settings` and `History` now share a right-side rail outside the calculator shell when real gutter space is available, and fall back to a right overlay when it is not.
- 2026-04-03: Added a shared side-surface host in `src/AppMain.tsx`, extracted `src/components/HistoryPanel.tsx`, and updated shell CSS so opening settings/history no longer truncates the calculator workspace.
- 2026-04-03: Verified `SX1.1` with `npm run test:gate`, including new jsdom and Playwright checks for outboard-vs-overlay presentation and shell-width stability.
