- Implemented `ARCH3` as a parity-safe shared runtime-envelope pass for `Calculate` and `Equation`.
- Added `src/lib/kernel/runtime-envelope.ts` so both mode runtimes now share `DisplayOutcome` assembly, planner-badge attachment, resolved-input handling, and optional internal advisory attachment instead of keeping parallel mode-local envelope helpers.
- Added minimal internal runtime advisories on `DisplayOutcome` in `src/types/calculator/display-types.ts` and used them only for Equation numeric-solve eligibility (`blocked/range-guard`, `blocked/invalid-request`, `manual-only`, `suggest-on-error`).
- Rewired `src/lib/modes/calculate.ts` and `src/lib/modes/equation.ts` to use the shared envelope helpers without changing visible titles, prompts, badges, or result wording.
- Replaced Equation numeric-solve controller string matching in `src/app/logic/runtimeControllers.ts` with advisory-driven gating while keeping the same panel behavior.
- Added focused ARCH3 coverage in `src/lib/kernel/runtime-envelope.test.ts`, `src/app/logic/runtimeControllers.test.ts`, and `src/lib/modes/equation.test.ts`.
- Verified with `npm run test:gate`.
