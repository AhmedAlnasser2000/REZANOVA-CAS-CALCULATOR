# 2026-07-04

- [agent: codex | model: gpt-5-codex] Landed Calculus integration recognition/readback fix: scalar-multiple retry, generated-LaTeX negation/product cleanup, first-200 ledger updates for 11 formerly unsupported rows, backend sweep evidence `200/200`, and Playwright visual evidence for scalar-multiple plus readback clarity outputs.
- [agent: codex | model: gpt-5-codex] Prepared Equation output polish gate: Complex On now preserves validated real exact answers with a boundary evidence card, the top output-style toggle reads `Display Exact/Both/Decimal` and drives exact/approx display, Polynomial 2x2 answers render clean solution-pair rows, and Playwright evidence covers trig fallback, exp/log display modes, and polynomial-system rows.
- [agent: codex | model: gpt-5-codex | primary_agent: codex | primary_agent_model: gpt-5-codex | attribution_basis: live] Implemented `CALCULUS-LIMITS-PIECEWISE-EDITOR-REPAIR2`: repaired Limit Piecewise row editor drag handling so the six-dot handle owns reorder, normalized glued pasted friendly branches such as `-1ifx<0;1otherwise`, stripped accidental leading `if` from condition fields, and preserved canonical cases output for readback/copy/evaluation.
- [agent: codex | model: gpt-5-codex | primary_agent: codex | primary_agent_model: gpt-5-codex | attribution_basis: live] Implemented `GUIDE-PAGE-SURFACE1`: ran backed-up Git GC maintenance, added `guide-page` as a protected singleton app page, moved Guide opening/focusing through Workspace Tabs and `ActiveSurfaceHost`, rehosted existing Guide content outside `.calculator-shell`, and verified Guide no longer brings quick inspectors or Order of Execution runtime context.
- [agent: codex | model: gpt-5-codex | primary_agent: codex | primary_agent_model: gpt-5-codex | attribution_basis: live] Implemented `INPUT-PASTE-CANONICALIZATION1`: input/editor paste now canonicalizes safe top-level ASCII `/` and `*` plus textbook function powers before insertion, so Calculus integration paste cases such as `1/2*(csc^2(x)-csc(x)cot(x))` and `sqrt(x)/2+2/sqrt(x)` render as structured fractions/products instead of ambiguous raw text.
