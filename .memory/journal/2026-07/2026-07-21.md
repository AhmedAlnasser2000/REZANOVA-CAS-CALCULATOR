# 2026-07-21

## GRAPHING-COMPLEX-MAPPING-SOLVER1

- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Completed Move 26: added structured general complex mappings and distinct real-parameterized Argand trajectories, graph-local assumptions, and a reviewed public Equation complex evaluation/branch seam guarded by an exact allowlist ratchet.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Added bounded continuous domain-color tiles, synchronized Re/Im/magnitude/phase component maps, accessible alternate phase colors, visible certified principal cuts/points, real-axis slices in Both mode, and independent Real/Complex/Both pane state.
- [agent: codex | model: gpt-5.6 | agent_family: sol | primary_agent: codex | primary_agent_model: gpt-5.6 | primary_agent_family: sol | attribution_basis: live] Analyze now reports exact or bounded validated complex zeros/poles/branch points with explicit search bounds and non-completeness evidence. Focused Graph, complex evaluator, UI, TypeScript, production-build, boundary, and Chromium checks pass; `ln(-x)` and `sqrt(-x)` retain their valid real domains.

## v0.3.0 Release Preparation

- [agent: claude | model: claude-fable-5 | attribution_basis: live] Bumped version to 0.3.0 in package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, src-tauri/Cargo.lock, and README's Project status line, ahead of a planned v0.3.0 GitHub Release (not just a tag).
- [agent: claude | model: claude-fable-5 | attribution_basis: live] Drafted CHANGELOG.md v0.3.0 entry grouped by product area. First draft used "Added" headers for every area; user flagged this as inaccurate since most workspaces predate v0.2.0. Corrected by checking `git ls-tree v0.2.0` vs `HEAD` file counts per `src/lib/*` module: Graphing and Notebook are genuinely new (0 files at v0.2.0), Equation/Calculus/Linear Algebra/Statistics already existed and were substantially expanded. Headers and a file-count evidence table now reflect this distinction.
- [agent: claude | model: claude-fable-5 | attribution_basis: live] Added README header logo (docs/assets/branding/logo.png, user-provided) and License/Release/Last-commit/Platform badges plus Linux-only AppImage/.deb/.rpm download buttons scoped to what's actually released — no Windows/macOS/Discord/npm badges, matching user confirmation that none of those exist yet.
- [agent: claude | model: claude-fable-5 | attribution_basis: live] Diagnosed a Cloudflare Pages clone failure ("error occurred while updating repository submodules"): a broken gitlink at `playground/sources/editors/libreoffice/mirror/core` (mode 160000, commit f09d661a) with no `.gitmodules` entry, despite the path already being covered by this repo's own `.gitignore` mirror-ignore rule. Untracked via `git rm --cached -r` (local files left in place).
- [agent: claude | model: claude-fable-5 | attribution_basis: live] Ran local release-gate checks ahead of the v0.3.0 tag/release: memory protocol, lint, frontend build, and `cargo check` all passed clean. `npm run test:unit` was left running in the background per user direction; see open-questions for the correlated CI hang.

## Cloudflare Pages Deploy Follow-up

- [agent: claude | model: claude-fable-5 | attribution_basis: live] First Cloudflare deploy attempt failed on JS heap OOM during `vite build`; fixed by setting `NODE_OPTIONS=--max-old-space-size=4096` as a build-scoped (not runtime-scoped) environment variable in Cloudflare's dashboard. Deploy then succeeded; verified live at https://rezanova-cas-calculator.ahmed-2007-fn.workers.dev via curl (200 on root and on referenced JS assets) and user confirmation.
- [agent: claude | model: claude-fable-5 | attribution_basis: live] Cloudflare auto-suggested a `wrangler.jsonc` on a separate `cloudflare/workers-autoconfig` branch, bundled with `wrangler`/`@cloudflare/vite-plugin` devDependencies. User chose to hand-add just the config file (with `workers_dev` explicitly `true` to remove ambiguity, plus `assets.not_found_handling: single-page-application` to fix deep-link 404s) without adopting the dependencies; the bot's branch itself was left alone, unmerged.

## Cloudflare Deploy Fix: Missing assets.directory

- [agent: claude | model: claude-fable-5 | attribution_basis: live] The wrangler.jsonc added in f104cae1 omitted the required `assets.directory` field, so `wrangler deploy` failed with "The 'assets' property in your configuration is missing the required 'directory' property" during the build after push (build itself succeeded in 4m38s; only the deploy step failed). Site stayed live throughout since Cloudflare doesn't replace a working deployment when a new one's deploy step fails. Fixed by adding `"directory": "./dist"` to the assets block.
