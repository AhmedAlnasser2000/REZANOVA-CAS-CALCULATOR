# 2026-07-01

- [agent: codex | model: gpt-5-codex] Implemented `SURFACE-QUERY1`: added pure snapshot-input Surface queries for current result summary, workspace info, and safe settings with optional angle unit only. The dispatcher validates protocol version, workspace, and query kind without throwing and keeps History, Variables, host commands, mounting, Graphing, app-global state facades, and raw Display internals out of scope.
- [agent: codex | model: gpt-5-codex] Implemented `SURFACE-EVENT-ADAPTER1`: added a read-only Surface lifecycle adapter over the existing Order of Execution event outbox for Calculate and Equation, mapping only job started, result committed, stale result dropped, job cancelled, and job failed facts while stripping raw payloads, host/plan internals, diagnostics, preflight, unsupported workspaces, and host commands.
- [agent: codex | model: gpt-5-codex] Implemented `SURFACE-VERSIONING-ERRORS1`: added supported-version and query-kind constants plus non-throwing structured validation helpers for protocol version, workspace kind, query kind, and unsupported fields, keeping unsupported Surface requests fail-closed until later dedicated milestones.
- [agent: codex | model: gpt-5-codex] Implemented `SURFACE-CAPABILITY-MANIFEST1`: added the first Surface Protocol capability manifest for Calculate and Equation only, advertising compact result summaries, lifecycle events, and current-result queries while keeping host commands, mounting, History, Variables, Graphing, tabs, plugins, remote compute, and external software development kit surfaces disabled.
- [agent: codex | model: gpt-5-codex] Implemented `SURFACE-DTO-FIREWALL1`: created the hostless `src/lib/surface-protocol/` boundary with versioned compact result DTOs, result/failure wrappers, empty-result summaries, and a pure `DisplayOutcome` to Surface result summary mapper for Calculate and Equation only; the mapper keeps Display block trees, solver/runtime objects, diagnostics, History, Variables, Graphing, mounting, host commands, plugins, remote compute, and external software development kit work out of scope.
- [agent: codex | model: gpt-5-codex | primary_agent: codex | primary_agent_model: gpt-5-codex | attribution_basis: live] `EQUATION-NUMERIC-KERNEL-CONTRACT1` added an internal real root-refinement kernel contract under Equation numeric interval solving. Existing interval/manual/auto-search behavior remains Brent-Dekker-style for this slice, but the refiner now reports method id, termination, residual, iteration, and evaluation evidence through a single seam before the planned ITP replacement.
- [agent: codex | model: gpt-5-codex | primary_agent: codex | primary_agent_model: gpt-5-codex | attribution_basis: live] `EQUATION-NUMERIC-ITP-KERNEL1` replaced Equation's sign-change bracket refiner with ITP under the internal numeric kernel contract. Numeric interval/manual/auto-search routes keep their existing eligibility and validation behavior, while visible method wording now names `Bracket-first adaptive ITP + local-minimum recovery`.
