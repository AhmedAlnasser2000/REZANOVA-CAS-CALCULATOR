# 2026-05-25

## Tauri Watch-Limit Preflight

- [agent: codex | model: gpt-5.5] Reproduced the Linux Tauri launch failure where `npx tauri dev --no-watch` still attempted to watch `src-tauri/Cargo.toml` and hit the host inotify limit.
- Extended `tools/check-tauri-linux-deps.mjs` so `npm run tauri:dev` checks Linux file-watch limits before Tauri starts and points to the one-command `npm run fix:linux-watch-limits` repair when limits are too low.
- Added `tools/fix-linux-watch-limits.mjs` so the host inotify repair does not require manually pasting a heredoc.
- Added preflight tests for insufficient file-watch limits and updated README/runtime-validation docs to describe the repair path.
- Added local VS Code watcher/search excludes for ignored source mirrors, `src-tauri/target`, and generated output so the editor consumes fewer watcher slots in this workspace.

Verification:

- `npm run test:launch-preflight`
- `npm run tauri:dev` now fails early with the clearer inotify repair message on the current host limits.
- `npm run pretauri:build` still passes without enforcing dev-only file-watch limits.
- `npm run lint`

## VARIABLE-MEMORY2

- [agent: codex | model: gpt-5.5] Implemented stored-value adoption beyond standard Calculate while preserving explicit variable roles.
- Added protected-name substitution support so Table, Calculus, Advanced Calc, and Equation numeric solve can substitute stored parameters without replacing active variables, bound variables, or selected solve targets.
- Wired Table, Basic Calculate calculus expressions, Advanced Calc workbenches, and Equation numeric solve to show `Stored Values` details and persist substitution snapshots where successful.
- Preserved Equation symbolic behavior: selected-target parameterized solving continues to treat non-target symbols as symbolic parameters, not stored values.

Verification:

- `npm run test:unit -- src/lib/algebra/variable-memory.test.ts src/lib/modes/table.test.ts src/lib/modes/calculate.test.ts src/lib/modes/equation.test.ts src/lib/advanced-calc/engine.test.ts src/lib/advanced-calc/integrals.test.ts src/lib/advanced-calc/limits.test.ts src/lib/advanced-calc/partials.test.ts src/lib/app-state/history-schema.test.ts src/app/logic/runtimeControllers.test.ts`

## VARIABLE-READBACK1

- [agent: codex | model: gpt-5.5] Added stored-value readback polish without changing substitution behavior.
- Shared readback now reports used stored values, effective substituted inputs, protected stored variables, and replay snapshot use.
- Concise mode keeps `Stored Values` visible while detailed-only `Variable Policy` sections explain protected table/calculus/Equation target variables.
- Equation numeric no-root wording now scopes stored-value failures to the searched target, interval, and substituted equation.

Verification:

- `npm run test:unit -- src/lib/algebra/variable-memory.test.ts src/lib/display/result-detail-policy.test.ts src/lib/modes/calculate.test.ts src/lib/modes/table.test.ts src/lib/modes/equation.test.ts src/lib/advanced-calc/engine.test.ts src/lib/app-state/history-schema.test.ts`

## VARIABLE-MEMORY3

- [agent: codex | model: gpt-5.5] Completed the stored-value mode-policy pass without adding Equation symbolic substitution.
- Added a shared apply/ignore/unsupported policy helper for stored values and migrated Calculate, Table, Advanced Calc, and Equation numeric routing onto that vocabulary.
- Added detailed-only ignored-value notes for Calculate symbolic transforms and Equation symbolic solve when matching stored values exist.
- Closed the derivative-at-point substitution gap by reconstructing the protected derivative-at-point expression after substituting non-bound parameters, and added Advanced numeric IVP coverage for protected ODE variables.

Verification:

- `npm run test:unit -- src/lib/algebra/variable-memory.test.ts src/lib/modes/calculate.test.ts src/lib/modes/table.test.ts src/lib/modes/equation.test.ts src/lib/advanced-calc/engine.test.ts src/app/logic/runtimeControllers.test.ts`

## EDITOR-VARIABLE-HINTS1

- [agent: codex | model: gpt-5.5] Added visible semantic variable hints over the existing variable-core and stored-value policy.
- Added a shared variable-hint adapter and hint strip component that labels stored values, stored-but-ignored Equation symbolic values, solve targets, symbolic parameters, active/bound variables, reserved functions/constants, ambiguous adjacent letters, and unsupported names.
- Wired hints into the main Calculate/Equation editor plus Table and clear Calculus/Advanced Calc workbench editors where active/bound-variable context is known.
- Preserved parser, solver, substitution, result-origin, badge, history, named-string variable, and algebraic-isolation behavior.

Verification:

- `npm run test:unit -- src/lib/algebra/variable-hints.test.ts src/lib/algebra/variable-core.test.ts src/lib/algebra/variable-memory.test.ts src/lib/modes/equation.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx`

## VARIABLE-READBACK2

- [agent: codex | model: gpt-5.5] Polished selected-target variable-boundary guidance without adding new solving.
- Added target-choice-aware power-isolation copy for cases such as `34x^3-z^2=25`, solve for `x`, where exact solving would need symbolic cube-root isolation while solving for `z` is a better supported direction.
- Preserved generic unsupported exact-family wording for cases without a clearer target-choice hint, such as `z^3+a=0`.
- Kept user-facing readback free of milestone/PARAM wording and preserved Equation symbolic stored-value policy.

Verification:

- `npm run test:unit -- src/lib/modes/equation.test.ts src/lib/equation/equation-parameterized-readback.test.ts src/lib/algebra/variable-core.test.ts src/lib/algebra/variable-memory.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx`

## NAMED-VARIABLES1

- [agent: codex | model: gpt-5.5] Added explicit named variables without changing raw adjacent-letter semantics.
- Added `@name` and `var(name)` normalization to a stable upright named-variable token while preserving raw `hello` as adjacent-letter multiplication with a hint.
- Extended variable-core, variable hints, and stored-variable validation/substitution so stored numeric named values use the existing mode policy surfaces.
- Variables panel now requires explicit syntax for multi-character stored names, stores the normalized bare name, and keeps single-letter raw names unchanged.
- Preserved Equation symbolic stored-value ignore policy, solver priorities, result origins, graphing deferral, `POLY-ELIM2` deferral, and source-mirror boundaries.

Verification:

- `npm run test:unit -- src/lib/algebra/variable-core.test.ts src/lib/algebra/variable-memory.test.ts src/lib/algebra/variable-hints.test.ts src/lib/modes/calculate.test.ts src/lib/modes/table.test.ts src/lib/modes/equation.test.ts src/lib/advanced-calc/engine.test.ts src/lib/app-state/history-schema.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx src/components/VariablesPanel.ui.test.tsx`
- `npm run test:golden`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## NAMED-VARIABLES2

- [agent: codex | model: gpt-5.5] Polished explicit named-variable UX without enabling named symbolic solve targets.
- Added Variables panel insertion: single-letter variables insert raw names, while multi-character named variables insert canonical `@name` syntax.
- Improved named-variable hints so `@mass` and `var(mass)` read as one explicit named variable, while raw `mass` / `hello` are explained as adjacent-letter multiplication.
- Added Equation target-boundary behavior for named-only equations: explicit named variables can be symbolic parameters beside a supported single-letter target, but are not selectable solve targets yet.
- Preserved Equation symbolic stored-value ignore policy, raw adjacent-letter multiplication, solver behavior, result origins, graphing deferral, and `POLY-ELIM2` deferral.

Verification:

- `npm run test:unit -- src/lib/algebra/named-variable.test.ts src/lib/algebra/variable-hints.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/algebra/variable-memory.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx src/components/VariablesPanel.ui.test.tsx`
- `npm run test:golden`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## NAMED-VARIABLES3

- [agent: codex | model: gpt-5.5] Enabled explicit named variables as Equation symbolic solve targets through existing bounded selected-target families.
- `@mass` and `var(mass)` normalize to the named target `mass`, render as upright math text, and can be selected/solved where the current linear/quadratic/rational/trig/composition helper files already support the shape.
- Raw adjacent-letter input such as `mass` remains multiplication, but Equation now exposes parsed single-letter target choices such as `m`, `a`, and `s` when the parse is safe.
- Stored named values remain ignored/protected in Equation symbolic solve; no Equation symbolic stored-value substitution, new solving family, graphing, `POLY-ELIM2`, source-mirror work, or Labs runner work was added.

Verification:

- `npm run test:unit -- src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/algebra/named-variable.test.ts src/lib/algebra/variable-hints.test.ts src/lib/algebra/variable-memory.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx src/components/VariablesPanel.ui.test.tsx`
- `npm run test:golden`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## CI-TIMEOUT1

- [agent: codex | model: gpt-5] Raised the unit-test timeout from `20000` to `55000` ms and added an explicit `55000` ms timeout to the slow bounded rational partial-fraction symbolic integration test that timed out in CI.

Verification:

- `npm run test:unit -- src/lib/symbolic-engine/integration.test.ts`
- `npm run test:memory-protocol`
- `npm run build`
- `npm run lint`

## EDITOR-KEYFLOW1

- [agent: codex | model: gpt-5.5] Polished MathLive editor keyflow without changing parser, solver, variable-policy, or result behavior.
- Disabled MathLive smart-superscript auto-exit so exponent typing remains inside the exponent until the user exits intentionally.
- Added plain-Space handling that inserts visible math spacing while execution trims only harmless trailing spacing commands before running.
- Added whole-field arrow wrapping that lets MathLive attempt nested movement first, so exponent/fraction/root exits are not mistaken for field-end wrapping.
- Disabled MathLive keypress/plonk sounds to avoid Tauri/GStreamer sound-path warnings.

Verification:

- `npm run test:unit -- src/components/MathEditor.test.ts src/app/logic/keypadRouter.test.ts src/lib/input/input-canonicalization.test.ts src/app/logic/runtimeControllers.test.ts`
- `npm run test:ui -- src/components/MathEditor.ui.test.tsx src/AppMain.ui.test.tsx`

## EQUATION-ISOLATION1

- [agent: codex | model: gpt-5.5] Added a bounded one-island selected-target isolation pass.
- New helper `src/lib/equation/equation-selected-target-isolation.ts` peels target-free add/subtract/multiply/divide shells around one target-containing expression, records target-free nonzero facts, and delegates the generated equation to existing selected-target helper files.
- Equation mode now uses the isolation pass after direct selected-target helpers fail and before final boundary readback.
- Supported examples include rational/additive shells around selected-target exp/log, trig, carrier, rational, linear, and explicit named-target expressions.
- Kept cube-root/power-isolation cases unsupported when isolation would otherwise delegate to principal-positive target-in-base power logic, preserving the existing honest readback for cases such as `34x^3-z^2=25` solved for `x`.
- No Equation symbolic stored-value substitution, new solver family, result-origin changes, history-schema changes, graphing, `POLY-ELIM2`, source-mirror work, or Labs runner work was added.

Verification:

- `npm run test:unit -- src/lib/equation/equation-selected-target-isolation.test.ts src/lib/modes/equation.test.ts src/lib/equation/equation-parameterized-readback.test.ts`
- `npm run test:unit -- src/lib/equation/equation-selected-target-isolation.test.ts src/lib/modes/equation.test.ts src/lib/equation/equation-parameterized-exp-log.test.ts src/lib/equation/equation-parameterized-rational.test.ts src/lib/equation/equation-parameterized-composition.test.ts src/lib/equation/equation-parameterized-readback.test.ts src/lib/equation/equation-target.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx`
- `npm run test:golden`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`

## EDITOR-PERF1

- [agent: codex | model: gpt-5] Added a shared editor-analysis boundary for MathLive-adjacent analysis.
- Debounced and guarded variable hints, deferred preview rendering, Equation target discovery, and Calculate/Equation algebra-transform eligibility while keeping editor values and execution current.
- Added last-safe preservation for guarded/error analysis states and focused runtime/UI coverage for the containment path.
- No visible Run/Stop/Restart controls, solver cancellation, OOE wiring, parser changes, solver changes, history schema changes, graphing, `POLY-ELIM2`, source-mirror work, or Labs runner policy changes were added.

Verification:

- `npm run test:unit -- src/lib/editor/editor-analysis-runtime.test.ts`
- `npm run test:ui -- src/components/VariableHintStrip.ui.test.tsx src/components/MathStatic.ui.test.tsx`
- `npm run test:ui -- src/AppMain.ui.test.tsx`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`

## EDITOR-RUNTIME1

- [agent: codex | model: gpt-5] Added display-header `Run`, `Stop`, and `Restart Editor` controls over the shared editor-analysis boundary.
- `Run` resumes editor analysis and delegates to the existing EXE/F1 execution path; `Stop` cancels only queued/deferred editor analysis; `Restart Editor` clears the active editor draft and stale preview/result cards before restarting analysis.
- Header controls are centered, have hover tooltips, and preserve clipboard/computing status priority before editor-analysis status.
- No solver cancellation, OOE wiring, parser changes, solver changes, result schema changes, history schema changes, graphing, `POLY-ELIM2`, source-mirror work, or Labs runner policy changes were added.

Verification:

- `npm run test:unit -- src/lib/editor/editor-analysis-runtime.test.ts`
- `npm run test:ui -- src/components/VariableHintStrip.ui.test.tsx src/components/MathStatic.ui.test.tsx`
- `npm run test:ui -- src/AppMain.ui.test.tsx`
- `npm run test:memory-protocol`
- `npm run lint`
- `npm run build`

## UI-CONTRAST-BACK1

- [agent: codex | model: gpt-5] Applied an app-wide contrast polish for editor-adjacent surfaces.
- Added surface-aware editor tokens so dark workspace MathLive editors, variable hints, and form inputs use light readable ink, while display-panel editors retain LCD dark ink.
- Fixed Equation Home navigation parity by adding Back/F5 and making Back/Esc return to the launcher when Equation Home has no parent route.
- Preserved parser, solver, result schema, history replay, and editor-analysis runtime behavior.

Verification:

- `npm run test:unit -- src/lib/equation/equation-navigation.test.ts src/lib/equation/equation-ux.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx -t "backs out of Equation Home"`
- `npm run test:ui -- src/AppMain.ui.test.tsx`
- `npm run lint`
- `npm run build`
- `npm run test:memory-protocol`

## RESULT-CLARITY1

- [agent: codex | model: gpt-5] Added shared result-card hierarchy and LCD chip contrast polish.
- Result cards now label exact output as `Answer` and supplement restrictions as `Valid when`, keeping method/context/fact detail sections below the main result.
- Display-only supplement cleanup removes redundant `Conditions:`/`Exclusions:` prefixes without changing raw solver data, copy behavior, history, parser, or solver behavior.
- LCD route/result chips now use readable LCD-scoped contrast, and exact/supplement math rows no longer vertically clip tall radical output.

Verification:

- `npm run test:unit -- src/lib/display/result-readback.test.ts`
- `npm run test:ui -- src/AppMain.ui.test.tsx -t "renders Calculate exact results and exclusion supplements|solves symbolic-base exp-log equations|keeps assumption details concise"`
- `npm run test:ui -- src/AppMain.ui.test.tsx -t "renders COMP4 nonlinear-in-k|renders COMP10 quadratic periodic carriers|renders COMP10 quadratic sawtooth"`
- `npm run test:ui -- src/AppMain.ui.test.tsx`
- `npm run lint`
- `npm run build`
- `npm run test:memory-protocol`

## APP-MEMORY1

- [agent: codex | model: gpt-5] Added calculator-memory autosave and restore for active calculator work.
- First launch now opens an empty Calculate editor, Equation symbolic starts empty, and display editor placeholders use readable plain text instead of `\text{...}` placeholders.
- Settings now expose `Save Calculator Memory`, autosave mode, a clamped 20-second-minimum interval, `Reset History`, and `Reset Calculator Memory`.
- Browser preview uses a versioned localStorage fallback; desktop Tauri extends the existing `calculator-state.json` and keeps the current identifier.
- No parser, solver, result schema, history replay, OOE, or job-cancellation behavior changed.

Verification:

- `npm run test:unit -- src/lib/app-state/settings.test.ts src/lib/app-state/tauri.test.ts`
- `npm run test:ui -- src/components/SettingsPanel.ui.test.tsx src/AppMain.ui.test.tsx`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `npm run lint`
- `npm run test:memory-protocol`
- `npm run build`
