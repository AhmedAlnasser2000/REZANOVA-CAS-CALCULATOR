# Journal - 2026-05-27

## APP-MEMORY1 Follow-Up

- [agent: codex | model: gpt-5] Ran a live Tauri dev quit/reopen calculator-memory check after the initial APP-MEMORY1 implementation.
- The live check exposed a restore bug: invalid legacy entries inside `calculatorMemory.history` caused the whole saved session snapshot to be rejected.
- Fixed calculator-memory parsing to sanitize/drop invalid memory-history rows before validating the rest of the snapshot, and made startup hydration restore valid memory even if non-critical bootstrap/history reads are dirty.
- The first live check verified full draft restore, but user review found that restoring heavy Equation sessions can make startup unresponsive.
- Updated APP-MEMORY1 to core-only calculator memory: settings, history, stored variables, and `Ans` remain durable, while saved drafts, result cards, mode/screen routes, and workbench session state are no longer restored on startup.
- Startup now intentionally opens a clean empty Calculate editor even when older saved memory contains heavy Equation/result session data.
- Added REZANOVA identity polish so the browser title and Tauri product/window title use `REZANOVA CLASSWIZ CALCULATOR`.
- Moved the static identity coverage to `npm run test:app-identity` so `npm run build` remains clean under the frontend TypeScript config.
- Re-ran a live Tauri quit/reopen check with a seeded heavy Equation session and confirmed both launches opened an empty Calculate editor under the REZANOVA title; the user's pre-check desktop state file was restored afterward.
- Restored the user's original desktop `calculator-state.json` from the pre-test backup after verification.

## EQUATION-ALGEBRAIC-ISOLATION1

- [agent: codex | model: gpt-5] Added bounded selected-target algebraic isolation for cube/fourth-power equations with real even-root branches and target-free shell facts.
- Wired `EQUATION-ISOLATION1` and selected-target composition branch handoff to delegate generated algebraic equations into the new helper.
- Former unsupported cube-root cases such as `34x^3-z^2=25`, solve for `x`, now solve symbolically; broad symbolic cubic/quartic formulas remain capped with controlled guidance.

## POLY-ELIM2

- [agent: codex | model: gpt-5] Added a backend-only bivariate resultant projection core over exact rational polynomial inputs.
- The projection substrate represents inputs as polynomials in an eliminated variable with exact univariate retained-variable coefficients, then returns a deterministic primitive univariate resultant.
- Stored finite numeric constants may substitute as exact rationals while retained/eliminated variables stay protected; Equation/UI/product polynomial-system adoption remains out of scope.

## POLY-SYSTEM1

- [agent: codex | model: gpt-5] Added Equation > Simultaneous > `Polynomial 2x2` as the first product-facing consumer of `POLY-ELIM2`.
- The new branch uses two MathLive editor rows for fixed variables `x` and `y`, projects with bounded bivariate resultants, solves real univariate projections, back-substitutes, and validates candidate pairs before showing `Answer` plus projection/candidate details.
- Stored finite numeric constants may substitute into coefficients while `x` and `y` stay protected; underconstrained x-only/y-only inputs now stop with clearer guidance.
- MathLive operator-adjacent spacing is normalized before preview/execution, and nonzero constant-resultant systems such as `y=x^2+44; y=x^2+5` now report no real solution pairs instead of generic projection ambiguity.
- Existing linear 2x2/3x3 solving remains unchanged; no Grobner bases, complex pairs, inequalities, 3x3 polynomial systems, graphing, source-mirror execution, Labs runner work, result schema change, or history schema change was added.

## OOE Traffic-Control And Observability Roadmap Update

- [agent: codex | model: gpt-5] Updated the OOE roadmap/evaluation to capture the intended traffic-controller meaning of Order Of Execution.
- Recorded that OOE should eventually cover execution priority, main-thread safety, cancellability, stale-result commit policy, trace events, diagnostic buffers, and developer/agent observability across editor, analysis, preview, rendering, persistence, and solver work.
- Added future observability direction for a bounded trace buffer, developer diagnostics/export surface, and a later local-dev-only read-only MCP diagnostics bridge.
- Preserved the first-gate boundary: `OOE-RS0` and `OOE-RS1` remain readiness/docs plus Rust schema/pure validation only, with no runtime routing, solver changes, MCP server, real cancellation, chunk scheduler, checkpoint ledger, or Progressive Solver implementation bundled into the first OOE gates.

## BUNDLE-SPLIT1 Startup Bundle Splitting

- [agent: codex | model: gpt-5] Implemented the pre-OOE startup bundle split and measurement pass.
- Added a manifest-based bundle-size report/check script plus `npm run build:analyze` and `npm run test:bundle-size`.
- Added stable Vite vendor chunks for React, MathLive, Compute Engine, Tauri API, Zod, and remaining vendor code.
- Lazy-loaded non-initial workspaces and side surfaces, moved Table/runtime execution and exact algebra-transform eligibility behind dynamic imports, and extracted light UI-only helpers from heavier Equation/variable/display modules.
- Measured result: eager startup JS is now `1519.65 kB` raw / `411.13 kB` gzip, with the largest app chunk at `470.44 kB` raw; the previous startup app chunk was about `3370.44 kB` raw / `893.22 kB` gzip.
- Preserved the boundary: no OOE runtime scheduling, cancellation, solver behavior, result schema, history schema, or variable-policy changes were included.

## OOE-RS0 Architecture Readiness

- [agent: codex | model: gpt-5] Implemented `OOE-RS0` as architecture/readiness only.
- Added `.memory/research/architecture/ooe-rs0-readiness-audit.md` to record current execution seams and RS1 inputs.
- Audited kernel capabilities/hosts, runtime profiles/budgets, stop/advisory policy, runtime envelopes, guarded Equation stage order/trace shape, editor-analysis runtime, BUNDLE-SPLIT1 boundary, and the current Rust entrypoint shape.
- Updated the OOE roadmap so `OOE-RS0` is implemented and `OOE-RS1` remains the first code milestone: Rust schema plus pure validation under `src-tauri/src/ooe/`.
- Preserved the boundary: no Rust OOE module, Tauri command, TypeScript bridge, runtime routing, scheduling, cancellation, trace buffer, MCP server, solver behavior, UI behavior, Progressive Solver, source-mirror, Playground, Labs runner, or remote execution was added.
