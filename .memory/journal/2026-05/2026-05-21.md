# Journal: 2026-05-21

## APPMAIN-SLIM1
- [agent: codex | model: gpt-5.5] Completed `APPMAIN-SLIM1` as a render-shell extraction and Statistics workspace parity pass. `src/AppMain.tsx` dropped from `8140` lines to `6973` lines while keeping AppMain as the orchestration owner for state, refs, routing, history replay, execution, keyboard handling, and top-level shell coordination.
- Extracted render-only shell boundaries under `src/app/shell/`: `DisplayPanel`, `ModeStrip`, `SoftMenu`, `KeypadPanel`, `LauncherWorkspace`, and `SideSurfaceHost`; wired `StatisticsWorkspace` with mean-inference/source-sync parity.
- Verification passed locally: targeted navigation/router unit tests, `test:golden`, `test:ui`, `lint`, `build`, `test:memory-protocol`, `e2e/qa1-smoke.spec.ts`, and `e2e/calc-audit0-smoke.spec.ts`.

## APPMAIN-SLIM2
- [agent: codex | model: gpt-5.5] Completed `APPMAIN-SLIM2` as a controller/handler boundary extraction pass. `src/AppMain.tsx` dropped from `6973` lines to `6094` lines by wiring the typed soft-action/keypad routers and moving window keyboard dispatch to `src/app/logic/windowKeyRouter.ts`.
- AppMain still owns state, refs, effects, history replay, execution handlers, keyboard listener registration, and shell orchestration; broad `@ts-nocheck` flow/mode handler files remain out of the stable boundary.
- Verification passed locally: targeted navigation/router unit tests, `test:golden`, `test:ui`, `lint`, `build`, `test:memory-protocol`, `e2e/qa1-smoke.spec.ts`, and `e2e/calc-audit0-smoke.spec.ts` after a transient LIM3 first-run assertion passed on focused and full reruns without code changes.

## APPMAIN-SLIM3
- [agent: codex | model: gpt-5.5] Completed `APPMAIN-SLIM3` as a low-risk runtime hooks and shell state grouping pass. `src/AppMain.tsx` dropped from `6094` lines to `5619` lines.
- Added `useSideSurfaceRuntime`, `useLauncherRuntime`, and `useShellFocusRuntime`; AppMain still owns current mode, settings, history entries, display outcome, mode-specific workbench state, refs, execution handlers, history replay, keyboard listener registration, and render composition.
- Verification passed locally: targeted navigation/router unit tests, `test:golden`, `test:ui`, `lint`, `build`, `e2e/qa1-smoke.spec.ts`, and `e2e/calc-audit0-smoke.spec.ts`; one earlier parallel Playwright attempt failed only because both suites tried to claim the preview port.

## APPMAIN-SLIM4
- [agent: codex | model: gpt-5.5] Implemented `APPMAIN-SLIM4` as a Matrix/Vector/Table runtime-hook extraction pass. `src/AppMain.tsx` dropped from `5619` lines to `5501` lines.
- Added `useLinearAlgebraRuntime`, `useTableRuntime`, and a view-only `LinearAlgebraTableWorkspaceHost`; AppMain keeps refs, global mode routing, keyboard registration, history replay, display outcome ownership, and cross-mode orchestration.
- Verification passed locally: targeted router/Matrix/Vector/Table unit tests, `test:golden`, `test:ui`, `lint`, `build`, `test:memory-protocol`, `e2e/qa1-smoke.spec.ts`, and `e2e/calc-audit0-smoke.spec.ts`.

## MEMORY-ORG0
- [agent: codex | model: gpt-5.5] Calendarized `.memory/journal/` and `.memory/sessions/`: dated journal files now live under `YYYY-MM/`, and session dossiers now live under `YYYY-MM/YYYY-MM-DD/` while keeping original leaf dossier names.
- Updated `.memory/PROTOCOL.md`, `.memory/INDEX.md`, journal/session READMEs, and `tools/validate-memory-protocol.mjs` so recursive discovery is canonical and deprecated flat journal/session entries fail validation.

## MEMORY-ORG1
- [agent: codex | model: gpt-5.5] Organized `.memory/research/` into purpose folders: `roadmaps/`, day-calendarized `checklists/YYYY-MM/YYYY-MM-DD/`, `readiness/`, `audits/`, `source-context/fricas/`, `architecture/`, and `references/`.
- Added `.memory/research/README.md` and `.memory/research/INDEX.md`, mechanically updated moved research links, and extended `tools/validate-memory-protocol.mjs` so root-level research files and flat research checklists fail validation.

## LIB-ORG0
- [agent: codex | model: gpt-5.5] Added the root `src/lib` taxonomy audit and `LIB-ORG0` through `LIB-ORG3` roadmap before moving source files.
- Locked the clean import-rewrite approach: moved files should not leave root-level compatibility shims behind.

## LIB-ORG1
- [agent: codex | model: gpt-5.5] Grouped root `src/lib` domain files into existing owner folders: algebra substrates, equation helpers, Matrix/Vector adapters/workbench, and Calculate/core-mode scaffolding.
- Kept tests colocated with moved source files and rewrote imports without root-level compatibility shims.

## LIB-ORG2
- [agent: codex | model: gpt-5.5] Created `src/lib/calculus/` and moved the shared calculus core, workbench, verification, strategy, integration helper, finite-limit target, and limit heuristic modules into it.
- Updated imports, durable path references, and CODEOWNERS so calculus ownership points at the new folder.

## LIB-ORG3
- [agent: codex | model: gpt-5.5] Grouped remaining root `src/lib` shared utilities into `display/`, `numeric/`, `engine/`, `input/`, `app-state/`, and `navigation/`.
- Rewrote imports without root-level compatibility shims and kept `.memory/sources/` verbatim snapshots untouched.

## MEMORY-SESSIONS0
- [agent: codex | model: gpt-5.5] Backfilled `.memory/sessions/2026-05/` with lightweight session dossiers for major May milestone clusters, using existing journal/checklist/research/commit evidence.
- Updated the memory protocol and validator so every journal month with milestone work must have corresponding session-month coverage.

## PGL-VIS1
- [agent: codex | model: gpt-5.5] Implemented `PGL-VIS1` as the first developer-only interactive Labs console for approved local Playground runners.
- Added a Vite dev-runner bridge gated by `VITE_SHOW_LABS=1` and `VITE_ENABLE_LAB_RUNNERS=1`, upgraded Labs with runner-gated equation/expression/corpus-case input, and added the `expression-baseline-probe` Playground record/manifest/lab beside visual execution for `sym-search-planner-ordering`.
- Preserved the one-way rule: stable runtime Labs code imports only `src/lib/labs/*`, runner execution is dev-server-only, release builds do not expose runner execution, and normal calculator history/provenance remains unchanged.

## PGL-VIS1-POLISH
- [agent: codex | model: gpt-5.5] Polished the interactive Labs console so Labs mode no longer shows an empty or stale normal calculator display.
- Added `useLabsRuntime` to share selected runner, input kind, corpus case, custom input, run status, and run result between the Labs control panel and the top display.
- Updated `DisplayPanel` so Labs gets a developer-only live preview with runner/input/result state and suppresses normal Calculate/Table/Equation result cards while in Labs mode.
- Updated Labs comparison rows to render math through `MathStatic`, keeping raw LaTeX out of the visible main comparison table while retaining raw data in details/accessibility surfaces.
- Verification passed locally: Labs/AppMain UI suites, Labs catalog validation, Playground tests, memory protocol, lint, and build.

## INCUBATION-INFRA Roadmap Capture
- [agent: codex | model: gpt-5.5] Preserved the external incubation-infrastructure prompt verbatim in `.memory/sources/2026-05-21__incubation-infrastructure-upgrade-redefined-codex-prompt.md` and recorded its hash in `.memory/sources/INDEX.md`.
- Added `.memory/research/roadmaps/incubation-infrastructure-roadmap.md` as the interpreted roadmap for source-mirror security, Labs runner policy, area-study synthesis modes, missing-capability gates, and truth-first math output taxonomy.
- Recorded that `FRICAS-CTX0` is complete as a single-source context milestone and future related work should become multi-source capability-area studies, with FriCAS as one evidence source rather than the organizing lane.

## INCUBATION-INFRA1
- [agent: codex | model: gpt-5.5] Implemented `INCUBATION-INFRA1` as incubation guardrail infrastructure before new `AREA-*` capability studies.
- Added source-mirror security tiers and required metadata policy fields, registered GeoGebra as a seventh context mirror, and kept all source mirrors non-executable by default.
- Added Labs runner policy metadata so approved runners are dev-only, no-history, no-remote, and no-source-mirror-execution.
- Added `playground/area-studies/` with lite, standard, full synthesis, and missing-capability gate templates plus `npm run test:area-studies`.
- Wired `test:area-studies` into `test:gate`, `ci-linux`, and `Release Linux`.

## SOURCE-CAPTURE1
- [agent: codex | model: gpt-5.5] Captured the six remaining registered open-source context mirrors as shallow ignored local clones: SymPy, Maxima, SageMath, Giac/XCAS, SymEngine, and GeoGebra.
- Updated source metadata and index so all seven registered mirrors, including existing FriCAS, are active context mirrors with exact capture commits where locally present.
- Preserved source-mirror safety boundaries: static reads only, no execution, no dependency installation, no submodules, no copied source, no Labs runner execution, and no stable `src` dependency.
- Intentionally excluded the previously mentioned TI closed calculator installation from source-mirror registration because this lane is for open-source context mirrors only.

## AREA-POLY-RAT0
- [agent: codex | model: gpt-5.5] Completed `AREA-POLY-RAT0` as the first full multi-source capability-area study over polynomial and rational-function substrates.
- Added `playground/area-studies/studies/area-poly-rat0/` with scope, source notes, cross-source comparison, pattern extraction, Calcwiz-fit evaluation, synthesis, native proposal, benchmark families, and risks.
- Compared Calcwiz `POLY-RAT-CORE0` readiness against FriCAS, SymPy, Maxima, SageMath, Giac/XCAS, SymEngine, and GeoGebra static source-context evidence without executing mirrors or copying code.
- Updated the area-study validator so committed studies under `playground/area-studies/studies/<area-id>/` are validated alongside templates.
- Decision: proceed to bounded `INT-RAT1` next; keep repeated factors, irreducible quadratics, square-free factorization, resultants, Grobner/elimination, and exact linear algebra outside that first slice.

## INT-RAT1
- [agent: codex | model: gpt-5.5] Completed `INT-RAT1` as the first visible consumer of `POLY-RAT-CORE0`.
- Added a bounded `partial-fractions` integration strategy for one-variable exact rational functions whose proper denominator decomposes into distinct rational linear factors.
- Reused `rational-function-core` normalization, shared polynomial division/GCD readiness, and existing derivative-backed antiderivative verification; no calculus-local rational algebra, source mirror execution, or external CAS runtime was added.
- Preserved existing `inverse-trig` and `derivative-ratio` priority and kept repeated factors, irreducible quadratics, square-free factorization, resultants, Grobner/elimination, exact linear algebra, and broad Risch/Liouville work deferred.
- Added/updated coverage across symbolic integration, calculus core, Advanced Calc, Calculate mode, math engine, golden cases, and capability-readiness facts.
