# Journal - 2026-05-24

## EQUATION-PARAM8

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM8` as exclusion-safe rational normalization for selected-target Equation solving.

What changed:

- Strengthened selected-target rational solving so nested rational structures flatten before LCD clearing.
- Added bounded support for nested denominators, parameter-only denominators, rational quotients, and rational sums when the cleared equation stays degree 2 or lower.
- Preserved original denominator exclusions and easy derived nonzero facts through existing supplement/detail readback.
- Added conditional target-cancel output for cases where rational clearing removes the selected target and leaves a parameter condition.
- Kept Guide content unchanged by design.

Boundaries:

- No higher-degree cleared solving.
- No mixed-carrier or composition solving.
- No symbolic-base exp/log solving.
- No variable memory.
- No named string variables.
- No `POLY-ELIM2`.
- No graphing, source-mirror execution, Labs runner work, result-origin changes, or badge changes.

## EQUATION-PARAM9

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM9` as factorable polynomial selected-target Equation solving.

What changed:

- Added a selected-target factorable polynomial helper.
- Supports explicit symbolic zero-products up to degree 4.
- Delegates linear and quadratic factor branches to existing PARAM1/PARAM2 solvers.
- Dedupes repeated roots while preserving multiplicity detail.
- Reuses the existing exact-rational bounded cubic/quartic factor core for expanded helper cases.

Boundaries:

- No general symbolic cubic or quartic formulas.
- No degree greater than 4.
- No partial solution sets from unsupported factors.
- No Guide update.
- No rational widening beyond PARAM8.
- No composition, symbolic-base exp/log, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result-origin changes, or badge changes.

## EQUATION-PARAM10

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM10` as symbolic-base exp/log selected-target Equation solving.

What changed:

- Extended the selected-target exp/log helper to accept symbolic bases with explicit base facts.
- Added target-free symbolic-base examples such as `a^z=b`, `a^{z+c}=d`, `\log_a(z+c)=d`, and same-base symbolic reductions.
- Added direct target-in-base power/log-base solving on the principal positive real branch.
- Preserved base positivity, base-not-one, output positivity, log-argument positivity, exponent nonzero, and branch-domain facts.
- Kept generated equations routed through existing selected-target helper files.

Boundaries:

- No Lambert W.
- No log-combine search.
- No arbitrary mixed exponential-polynomial solving.
- No target in both base and exponent/argument.
- No extra integer/rational exponent branch enumeration.
- No Guide update.
- No composition widening, variable memory, named string variables, `POLY-ELIM2`, graphing, source-mirror execution, Labs runner work, result-origin changes, or badge changes.

## EQUATION-PARAM11

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM11` as bounded one-layer composition handoff plus the PARAM10 generated-power readback fix.

What changed:

- Added a selected-target composition helper for one outer nonperiodic, exp/log, or direct trig carrier.
- Generates branch equations and delegates them to existing selected-target helper files rather than reopening the older x-centric `COMP` lane.
- Supports examples such as `sqrt(z^2+a)=b`, `|z^2-a|=b`, `ln(z^2+a)=b`, `e^(z^2+a)=b`, `sin(z^2+a)=b`, and `cos((z-a)(z-b))=c`.
- Preserves branch/domain facts, trig range facts, integer-family facts, and delegated nonzero/denominator facts.
- Fixed the `a^z=b^z` solve-for-`a` readback so generated powers render as parenthesized power expressions rather than exponent-list notation.

Boundaries:

- No nested/two-layer composition.
- No mixed-carrier equations.
- No broad or deep `COMP` reopening.
- No variable memory.
- No named string variables.
- No `POLY-ELIM2`.
- No graphing, source-mirror execution, Labs runner work, result-origin changes, badge changes, or history schema changes.

## COMP13A

- [agent: codex | model: gpt-5.5] Implemented `COMP13A` as a shared composition-core refactor.

What changed:

- Added a shared composition core for selected-target carrier detection, one-layer branch generation, branch-set provenance, and composition-depth policy.
- Rewired `equation-parameterized-composition.ts` to consume the shared core while preserving PARAM11 behavior.
- Rewired the old guarded `composition-stage.ts` to consume shared branch-set and depth-policy helpers without changing its runtime ownership.
- Recorded that the old COMP engine is valuable and being refactored from inside, not replaced.

Boundaries:

- No new solving families.
- No two-layer or mixed-carrier selected-target wins.
- No variable memory.
- No named string variables.
- No `POLY-ELIM2`.
- No graphing, source-mirror execution, Labs runner work, result-origin changes, badge changes, or history schema changes.

## EQUATION-PARAM12

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM12` as bounded two-layer nested selected-target composition over the shared `composition-core` seam.

What changed:

- Extended `composition-core` with nested carrier-chain matching and branch generation for one selected-target chain of depth 2.
- Preserved one-layer PARAM11 behavior while allowing selected two-layer chains to generate final branch equations and delegate them to existing selected-target helper files.
- Added bounded support for examples such as `sqrt(|z-a|)=b`, `ln(|z-a|)=b`, `e^(sqrt(z+a))=b`, `sqrt(1/(z-a))=b`, `sin(sqrt(z+a))=b`, `sqrt(sin(z+a))=b`, and `sin(tan(z))=a`.
- Distinguishes two independent periodic-family parameters as `n` and `m` when a capped two-periodic chain requires both.

Boundaries:

- No depth-three composition chains.
- No additive mixed-carrier solving such as `sin(z)+sqrt(z)=a`.
- No variable memory.
- No named string variables.
- No `POLY-ELIM2`.
- No graphing, source-mirror execution, Labs runner work, result-origin changes, badge changes, or history schema changes.

## Composition Readback And Result Overflow Polish

- [agent: codex | model: gpt-5.5] Applied a small post-`EQUATION-PARAM12` polish pass for oversized symbolic results and periodic-composition readback.

What changed:

- Result cards now keep long exact LaTeX inside horizontal scroll lanes instead of allowing one oversized formula to stretch or break the app shell.
- Scrollbars are attached to padded result/detail wrappers rather than directly to the math glyph line.
- Common exact inverse-trig constants inside selected-target composition branches now remain exact/unit-native, avoiding raw leaked decimals such as `1.570796...` or `314.159.../\pi` when `arcsin(1)` can be rendered as `\pi/2`, `90`, or `100`.
- Unavoidable long decimal literals in composition readback now honor the configured approximate digit count in a LaTeX-safe decimal form.
- The Equation parameterized roadmap now reserves `EQUATION-PARAM13` for user-facing error/boundary/readback polish before more capability. Additive mixed-carrier selected-target solving moves to `EQUATION-PARAM14`.

Boundaries:

- No new solving families.
- No additive mixed-carrier solving.
- No deeper composition.
- No result-origin, badge, history-schema, variable-memory, named-string variable, `POLY-ELIM2`, graphing, source-mirror, or Labs runner changes.

## EQUATION-PARAM13

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM13` as selected-target Equation error, boundary, and readback polish.

What changed:

- Added shared boundary readback for selected-target parameterized Equation stops.
- Replaced generic selected-target fallback wording with specific user-facing messages for range failures, deeper composition needs, mixed carriers, target-outside-carrier shapes, adjacent-letter ambiguity, branch limits, and unsupported generated branches.
- Renamed fallback detail wording from `Parameterized Boundary` to `Why It Stopped`.
- Added `What To Try` only when the stop has an actionable next step.
- Cleaned the generic exact symbolic unsupported message so it no longer mentions milestones.

Boundaries:

- No new solving families.
- No additive mixed-carrier solving.
- No deeper composition.
- No variable memory.
- No named string variables.
- No `POLY-ELIM2`.
- No graphing, source-mirror execution, Labs runner work, result-origin changes, badge changes, or history schema changes.
