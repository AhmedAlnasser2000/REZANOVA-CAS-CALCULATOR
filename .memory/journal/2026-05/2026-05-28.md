# 2026-05-28

## OOE-RS15 Equation Stale Commit Gate

- [agent: codex | model: gpt-5] Implemented `OOE-RS15` as the second real commit-legality gate, scoped only to existing OOE-covered Equation routes.
- Added canonical Equation snapshot/input-revision helpers and lazy active-revision support to `runEquationModeWithOoePilot`.
- Symbolic Equation solve and Equation numeric-interval solve now ask the OOE envelope whether the completed job is still current before committing.
- Stale Equation completions are dropped silently; stale numeric drops preserve replay substitution snapshots.
- Non-symbolic Equation screens, linear/polynomial systems, coefficient polynomial tools, and algebra transforms do not use the stale gate.
- Recorded the forward traffic-controller path: `OOE-RS16` active job registry, `OOE-RS17` cancellation contract, and `OOE-RS18` editor runtime containment and control lane.
- Preserved boundaries: no cancellation, scheduler, active job registry, UI trace panel, result wording changes, history/result schema changes, solver behavior changes, Rust execution, MCP diagnostics, or Progressive Solver behavior.

## OOE-RS16 Active Job Registry

- [agent: codex | model: gpt-5] Implemented `OOE-RS16` as an internal active job registry for existing OOE pilots.
- Added active and bounded recent lifecycle records for OOE jobs, including completed, stale-dropped, skipped, and failed terminal states.
- Threaded registry recording through standard Calculate expression, shared Equation, and active Table OOE pilots.
- Table remains metadata-only; no Table stale gate was added.
- Preserved boundaries: no cancellation, scheduler, UI diagnostics, trace panel, MCP endpoint, history/result schema changes, solver behavior changes, Rust execution, remote execution, or Progressive Solver behavior.
- Recorded `OOE-RS17` as cancellation contract and `OOE-RS18` as editor runtime containment/control lane.

## OOE-RS17 Cancellation Contract

- [agent: codex | model: gpt-5] Implemented `OOE-RS17` as cancellation contract state only.
- Added cancellation request metadata plus active `cancelRequested` and terminal `cancelled` lifecycle states to the active job registry.
- Added registry helpers to request cancellation by registry ID, request the latest job for a capability, query cancellation state, and mark jobs terminally cancelled.
- Preserved request metadata when current one-shot jobs complete or fail normally.
- Kept Expression, Equation, and Table pilots behavior-neutral; no current work is stopped, skipped, or interrupted.
- Preserved boundaries: no visible Stop button, scheduler, worker isolation, hard interruption, Rust solver migration, trace buffer, MCP diagnostics, history/result schema change, result wording change, or solver behavior change.

## OOE-RS18 Editor Runtime Containment And Control Lane

- [agent: codex | model: gpt-5] Implemented `OOE-RS18` as the first visible OOE editor control lane.
- Added a current-lane helper that maps standard Calculate, Equation symbolic, and active Table surfaces to their latest active OOE jobs for RS17 cancellation requests.
- Display-header `Run`, `Stop`, and `Restart Editor` now coordinate editor analysis state with current-lane cancellation requests.
- Added MathEditor containment and remount-on-generation behavior so editor crashes are contained and restartable.
- Fixed OOE snapshot canonicalization to skip undefined optional fields, avoiding false stale drops for equivalent requests.
- Preserved boundaries: no hard solver interruption, scheduler, worker/iframe sandbox, Rust solver execution, trace panel, MCP diagnostics, history/result schema change, result schema change, or solver output change.
