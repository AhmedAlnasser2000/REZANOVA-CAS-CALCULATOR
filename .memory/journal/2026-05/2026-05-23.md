# Journal - 2026-05-23

## Tauri Dev Watcher Fix

- [agent: codex | model: gpt-5.5] Updated the default desktop dev command so `npm run tauri:dev` runs `npx tauri dev --no-watch`.
- [agent: codex | model: gpt-5.5] Added `npm run tauri:dev:watch` as the explicit Rust hot-reload watcher path for environments with sufficient Linux file-watch limits.
- [agent: codex | model: gpt-5.5] Documented that the default no-watch mode avoids inotify exhaustion caused by the ignored source-mirror research trees.
- [agent: codex | model: gpt-5.5] Recorded that the active checkout is now the top-level Calculator folder, not the old `tests and learn` location; exact local paths remain local/private.

## AREA-POLY-ELIM0

- [agent: codex | model: gpt-5.5] Completed `AREA-POLY-ELIM0` as a study-only milestone for polynomial elimination, resultants, and Grobner-style work.

What changed:

- [agent: codex | model: gpt-5.5] Added a full synthesis area study under `playground/area-studies/studies/area-poly-elim0/`.
- Registered `area-poly-elim0` in the area-study validator and updated the area-study index.
- Studied Calcwiz plus FriCAS, SymPy, Maxima, SageMath, Giac/XCAS, SymEngine, and GeoGebra as static context sources only.
- Recorded the exact next recommendation: `AREA-EXACT-LINEAR-ALGEBRA0` before `POLY-ELIM1`.
- Locked the milestone convention that `0` is for audit/study/surveillance/readiness work and implementation starts at `1`.
- Recorded that graphing remains an endgame product area and should not be proposed again until the calculator is broadly stabilized.

Boundaries:

- No product math behavior changes.
- No source mirror execution.
- No copied source.
- No resultants, Grobner bases, elimination, exact matrix algebra, graphing, UI, solver, calculus, table, or Labs runner changes.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:area-studies`, `npm run test:source-mirrors`, `npm run test:memory-protocol`, `npm run lint`, `npm run build`, and source-mirror ignore checks.

## EXACT-LINEAR-ALGEBRA1

- [agent: codex | model: gpt-5.5] Implemented `EXACT-LINEAR-ALGEBRA1` as a bounded internal exact rational matrix core.

What changed:

- Added exact matrix validation plus determinant, RREF/rank, square solve, and inverse operations under strict dimension and scalar-growth caps.
- Reused the existing number-backed `ExactScalar` shape without adding bigint, modular, algebraic, complex, or symbolic scalar domains.
- Migrated rational-function partial-fraction coefficient solving to the shared exact matrix core.
- Updated capability readiness so `exact-linear-algebra` is now `ready-with-adapter`.
- Kept product Matrix/Vector UI behavior, Equation solving, polynomial elimination, graphing, Labs runners, source execution, and copied source out of scope.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: focused unit slice, `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## AREA-EXACT-LINEAR-ALGEBRA0

- [agent: codex | model: gpt-5.5] Completed `AREA-EXACT-LINEAR-ALGEBRA0` as a study-only milestone for exact scalar, matrix, vector, and row-reduction readiness.

What changed:

- Added a full synthesis area study under `playground/area-studies/studies/area-exact-linear-algebra0/`.
- Registered `area-exact-linear-algebra0` in the area-study validator and updated the area-study index.
- Studied Calcwiz plus FriCAS, SymPy, Maxima, SageMath, Giac/XCAS, SymEngine, and GeoGebra as static context sources only.
- Recorded the exact next recommendation: `EXACT-LINEAR-ALGEBRA1`.
- Kept product Matrix/Vector behavior, solver behavior, polynomial elimination, UI, graphing, Labs runners, source execution, and copied source out of scope.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:area-studies`, `npm run test:source-mirrors`, `npm run test:memory-protocol`, `npm run lint`, `npm run build`, and source-mirror ignore checks.

## POLY-ELIM1

- [agent: codex | model: gpt-5.5] Implemented `POLY-ELIM1` as a bounded internal scalar univariate resultant core.

What changed:

- Added `src/lib/algebra/polynomial-elimination-core.ts`.
- Added Sylvester matrix construction for same-variable positive-degree exact polynomials.
- Added exact resultant computation by delegating the Sylvester determinant to `determinantExactMatrix`.
- Added structured stops for variable mismatch, zero polynomial, constant polynomial, Sylvester dimension limits, and exact-matrix determinant/growth failures.
- Updated capability readiness so `polynomial-elimination-core` is `ready-with-adapter`.

Boundaries:

- No Grobner bases.
- No bivariate or multivariate elimination.
- No Equation solver adoption.
- No Matrix UI exact mode.
- No graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: focused unit slice, `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## Multivariable Variable Policy Roadmap

- [agent: codex | model: gpt-5.5] Created a dedicated roadmap for multivariable recognition, explicit solve-target selection, and future variable memory policy.

What changed:

- Added `.memory/research/roadmaps/multivariable-variable-policy-roadmap.md`.
- Cross-linked the roadmap from `poly-rat-native-roadmap.md`.
- Recorded that bivariate elimination and `POLY-ELIM2` are blocked on app-wide variable semantics, not only on polynomial algebra.
- Locked the rule that stored variable values must never silently override solve targets.

Boundaries:

- No app runtime changes.
- No math behavior changes.
- No variable memory implementation.
- No Equation target-selection UI yet.
- No bivariate elimination, Grobner bases, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:memory-protocol`.

## AREA-MULTIVAR0

- [agent: codex | model: gpt-5.5] Completed `AREA-MULTIVAR0` as a study-only milestone for variable semantics and multivariable readiness.

What changed:

- Added `playground/area-studies/studies/area-multivar0/`.
- Registered `area-multivar0` in the area-study validator and updated the area-study index.
- Studied Calcwiz plus FriCAS, SymPy, Maxima, SageMath, Giac/XCAS, SymEngine, and GeoGebra as static context sources only.
- Recorded the exact next recommendation: `VARIABLE-CORE1`.
- Recorded that stored numeric variables must remain distinct from solve targets.
- Kept `POLY-ELIM2` blocked until variable roles are explicit.

Boundaries:

- No runtime behavior changes.
- No variable memory implementation.
- No Equation target-selection UI.
- No multivariable solving.
- No bivariate resultants, Grobner bases, polynomial-system solving, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:area-studies`, `npm run test:source-mirrors`, `npm run test:memory-protocol`, `npm run lint`, `npm run build`, and source-mirror containment checks.

## Variable Identifier Policy Refinement

- [agent: codex | model: gpt-5.5] Recorded the post-`AREA-MULTIVAR0` identifier policy for future `VARIABLE-CORE1` planning.

What changed:

- Locked case sensitivity for variable identifiers: `K` and `k` are distinct symbols.
- Deferred coding-style multi-character string variables such as `hello` to a later named-variable milestone.
- Recorded the interim rule that adjacent raw letters should be interpreted as multiplied single-character symbols where supported, or stopped as ambiguous/unsupported where the parser cannot prove that meaning.
- Recorded reserved-token semantic highlighting as future visible UX work, fed by `VARIABLE-CORE1` classifications rather than a separate editor-side parser policy.

Boundaries:

- No runtime behavior changes.
- No variable memory implementation.
- No solve-target UI.
- No named string variable support.
- No visible reserved-token highlighting yet.

## VARIABLE-CORE1

- [agent: codex | model: gpt-5.5] Implemented `VARIABLE-CORE1` as an internal symbol-discovery and variable-role substrate.

What changed:

- Added `src/lib/algebra/variable-core.ts` and focused tests.
- Added metadata-only discovery for supported LaTeX/MathJSON symbols, reserved functions, reserved constants, implicit adjacent-character products, deferred named-string variables, and unsupported identifiers.
- Added role metadata for solve targets, active variables, bound variables, symbolic parameters, stored-value candidates, and unsupported symbols.
- Updated `math-analysis` to expose richer variable metadata beside the existing `containsSymbolX` flag.
- Marked `variable-core` as `ready-with-adapter` in math capability readiness.

Boundaries:

- No visible behavior changes.
- No Equation target UI.
- No variable memory.
- No named string variable support.
- No visible semantic highlighting.
- No multivariable solving, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:unit -- src/lib/algebra/variable-core.test.ts src/lib/engine/math-analysis.test.ts src/lib/modes/equation.test.ts src/lib/modes/calculate.test.ts src/lib/calculus/calculus-core.test.ts src/lib/modes/table.test.ts src/lib/algebra/capability-readiness.test.ts`, `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## EQUATION-TARGET1

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-TARGET1` as the first visible Equation consumer of the shared variable core.

What changed:

- Added `src/lib/equation/equation-target.ts` for target discovery, selected-target fallback, parameterized-stop metadata, safe target-to-`x` retargeting, and result rewrites back to the selected target.
- Updated Equation symbolic solving so normal `x` equations keep the existing path.
- Added safe single-variable non-`x` solving for inputs such as `z+1=3` and `K^2=4`.
- Added a compact `Solve for` selector in the Equation symbolic workspace when multiple supported symbols appear.
- Multi-symbol equations now stop with explicit parameterized-target guidance instead of silently choosing `x`.
- Added focused unit and UI coverage for target discovery, retargeting, non-`x` solves, multi-target stops, and selector rendering.

Boundaries:

- No parameterized solving for cases such as `x+z=5`.
- No variable memory.
- No named string variables.
- No multivariable solving, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:unit -- src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts src/lib/algebra/capability-readiness.test.ts`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## Equation Parameterized Solving Roadmap

- [agent: codex | model: gpt-5.5] Captured `.memory/research/roadmaps/equation-parameterized-solving-roadmap.md` as the dedicated follow-on lane after `EQUATION-TARGET1`.

What changed:

- Locked `EQUATION-PARAM*` as selected-target parameterized Equation solving, separate from variable memory, named string variables, product-facing multivariable solving, and `POLY-ELIM2`.
- Sequenced the lane as:
  - `EQUATION-PARAM1`: affine and linear parameterized target solving.
  - `EQUATION-PARAM2`: bounded polynomial-in-target parameter families.
  - `EQUATION-PARAM3`: rational parameterized equations.
  - `EQUATION-PARAM4`: absolute-value, radical, and power carrier parameter families.
  - `EQUATION-PARAM5`: bounded exponential and logarithmic target isolation.
  - `EQUATION-PARAM6`: bounded trigonometric parameterized families.
  - `EQUATION-PARAM7`: readback and Guide polish after enough families exist.
- Recorded that this lane should grow from Calcwiz's existing one-variable strengths without jumping to broad transcendental algebra.
- Updated multivariable and POLY/RAT roadmaps so `POLY-ELIM2` stays blocked until target/parameter preservation is product-stable.

Boundaries:

- No runtime behavior changes.
- No new solver capability.
- No variable memory.
- No named string variable support.
- No multivariable solving, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

## EQUATION-PARAM1

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM1` as the first selected-target parameterized Equation capability.

What changed:

- Added `src/lib/equation/equation-parameterized-linear.ts` for affine/linear target-aware solving.
- Wired Equation mode so supported multi-symbol equations now solve after a target is selected.
- Supported examples include:
  - `x+z=5`, solve for `z`, returning `z=5-x`.
  - `K+k=8`, solve for `K`, returning `K=8-k`.
  - `2z+a=7`, solve for `z`, returning `z=\frac{7-a}{2}`.
  - `a z+b=c`, solve for `z`, returning `z=\frac{c-b}{a}` plus `a\ne0`.
  - `a z+b=c z+d`, solve for `z`, returning `z=\frac{d-b}{a-c}` plus `a-c\ne0`.
  - `x\cdot z=1`, solve for `z`, returning `z=\frac{1}{x}` plus `x\ne0`.
- Preserved non-target symbols as symbolic parameters, not stored numeric values.
- Kept raw adjacent-letter products such as `xz=1` unsupported until future variable hints or named-variable policy can make that meaning visible.

Boundaries:

- No polynomial-in-target parameter solving.
- No rational target-denominator solving.
- No absolute/radical/power, exponential/logarithmic, or trigonometric parameterized solving.
- No variable memory, named string variables, broad simplification, broad transcendental algebra, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Focused unit suite passed locally: `npm run test:unit -- src/lib/equation/equation-parameterized-linear.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts`.
- [agent: codex | model: gpt-5.5] UI suite passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## EQUATION-PARAM4

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM4` as the bounded nonperiodic carrier selected-target parameterized Equation slice.

What changed:

- Added `src/lib/equation/equation-parameterized-carrier.ts` for one-carrier absolute-value, square-root, and square-power target solving.
- Wired Equation mode to try parameterized families in order: affine/linear, quadratic, rational LCD clearing, then nonperiodic carrier solving.
- Supported examples include:
  - `|z-a|=b`, solve for `z`, returning a two-branch `z` set plus `b\ge0`.
  - `2|z-a|+c=d`, solve for `z`, isolating the carrier before branch solving.
  - `sqrt(z+a)=b`, solve for `z`, returning `z=b^2-a` plus `b\ge0`.
  - `sqrt(z-a)+c=b`, solve for `z`, returning `z=(b-c)^2+a` plus `b-c\ge0`.
  - `(z-a)^2=b`, solve for `z`, returning a square-root branch set plus `b\ge0`.
  - `|1/(z-a)|=b`, solve for `z`, delegating rational branch equations to `EQUATION-PARAM3` and preserving denominator facts.
- Recorded that PARAM4 is historically related to the old `COMP` composition lane, but deliberately does not reopen periodic or deep composition.
- Recorded restriction-formatting polish such as `(...)^{-1}` versus `1/(...)` as deferred readback work, likely `EQUATION-PARAM-READBACK1` or `EQUATION-PARAM7`.

Boundaries:

- No periodic trig composition.
- No deep inverse-function composition.
- No nested carrier towers.
- No variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Focused unit suite passed locally: `npm run test:unit -- src/lib/equation/equation-parameterized-carrier.test.ts src/lib/equation/equation-parameterized-rational.test.ts src/lib/equation/equation-parameterized-polynomial.test.ts src/lib/equation/equation-parameterized-linear.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts`.
- [agent: codex | model: gpt-5.5] UI suite passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## EQUATION-PARAM2

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM2` as the real-guarded quadratic selected-target parameterized Equation slice.

What changed:

- Added `src/lib/equation/equation-parameterized-polynomial.ts` for bounded degree-2 collection in the selected target.
- Wired Equation mode to try `EQUATION-PARAM1` affine/linear solving first, then `EQUATION-PARAM2` quadratic solving.
- Supported examples include:
  - `z^2-a=0`, solve for `z`, returning a `z` root set plus a real-domain `a\ge0` fact.
  - `z^2+x z+1=0`, solve for `z`, preserving `x` as a symbolic parameter and requiring `x^2-4\ge0`.
  - `a z^2+b z+c=0`, solve for `z`, returning formula-style roots plus `a\ne0` and discriminant facts.
  - `K^2-k=0`, solve for `K`, preserving case sensitivity.
- Kept raw adjacent-letter products such as `xz` unsupported unless multiplication is explicit.

Boundaries:

- No higher-degree parameterized polynomial solving.
- No rational target-denominator solving.
- No absolute/radical/power, exponential/logarithmic, or trigonometric parameterized solving.
- No variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Focused unit suite passed locally: `npm run test:unit -- src/lib/equation/equation-parameterized-polynomial.test.ts src/lib/equation/equation-parameterized-linear.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts`.
- [agent: codex | model: gpt-5.5] UI suite passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## EQUATION-PARAM3

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM3` as the bounded rational selected-target parameterized Equation slice.

What changed:

- Added `src/lib/equation/equation-parameterized-rational.ts` for selected-target rational expression collection, LCD clearing, original denominator fact preservation, and delegation into existing `EQUATION-PARAM1`/`EQUATION-PARAM2` cleared equations.
- Wired Equation mode to try parameterized families in order: affine/linear, quadratic, then rational LCD clearing.
- Supported examples include:
  - `\frac{1}{z-a}=b`, solve for `z`, returning `z=\frac{ab+1}{b}` plus `z-a\ne0` and `b\ne0`.
  - `\frac{z+1}{z-a}=2`, solve for `z`, returning `z=2a+1` plus `z-a\ne0`.
  - `\frac{z-a}{z+b}=c`, solve for `z`, returning `z=\frac{bc+a}{1-c}` plus denominator and coefficient facts.
  - `\frac{1}{z-a}+\frac{1}{z+b}=c`, solve for `z`, delegating the cleared quadratic to the real-guarded PARAM2 path.
  - `\frac{K}{K-k}=2`, solve for `K`, preserving case sensitivity.
- Kept raw adjacent-letter products such as `xz` unsupported unless multiplication is explicit.

Boundaries:

- No broad rational simplification.
- No cleared equations above degree 2.
- No nested target-denominator rational families.
- No variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Focused unit suite passed locally: `npm run test:unit -- src/lib/equation/equation-parameterized-rational.test.ts src/lib/equation/equation-parameterized-polynomial.test.ts src/lib/equation/equation-parameterized-linear.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts`.
- [agent: codex | model: gpt-5.5] UI suite passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.

## EQUATION-PARAM5

- [agent: codex | model: gpt-5.5] Implemented `EQUATION-PARAM5` as the bounded exp/log inverse-pair selected-target parameterized Equation slice.

What changed:

- Added `src/lib/equation/equation-parameterized-exp-log.ts` for one supported exp/log carrier in the selected target.
- Wired Equation mode to try parameterized families in order: affine/linear, quadratic, rational LCD clearing, nonperiodic carrier, then exp/log inverse-pair isolation.
- Supported examples include:
  - `e^z=a`, solve for `z`, returning `z=ln(a)` plus `a>0`.
  - `e^(z+a)=b`, solve for `z`, returning a `ln(b)-a` style result plus `b>0`.
  - `ln(z+a)=b`, solve for `z`, returning `z=e^b-a` plus a log-domain fact.
  - `log(z+a)=b`, solve for `z`, returning `z=10^b-a`.
  - `2^(z+a)=b`, solve for `z`, returning a `log_2(b)-a` style result plus `b>0`.
  - `ln(z^2+a)=b`, `ln(1/(z-a))=b`, and `e^(|z-a|)=b`, which delegate to existing selected-target quadratic, rational, and nonperiodic-carrier solvers.
- Preserved positive-output, log-domain, denominator, and delegated branch facts.
- Tightened generated rational handoff formatting so exp/log coefficients remain explicit products when PARAM3 clears denominators.
- Fixed a `-0` collector edge in parameterized linear/polynomial helpers so negated zero coefficients do not look target-bearing.

Boundaries:

- No symbolic bases.
- No log-combine sums/quotients.
- No Lambert W.
- No nested exp/log towers.
- No broad transcendental algebra.
- No variable memory, named string variables, bivariate elimination, `POLY-ELIM2`, graphing, Labs runner work, source-mirror execution, or copied source.

Verification:

- [agent: codex | model: gpt-5.5] Focused unit suite passed locally: `npm run test:unit -- src/lib/equation/equation-parameterized-exp-log.test.ts src/lib/equation/equation-parameterized-carrier.test.ts src/lib/equation/equation-parameterized-rational.test.ts src/lib/equation/equation-parameterized-polynomial.test.ts src/lib/equation/equation-parameterized-linear.test.ts src/lib/equation/equation-target.test.ts src/lib/modes/equation.test.ts src/lib/engine/math-analysis.test.ts src/lib/algebra/variable-core.test.ts`.
- [agent: codex | model: gpt-5.5] UI suite passed locally: `npm run test:ui -- src/AppMain.ui.test.tsx`.
- [agent: codex | model: gpt-5.5] Passed locally: `npm run test:golden`, `npm run test:memory-protocol`, `npm run lint`, and `npm run build`.
