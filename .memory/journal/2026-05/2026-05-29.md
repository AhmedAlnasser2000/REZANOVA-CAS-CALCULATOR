# 2026-05-29

## OOE-RS19 Table Stale Commit Gate And Roadmap Extension

- [agent: codex | model: gpt-5] Implemented `OOE-RS19` as the Table parity stale-commit gate after `EQUATION-ANSWER-MODES1`.
- Added canonical Table OOE snapshot/input-revision helpers covering formulas, secondary enabled state, range, step, stored variables, and replay substitution snapshots.
- Updated the active `useTableRuntime` hook so completed Table builds compare their job input revision against the latest active Table request before committing.
- Stale Table completions are silently dropped; the previous visible table/result remains and replay substitution snapshots are not cleared.
- Preserved the legacy `modeActionHandlers.ts` Table path and all Table math/row/warning/history/result wording behavior.
- Extended the OOE roadmap beyond RS18/RS19: `OOE-RS20` central runtime coordinator, `OOE-RS21` editor analysis budget lane, `OOE-RS22` diagnostics trace buffer, `OOE-RS23` host adapter contract, `OOE-RS24` cooperative budget/cancellation pilot, and `OOE-RS25` first isolated runtime pilot.
- Reaffirmed the product boundary: OOE is Calcwiz's traffic controller for ordering, priority, budgets, stale commits, cancellation contracts, host routing, and diagnostics; Progressive Solver is a separate future execution strategy that can depend on OOE but is not OOE itself.
- Preserved boundaries: no central coordinator, scheduler, cancellation enforcement, trace UI, MCP endpoint, worker/Rust host migration, Progressive Solver behavior, broad routing change, result schema change, history schema change, or UI layout change.

## OOE-RS20 Central Runtime Coordinator

- [agent: codex | model: gpt-5] Implemented `OOE-RS20` as the first central OOE lifecycle coordinator.
- Added a shared coordinator for existing OOE-covered lanes: standard Calculate expression actions, Equation symbolic/numeric interval, and active Table builds.
- Coordinator now owns job identity start, active registry start, OOE plan preflight, existing TypeScript runtime execution, post-run commit assessment, registry completion/failure, and envelope return.
- Preserved the important stale-gate timing: active input revision is resolved after runtime execution so edits during a job can still stale-drop.
- Migrated expression, Equation, and Table OOE pilots to the coordinator without changing their public wrapper APIs or visible payload semantics.
- Kept Equation guarded stage trace metadata and stage order intact.
- Preserved boundaries: no scheduler, budget policy, trace buffer, MCP diagnostics, Rust solver execution, worker isolation, Progressive Solver behavior, UI change, result schema change, history schema change, or new math capability.

## OOE-RS21 Editor Analysis Budget Lane

- [agent: codex | model: gpt-5] Implemented `OOE-RS21` as the first OOE budget lane for editor-side background work.
- Added Rust built-in editor-analysis plans for variable hints, Equation target discovery, Calculate transform eligibility, Equation transform eligibility, and preview render handoff.
- Added TypeScript editor-analysis OOE helpers that build stable source/context/generation snapshots and run debounced analysis through the central OOE coordinator.
- Variable hints, Equation target discovery, transform eligibility, and preview LaTeX handoff now stale-drop or skip outdated editor-analysis results instead of replacing the last safe value.
- Existing debounce, huge-input guard, Stop, Restart Editor, and header status semantics remain intact.
- Preserved boundaries: no typing behavior change, solver routing, solver output change, history/result schema change, scheduler, worker sandbox, Rust solver execution, Progressive Solver behavior, MCP endpoint, trace UI, or new math capability.
- Next OOE milestone remains `OOE-RS22`: diagnostics trace buffer with solver provenance.

## OOE-RS22 App-Wide Diagnostics Trace Buffer And Provenance

- [agent: codex | model: gpt-5] Implemented `OOE-RS22` as the first developer/test-visible OOE diagnostics and provenance layer.
- Added a bounded in-memory diagnostics buffer with record/list/latest/reset helpers.
- Extended Rust/bridge built-in plan categories to cover provenance-only executable workspaces: Calculate workbench/algebra transform, Advanced Calc, Trigonometry, Statistics, Geometry, Matrix, and Vector.
- Central OOE coordinator now records diagnostics for completed, stale-dropped, skipped, and failed jobs after metadata is built.
- Standard Calculate, Equation, Table, and editor-analysis pilots now record compact provenance through the coordinator.
- Added richer Equation provenance for answer mode, selected target, guarded stage attempts, winning stage, stop summary, generated isolation/rewrite details, and output hygiene status when available.
- Added generic workspace provenance wrapping for non-Equation executable modes while keeping Guide and Labs excluded.
- Preserved boundaries: no public diagnostics UI, no Tauri trace command, no MCP endpoint, no scheduler, no cancellation enforcement change, no worker/Rust migration, no Progressive Solver behavior, no history/result schema change, and no solver behavior change.
- Next OOE milestone is `OOE-RS23`: host adapter contract.

## OOE-RS23 Host Adapter Contract

- [agent: codex | model: gpt-5] Implemented `OOE-RS23` as the host adapter contract layer for OOE.
- Added Rust-owned built-in host descriptors for current active TypeScript hosts and schema-only future host kinds for worker, iframe, Rust/Tauri command, and progressive runner hosts.
- Added Tauri diagnostics commands and TypeScript bridge schemas for listing and fetching built-in host descriptors.
- Added a TypeScript host adapter resolver that reports ready, unavailable, missing-host, incompatible-host, and bridge-error states without blocking runtime execution.
- Updated the central OOE coordinator so every job carries host adapter metadata in its internal envelope and diagnostics record.
- Preserved boundaries: no scheduler, budget enforcement, cancellation enforcement, public diagnostics UI, MCP endpoint, worker/iframe/Rust host migration, Progressive Solver behavior, history/result schema change, result wording change, or solver behavior change.
- Next OOE milestone is `OOE-RS24`: cooperative budget and cancellation pilot.

## OOE-RS24 Cooperative Table Budget And Cancellation Pilot

- [agent: codex | model: gpt-5] Implemented `OOE-RS24` as the first OOE milestone where the traffic controller actively influences running work.
- Chose active Table build as the pilot lane; `OOE-RS25` remains the first isolated runtime pilot.
- Marked `table-runtime` and the `table.build` built-in plan as cooperative for budget/cancellation metadata while keeping execution on the current main-thread TypeScript host.
- Added a cooperative runtime context to the central OOE coordinator with registry ID, cancellation query, checkpoint, and budget-yield helpers.
- Added cooperative Table row generation through the OOE wrapper while preserving synchronous `runTableMode` as the behavior-stable reference path.
- Cancelled Table builds now stop at cooperative checkpoints, record terminal `cancelled` diagnostics/registry state, and commit a controlled cancellation note without replacing the previous `TableResponse` or clearing replay substitutions.
- Preserved boundaries: no Equation cancellation, Progressive Solver, worker/iframe/Rust host migration, scheduler, history schema change, result schema change, or Table math behavior change.
- Post-RS25 expansion is recorded separately: `OOE-RS26` Equation guarded-stage cancellation checkpoints, `OOE-RS27` Equation heavy-helper isolation pilot, and `OOE-RS28` broader Equation cancellation coverage.

## OOE-RS25 First Isolated Runtime Pilot

- [agent: codex | model: gpt-5] Implemented `OOE-RS25` as the first isolated runtime pilot, using active Table build as the bounded lane.
- Added Rust OOE host metadata for `table-worker-runtime` as a Web Worker host with isolated budget policy, hard-stop cancellation, and worker-safe thread safety.
- Switched the built-in `table.build` plan to `table-worker-runtime` while retaining `table-runtime` as the cooperative main-thread fallback host.
- Extracted pure Table execution into `src/lib/modes/table-core.ts`, added a Vite module worker, and added a worker client that cleans up workers/listeners/timers across completion, cancellation, fallback, and failure.
- The Table OOE wrapper now attempts the worker first and falls back to the cooperative main-thread Table path when the worker is unavailable or reports failure.
- Successful worker/fallback Table payloads preserve parity with `runTableMode`; cancellation terminates the worker and returns the controlled cancellation note.
- OOE diagnostics/provenance record worker, fallback, and cancellation host execution metadata without storing table rows.
- Preserved boundaries: no Equation cancellation, Progressive Solver behavior, Rust solver execution, public diagnostics UI, MCP endpoint, history/result schema change, row-limit change, or Table math semantic change.
