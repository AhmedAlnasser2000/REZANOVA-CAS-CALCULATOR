# Editor source context

This folder is reserved for document-editor research context that is separate from
the math/CAS source mirrors under `playground/sources/mirrors/`.

Use it for editor interaction-model notes, metadata, and optional local source
mirror pointers. Do not make production code depend on this folder.

